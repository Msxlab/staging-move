LocateFlow Uctan Uca Sistem Denetim Raporu
Tarih: 2026-06-11
Kapsam: web, admin, mobile, shared paketler, Prisma schema, connectors, odeme, cron/deploy, SEO/GEO ve capraz katman veri akislari.

Varsayimlar
- Uretim secret degerleri okunmadi; env/deploy tespiti statik dosya ve runtime-config kodu uzerinden yapildi.
- Gercek Stripe/App Store/Play Store islemi yapilmadi; odeme/IAP bulgulari kod, config, schema, unit test ve lokal E2E analizi kanitidir.
- GitHub Actions cron workflow prod scheduler kabul edilirse cron kapsami daha iyi; Vercel/Ofelia tek scheduler olursa cron bulgularinin etkisi artar.
- Proje .md/memory dosyalari denetim kaniti olarak kullanilmadi; rapor kod, config, schema ve calistirilan testlerden uretildi.

Dogrulama
- Web unit: 258 test dosyasi / 2275 test gecti; web typecheck gecti.
- Admin unit: 114 test dosyasi / 717 test gecti; admin typecheck gecti.
- Shared typecheck gecti; connectors typecheck gecti; connectors unit: 15 test dosyasi / 105 test gecti.
- Mobile typecheck gecti; mobile unit testlerde 4 IAP recovery testi __DEV__ ReferenceError nedeniyle basarisiz, 228 test gecti.
- Web Playwright public smoke: 5 gecti, 8 basarisiz. Baslica kanitlar: ciddi kontrast ihlalleri, FAQ JSON-LD eksikligi, robots dev/prod beklenti farki, auth selector belirsizligi.
- Local public crawl: 21 sayfa ve 27 internal link tarandi; kirik internal link bulunmadi.
- DB package icinde lint script yok; Node 24.12 ile calisirken repo engine 22.x uyarisi uretildi.

Bulgu Sayisi: Toplam 99 | Kritik 0 | Yuksek 25 | Orta 55 | Dusuk 19

Bulgular
Katman | Onem | Bulgu | Neden sorun | Onerilen cozum
Admin / Auth / Forced Password Change | Yüksek | /api/auth/force-password-change accepts any valid admin session and does not require admin.mustChangePassword=true before setting a new password. | Any authenticated admin session can change its own password without current-password or MFA step-up by calling the forced-rotation endpoint directly, bypassing the normal /api/auth/password safeguards. | Before updating password, require the DB row to have mustChangePassword=true; otherwise return 403 and route normal password changes through the step-up protected endpoint. Revoke all active sessions after successful forced rotation.
Admin / Billing / Manual Entitlement Validation | Yüksek | validateBillingCombination only checks some required dates when fields are explicitly sent; changing status/provider to TRIALING, FREE_ACCESS, or ADMIN ACTIVE can pass even when the existing row lacks trialEndsAt/freeAccessEndsAt/premiumUntil. | Admin can create entitlements the resolver cannot reason about, including accidental indefinite free/manual access or trial states with no expiry. | Validate against the fully merged next subscription state, not just request fields; require expiry fields for every status/provider combination and add matrix tests.
Admin / Billing / Workspace Seat Reconciliation | Yüksek | Admin user subscription PATCH, subscription change-plan, cancel, and resync update plan/status but do not reconcile workspaces owned by that user. | Family/Pro upgrades, downgrades, cancellations, or manual grants can leave workspace members ACTIVE/OVERFLOW inconsistent with the owner's new entitlement until another path reconciles seats. | After every admin billing lifecycle mutation, call shared workspace seat reconciliation for owned workspaces and include overflow/restored counts in the audit.
Admin / Mobile Billing / IAP Revalidation | Yüksek | /api/subscriptions/[id]/revalidate for APP_STORE and PLAY_STORE does not call App Store Server API or Google Play Developer API; it stamps lastValidatedAt/lastSyncedAt when a stored transaction id or purchase token exists. | Admin can mark a store subscription as revalidated even if it is expired, refunded, revoked, or otherwise invalid at the provider, leaving stale mobile entitlements and misleading billing health. | Move Apple/Google receipt validators into a shared package or internal web endpoint and have admin revalidate fetch provider truth, update status/period/grace/refund fields, and audit the provider result; do not stamp lastValidatedAt on credential presence alone.
Admin / Notifications / Deleted Users | Yüksek | Admin broadcast and single-user notification sends query prisma.user without deletedAt filters and dispatchEmailBatch/dispatchPushBatch also select recipients without excluding soft-deleted users. | Soft-deleted or deletion-pending users can receive admin emails/pushes and get new notification rows, violating deletion/privacy expectations and creating compliance risk. | Restrict broadcast and single-user sends to active users only; add deletedAt=null checks in recipient lookup, dispatch helpers, and tests for soft-deleted targets.
Admin / Workspace / Billing Transfer | Yüksek | Admin workspace transfer-ownership repoints Workspace.ownerUserId and promotes the new owner, but does not transfer billing responsibility, verify the target owner's eligible plan, or cancel/notify the old payer. | The admin route can create the same entitlement/billing split as the web route: the former owner may keep paying while the workspace's seat limit is derived from a different user. | Separate administrative owner from billing owner, or require an explicit billing transfer workflow with subscription eligibility checks, payer notification, and Stripe/customer reconciliation.
Mobile / Auth Bridge / Web-Owned Flows | Yüksek | Mobile opens owned web URLs with only ?embed=mobile; it does not hand off the bearer session to a web cookie before connector OAuth or web billing pages. | Password-login and native-Apple mobile users can be fully authenticated in the app but hit unauthenticated web-cookie-only flows such as /api/partner-consents/oauth/initiate and /settings/subscription. | Add a short-lived mobile-to-web session handoff endpoint that sets the web cookie from the current bearer token, or replace those flows with native/bearer-compatible endpoints.
Mobile / Workspace Context | Yüksek | Mobile has a workspace picker but the API client never sends X-Workspace-Id and no mobile code references that header. | Address, service, moving, budget, provider, export, and connector calls fall back to the backend default workspace instead of the workspace the user selected in Settings. | Persist selected workspace id in mobile state/AsyncStorage and attach x-workspace-id to all domain API calls; refresh it after invite accept, leave, create, and ownership changes.
Shared / Billing / Workspace | Yüksek | Family/Pro self-serve checkout, WORKSPACE_MODEL_ENABLED kapalıyken de çalışabiliyor | Checkout akışı sadece price ID ve terms kabulünü kontrol ediyor; workspace provisioning ve /api/workspaces yüzeyi flag kapalıyken inert. Price ID'ler canlıya girerse kullanıcı shared workspace vaadi için ödeme yapıp özelliği göremeyebilir | Family/Pro checkout ve pricing CTA'larını workspace flag + backfill/readiness koşuluna bağla; flag kapalıysa satışı kapat veya açık “coming soon/admin grant only” durumuna düşür
Shared / Deploy / Env Parity | Yüksek | docker-compose.prod.yml web/admin env whitelist'i Family/Pro Stripe ve mobile IAP anahtarlarını container'a taşımıyor | .env.production içinde değer olsa bile compose environment listesinde STRIPE_PRICE_FAMILY_*, STRIPE_PRICE_PRO_*, MOBILE_*, APPLE_APP_STORE_* ve GOOGLE_PLAY_* yok; web/admin billing ve IAP endpointleri null konfigürasyonla çalışır veya 503 döner | compose env listesini runtime-config/readiness anahtarlarıyla eşitle veya env_file ile tüm gerekli anahtarları geçir; CI'da compose-vs-runtime-config parity testi ekle
Shared / Env Templates | Yüksek | .env.example ve .env.production.example Family/Pro Stripe price ID'lerini içermiyor | production-readiness ve checkout Family/Pro için STRIPE_PRICE_FAMILY_* / STRIPE_PRICE_PRO_* bekliyor; template'i kopyalayan deploy ya /api/ready 503 alır ya da Family/Pro checkout kullanılamaz | Template'lere dört Family/Pro Stripe price ID'sini ekle; stripe sync scriptini Individual-only olmaktan çıkar veya Family/Pro çıktısı üret
Shared / Mobile Billing Readiness | Yüksek | Production EAS store purchases açık, server readiness IAP ürün/credential hazır olmasını zorunlu tutmuyor | Mobil prod build purchase UI'ı açıyor; fakat /api/mobile/iap/products ürünleri runtime config'den okuyor ve /api/mobile/iap/verify Apple/Google credential yoksa 503/424 dönebiliyor. Yayın günü satın alma kullanıcıdan ücret alıp entitlement veremeyebilir | Mobile store flag'leri açıkken IAP product IDs, APPLE_BUNDLE_ID, Apple Server API, Google package/API/RTDN anahtarlarını readiness gate'e ekle; yayın öncesi products+verify smoke testi koş
Web / Accessibility / Public Pages | Yüksek | Playwright axe smoke on /, /sign-in, /sign-up, and /pricing found serious color-contrast violations; examples include white text on #2c7ac3 at 4.48:1, primary #2c7ac3 on #f2f4f7 at 4.07:1, honey #b98318 on #f2efe9 at 2.89:1, and pricing plan colors around 2.64-3.48:1. | Public acquisition and auth pages do not meet WCAG 2 AA contrast expectations for multiple buttons, chips, badges, and pricing table labels. | Darken foreground accent tokens or lighten backgrounds, then rerun apps/web/tests/e2e/accessibility.spec.ts until serious/critical axe violations are zero.
Web / Budget / Workspace | Yüksek | Budget month drill-down page is not workspace-aware. | A user viewing a shared workspace sees summary totals from scoped APIs but drill-down data from only their own records, causing mismatched financial decisions. | Convert drill-down loaders/actions to the same workspace scope used by budget summary APIs.
Web / Cron / Reminder Scheduling | Yüksek | Reminder routes self-gate on local 8am using isReminderDeliveryHour, but Vercel/Ofelia configs still call user-facing reminders at fixed 07:00/08:00/10:00 UTC; only .github/workflows/cron.yml has the required 12:00-18:00 UTC per-US-zone slots. | If deployment uses Vercel crons or Ofelia instead of GitHub Actions, US users will mostly be skipped because 08:00 UTC is not 08:00 local for US zones. | Keep exactly one active production scheduler or update all scheduler configs to the same 12-18 UTC fan-out; add a CI check that scheduler configs include every local-hour gated route and slot.
Web / Cron / Scheduled Delivery | Yüksek | A scheduled-delivery worker exists and the GitHub Actions cron calls it, but Vercel and Ofelia scheduler configs omit /api/cron/scheduled-delivery and the admin notifications API still advertises schedulingEnabled=false/workerEnabled=false while rejecting future sendAt. Evidence: apps/web/src/app/api/cron/scheduled-delivery/route.ts, .github/workflows/cron.yml, apps/web/vercel.json, docker/ofelia.ini, apps/admin/src/app/api/notifications/route.ts. | Scheduled NotificationQueue rows and future notification UX depend on which scheduler/deploy target is active; one deployment path drains the queue, the others never do, and admin code contradicts the worker's existence. | Pick one canonical scheduler contract; add scheduled-delivery to every production scheduler or remove the route, then enable admin scheduling only after queue retries/preferences are aligned.
Web / Email / Unsubscribe | Yüksek | One-click unsubscribe and Resend bounce/complaint suppression write only legacy MARKETING/REMINDER/LIFECYCLE preference rows, while current settings use granular rows such as BILL_REMINDER, BILL_OVERDUE, CONTRACT_EXPIRY, TASK_REMINDER, MOVE_ALERT, WEEKLY_DIGEST, and MONTHLY_REPORT. Evidence: apps/web/src/lib/unsubscribe.ts, unsubscribe-actions.ts, notification-preferences.ts. | Existing senders often still check legacy rows, but the settings UI can remain opted-in and any granular sender can regress unsubscribe/complaint compliance. | On unsubscribe/bounce/complaint, disable both legacy and all current EMAIL preference types in the selected category; add regression tests where direct granular rows are true before unsubscribe.
Web / Email Unsubscribe | Yüksek | /unsubscribe human confirmation page is not in the public middleware allow-list. | Email recipients who are logged out may be redirected instead of reaching an unsubscribe confirmation flow, risking compliance and trust issues. | Add /unsubscribe to public routes and keep token validation inside the page/API.
Web / Export / Workspace | Yüksek | Settings export UI lists workspace-visible addresses, but JSON/PDF export routes fetch user-owned records only. Evidence: apps/web/src/app/(app)/settings/export/page.tsx uses /api/addresses, while apps/web/src/app/api/export/route.ts and apps/web/src/app/api/export/pdf/route.ts query by userId. | In Family/Pro workspaces, visible shared records can fail PDF export or be omitted from full exports, producing incomplete data archives and inconsistent privacy/product behavior. | Decide explicit export semantics. If export means current workspace data, use workspace data scope helpers; if export means personal GDPR data, hide shared records in the UI and label the export as owner-only.
Web / Moving / Workspace | Yüksek | /api/moving/migration is not workspace-scoped; it only queries user-owned addresses/services. | Family/Pro shared workspace migration analysis ignores shared records visible elsewhere, producing incomplete move plans. | Use resolveWorkspaceDataScope/scopedRecordWhere consistently for migration queries.
Web / Payment / Pricing | Yüksek | Family/Pro self-serve checkout does not persist Individual-level disclosure and consent snapshot. Evidence: apps/web/src/app/api/stripe/checkout/route.ts around legal disclosure creation; packages/db/prisma/schema.prisma IndividualConsent. | Checkout records ToS/privacy metadata but not the regulated disclosure/consent model used by the app's legal domain, so higher-risk plan purchases may be auditable only through weak Stripe metadata. | Before Stripe session creation, require the plan-specific disclosure acknowledgement and store IndividualConsent/LegalAcceptance with plan, billing interval, IP/user agent, and versioned document IDs.
Web / Providers / Workspace | Yüksek | Provider detail and compare address context are not workspace-aware. | Provider recommendations for shared/family workspaces can use only personal addresses/services, making recommendations inconsistent with the active workspace. | Apply workspace context to provider detail and comparison queries or clearly label them as personal-only.
Web / Workspace / Billing | Yüksek | Ownership transfer changes Workspace.ownerUserId, which is also the entitlement/seat anchor, without transferring/canceling Stripe billing or requiring the target owner to have an eligible plan. Evidence: apps/web/src/lib/workspace-ownership.ts and apps/web/src/app/api/workspaces/[id]/transfer/route.ts. | Paid Family/Pro access can become governed by a free target user's subscription while the former owner may still pay, causing seat collapse, billing confusion, and incorrect entitlement enforcement. | Treat billing owner and administrative owner separately, or block/sequence transfer until the target accepts billing responsibility and has the required subscription.
Web / Workspace / Invitations | Yüksek | Token-based invite accept requires only getUserSession plus email string match, while in-app accept requires requireVerifiedUser. Evidence: apps/web/src/app/api/invitations/[token]/accept/route.ts vs apps/web/src/app/api/invitations/pending/[id]/accept/route.ts. | A user can sign in with an unverified password account for the invited email and join a workspace before proving mailbox ownership. | Require requireVerifiedUser for token accept too, and route unverified users to email verification before accepting the invitation.
Web / Workspace / Privacy | Yüksek | Product copy says members keep private data, but invitation acceptance backfills existing addresses/services/moving plans into the shared workspace and non-child members read workspace-wide records. Evidence: apps/web/src/lib/workspace-invite-accept.ts backfills records; apps/web/src/lib/workspace-data-scope.ts scopedRecordWhere uses workspaceId; apps/web/src/app/api/addresses/route.ts decrypts formattedAddress for workspace reads; apps/web/src/app/(app)/settings/workspace/page.tsx says "private data". | A new household/team member can expose existing address and service metadata to other members contrary to the user-facing promise, creating privacy and trust risk. | Choose one model and enforce it: either change copy/onboarding consent to clearly say records are shared, or add per-record/member visibility and avoid automatic backfill without explicit consent.
Admin / Billing / Lifecycle Concurrency | Orta | Admin subscription change-plan uses subscription.version only in the Stripe idempotency key and updates the local row by id without a version guard. | Two admins can preview and submit competing plan changes concurrently; both may hit Stripe and the last local DB update wins without detecting the stale view. | Add optimistic concurrency on the local update where id+version, return 409 on stale version, and include previewed version in the POST body.
Admin / Blog / Category Validation | Orta | Blog post PATCH accepts categoryId as a raw string and does not verify that the category exists or matches the post locale, unlike tagIds which are checked in-transaction. | Invalid category ids can become 500s via FK errors, and cross-locale category links can break public category pages, cards, and SEO grouping. | Resolve categoryId inside the same transaction, require locale match with the target post locale, and return 400/409 for invalid category selections.
Admin / Connectors / Fallback Link Control | Orta | Connector fallback action writes are admin-editable without step-up and allow any https external URL for DEEP_LINK/PDF actions; web /api/connectors/catalog exposes the resolved URL to users. | A compromised admin session can swap guided-update CTAs to a malicious external site that appears in the LocateFlow connection flow. | Require MFA step-up for fallback URL changes and restrict external hosts per connector allowlist; keep relative internal URLs as the default.
Admin / MFA / Verification Rate Limit | Orta | /api/auth/mfa/verify checks only a 6-digit TOTP code and has no dedicated per-admin/IP failure counter or lockout. | A stolen active session during MFA enrollment can brute-force the setup TOTP endpoint within the broad middleware mutation limiter; the broad limiter is per-process and not tied to MFA failures. | Reuse the admin step-up store for admin_mfa_verify failures, key it by adminId/session/IP, return 429 with Retry-After after a small number of invalid codes, and audit failed attempts.
Admin / Notifications / Link Safety | Orta | Admin-created notification href accepts any string and email rendering treats absolute http(s) hrefs as direct CTA URLs. | A compromised or mistaken admin can broadcast a LocateFlow-branded email/push that links users to an arbitrary external domain. | Restrict href to same-origin paths by default; if external links are needed, require an allowlist, explicit warning, and step-up/audit metadata.
Admin / Providers / Bulk Change Control | Orta | Bulk activate/deactivate/category/score/affiliate actions can change up to 200 public catalog or revenue-affecting rows with ADMIN permission but without password/MFA step-up. | A hijacked admin session or accidental click can mass-change public provider visibility and affiliate behavior; only delete currently gets step-up. | Require requirePasswordConfirm with MFA for bulk public/revenue-impacting operations, or split low-risk metadata edits from public activation and affiliate changes.
Admin / Providers / Bulk Data Integrity | Orta | Provider bulk actions other than delete do not consistently filter deletedAt=null, and activate can set isActive=true on a soft-deleted provider without restoring deletedAt. | The public catalog can accumulate impossible states such as deletedAt set but active true, and later queries that forget deletedAt can count or expose stale provider rows. | Add deletedAt:null to every non-restore bulk action and implement an explicit restore flow if reactivation of deleted providers is desired.
Admin / RBAC / Page Guards | Orta | Many privileged admin pages under (admin) rely only on the generic requirePageAdmin layout and do not call requirePagePermission/requirePageRole themselves; examples include users, providers, analytics, reports, tickets, support, moving, state-rules, help-center, and blog pages. | Direct URL access by a lower-privilege active admin can render privileged page chrome/actions and then fail at API fetch time, contradicting the page-guard contract that privileged admin pages should fail closed before rendering. | Add small server wrapper pages with requirePagePermission for every admin module, matching the API resource/action and minimum role; keep client components as children after the guard succeeds.
Admin / Reports / Date Validation | Orta | /api/reports parses startDate/endDate with new Date() and does not reject invalid dates or startDate after endDate before querying. | Bad query params can produce 500s or misleading empty/huge ranges instead of a controlled validation response. | Validate YYYY-MM-DD/ISO inputs, enforce start<=end and a maximum range, and return 400 for invalid filters.
Admin / Reports / Query Scale | Orta | /api/reports pulls every user in the range into memory to build dailyUsers and runs date-window counts without a range cap. | A large date range can turn a dashboard card into a slow unbounded scan and memory-heavy response. | Aggregate daily users in SQL/groupBy, cap report ranges, and add indexes/tests for report windows.
Admin / Support / Ticket Lifecycle | Orta | Ticket PATCH sets resolvedAt/closedAt when moving to RESOLVED/CLOSED but does not clear those timestamps when a ticket is reopened or moved back to OPEN/IN_PROGRESS/WAITING_USER. | Reopened tickets can still look resolved/closed in SLA, reporting, and downstream analytics. | On non-terminal status transitions clear resolvedAt/closedAt as appropriate, and add tests for CLOSED->OPEN and RESOLVED->IN_PROGRESS.
Admin / Support / Ticket List Performance | Orta | /api/tickets loads all matching tickets into memory to custom-sort by priority before slicing the requested page. | Large support queues turn a paginated endpoint into an unbounded scan, increasing latency and memory usage for every admin list view. | Push priority ranking into SQL with CASE/orderByRaw or store a numeric priorityRank column and paginate in the database.
Admin / Users / PII Redaction | Orta | redactUserDetail comments say impersonation context is SUPER_ADMIN-only and deviceName is masked even for ADMIN, but the code redacts impersonatedByAdminId and deviceName only for non-ADMIN roles. | Regular ADMIN users can receive impersonation actor ids and raw user device labels on user-detail responses, exposing operational/admin context and user-chosen device PII beyond the documented redaction policy. | Change the checks to redact impersonatedByAdminId for every role except SUPER_ADMIN and mask deviceName for ADMIN as documented; add unit tests for VIEWER, ADMIN, and SUPER_ADMIN response shapes.
Mobile / Address Lifecycle | Orta | Mobile address list and detail classify Past addresses by endDate, but new/edit address screens do not expose endDate and edit omits startDate too. | Users cannot mark an old address as past or correct move-in/out dates from mobile even though the UI warns about past addresses with services. | Add startDate/endDate controls to mobile edit/new flows and share the same semantic date validation used by the backend/web.
Mobile / Address Validation | Orta | Mobile validates state only by length and zip by regex; it defines US_STATES but does not enforce membership. | Invalid state abbreviations such as XX can be submitted until backend rejection or can produce poor provider/state-rule recommendations if accepted elsewhere. | Use the shared address validator or enforce US state membership in mobile before save; keep autocomplete metadata only when the normalized fields still match.
Mobile / Apple Native Auth | Orta | Native Apple sign-in does not generate/send a nonce, and the server only checks nonce when both request and token include one. | A valid Apple identity token is still verified, but replay resistance is weaker than the intended nonce-bound OAuth pattern. | Generate a cryptographic nonce in the client, pass it to Apple sign-in, and require the expected nonce/hash in /api/mobile/auth/apple/native.
Mobile / Build Config / Preview Safety | Orta | preview and staging-preview EAS profiles point at https://locateflow.com/api and https://locateflow.com. | Internal QA builds can mutate production accounts/data while store purchases are disabled, making testing safer for billing but not for core data. | Add a real staging backend/app origin for preview profiles or rename the profile to production-preview and require an unmistakable in-app production watermark.
Mobile / IAP Verification Resilience | Orta | /api/mobile/iap/verify can fall back to locally verified Apple transaction JWS when App Store server lookup fails. | The signed transaction proves purchase fields, but latest refund, revocation, grace, or renewal status can be stale until webhook/next refresh. | On Apple API outage, grant only a short PENDING_VALIDATION entitlement or return retryable 503 for new paid access; keep local-JWS fallback for restore display only.
Mobile / Invite Cache / Account Switching | Orta | clearSensitiveLocalState does not remove locateflow.pendingInviteToken even though invite tokens are device-keyed. | A stale invite captured before logout can survive account switching and be retried by the next session; server email checks reduce impact but the device can still carry another user invite credential. | Add locateflow.pendingInviteToken to sensitive cleanup and namespace pending invites by intended email/session when available.
Mobile / iOS Widget | Orta | iOS WidgetKit reads App Group UserDefaults, but writeSnapshotToAppGroup is an intentional no-op. | Android widget updates can be requested from JS, while the iOS widget extension has no live data bridge and will not receive dashboard snapshots after install. | Implement a native App Group storage bridge plus WidgetCenter.reloadAllTimelines and add a device/EAS verification test for iOS widget refresh.
Mobile / Privacy / Push Tokens | Orta | Forced 401 logout clears local state but cannot unregister the Expo push token; manual logout/delete unregister before clearing. | A device signed out due to expired or invalid session can continue receiving old-account push notifications until the server prunes or the user signs in and out again. | Track device token lifecycle server-side by session, prune tokens when mobile sessions are invalidated, or perform a best-effort unregister before clearing when the token is still accepted.
Mobile / QA / IAP Tests | Orta | pnpm --filter @locateflow/mobile test fails 4 reconcilePendingPurchases tests because reportIapIssue reads global __DEV__ in Vitest. | Cold-start store purchase recovery is an important billing safety net, and its regression tests currently fail before they can assert finishTransaction/verification behavior. | Define __DEV__ in Vitest setup or guard with typeof __DEV__ !== "undefined"; keep the IAP recovery tests in CI after the environment fix.
Mobile / Reminder Deep Links | Orta | Reminders screen pushes notificationHref directly, while Notifications screen maps web hrefs such as /moving/plan/:id to mobile routes. | Reminder rows can navigate to routes that do not exist in the mobile app even though the same feed item works from the notifications inbox. | Reuse a shared resolveMobileHref helper in Reminders before router.push and mark rows without supported destinations as notification-only.
Mobile / Services / Budget Logic | Orta | Several mobile surfaces still sum raw monthlyCost as monthly spend even though shared code documents it as the per-cycle amount. | Yearly, quarterly, weekly, and one-time services are overstated in Services and dashboard/savings insights, while Budget and Address cards use monthlyAmountForCycle, so the app shows contradictory spend. | Replace raw sums with the shared monthlyAmountForCycle/normalizeServiceCost helpers in services tab, dashboard insights, service detail stats, and savings cards.
Mobile / Store Compliance / Privacy Metadata | Orta | app.json declares NSPrivacyCollectedDataTypes as an empty array while the app handles account, address, purchase, support, push, analytics, and crash data. | App Store privacy review and the product privacy label can drift from actual data collection, especially with subscriptions and support tickets. | Audit Apple privacy manifest/App Store Connect labels and declare the relevant collected-data categories and third-party SDK practices.
Shared / Connectors / Consent Race | Orta | PartnerConsent için aktif grant tekilliği DB seviyesinde yok | Schema sadece index kullanıyor; upsertGrantedConsent findMany/create sonra fazlaları revoke ediyor. Paralel OAuth callbackleri iki GRANTED consent yaratabilir ve enqueue aynı connector için çift dispatch üretebilir | (userId, connectorKey) için aktif-grant unique stratejisi ekle: activeGrantKey, serializable transaction veya önce revoke sonra unique create; race testi ekle
Shared / Connectors / Soft Delete | Orta | Connector worker soft-deleted kullanıcı kuyruğunu açıkça durdurmuyor | runDueDispatches status'a göre row seçiyor; account deletion grace sadece kullanıcıyı soft-delete edip session'ları öldürüyor. Var olan ConnectorDispatch satırı partner sync veya NEEDS_USER bildirimi üretebilir | Soft-delete başlatılırken queued/dispatching rows'u NEEDS_USER/CANCELED yap; worker'da user.deletedAt guard'ı ve test ekle
Shared / Cron / Deploy Parity | Orta | GitHub cron, Vercel cron ve Ofelia cron listeleri farklı | GitHub workflow scheduled-delivery, local-hour reminder batch, daily weekly-digest ve birçok bakım job'ını kapsıyor; Vercel/Ofelia listeleri eksik veya farklı. Scheduler kaynağı değişirse queue, digest, cleanup veya publish işleri sessizce çalışmayabilir | Tek bir cron manifestinden GitHub/Vercel/Ofelia üret; /api/ready veya admin health'te “expected job coverage” kontrolü göster
Shared / Email Config | Orta | Prod env template public NEXT_PUBLIC_SUPPORT_EMAIL veriyor ama server email/readiness SUPPORT_EMAIL bekliyor | Legal/public sayfalar support adresi gösterebilirken transactional email config ve /api/ready “SUPPORT_EMAIL or EMAIL_REPLY_TO missing” hatasına düşer | SUPPORT_EMAIL'i env template ve compose'a ekle; gerekiyorsa NEXT_PUBLIC_SUPPORT_EMAIL ile server SUPPORT_EMAIL arasında bilinçli tek yönlü fallback kur
Shared / Notifications / Soft Delete | Orta | Notification helper'ları stale userId ile in-app/push yan etkisi üretebiliyor | createInAppNotification kullanıcı aktif mi bakmadan Notification oluşturuyor; direct NotificationQueue/admin/workspace çağrıları stale userId taşırsa grace-window hesabına feed/push/email denemesi oluşabilir | createInAppNotification/sendNotification içinde aktif User guard'ı merkezileştir; soft-delete başlangıcında pushDevice ve NotificationQueue temizliği veya disable işareti ekle
Web / Addresses | Orta | Address date/country semantic validation is missing. | Invalid dates, inverted ranges, or unsupported country/state combinations can be persisted and then poison move/budget/provider logic. | Add schema-level validation for dates, start/end ordering, and country/state compatibility; mirror it in UI.
Web / Addresses | Orta | Primary address delete can promote a stale replacement under concurrent updates. | Two concurrent mutations can leave no reliable current primary address or promote an unintended old address. | Perform primary promotion in a transaction with deterministic ordering and workspace-aware scope.
Web / Auth / Billing Gate | Orta | Email verification/legal onboarding gate is not enforced at route level on sensitive billing APIs. | A session can reach subscription/portal endpoints before the same app-level eligibility rules used by normal app mutations. | Use a single billing mutation guard that checks verified email, legal acceptance, and onboarding state before checkout/portal changes.
Web / Auth / Password Change | Orta | /api/auth/password/change has no dedicated rate limit or lockout for currentPassword attempts. | An authenticated stolen session can brute-force the current password without the protections applied to login flows. | Add per-user/IP throttling, attempt logging, and alerting/step-up after repeated failures.
Web / Blog API | Orta | /api/blog/posts documents keyset pagination with before=<isoDate> but implementation uses page/pageSize. | Client or crawler integrations built from the API contract can paginate incorrectly or duplicate/skip posts. | Align the implementation and comment/API contract; either implement keyset pagination or update all callers/docs/tests to page/pageSize.
Web / Connectors / Stale Dispatch Recovery | Orta | runDueDispatches converts live rows stuck in DISPATCHING for 15 minutes or SUBMITTED for 7 days directly to NEEDS_USER without attempting partner-side status lookup. | Users may be asked to manually retry/verify even when the partner already accepted the change; for COA-style filings this creates support ambiguity. | Add connector-specific verify/resume hooks for stale rows before NEEDS_USER, and expose confirmation/status evidence in admin/user UI.
Web / Connectors / Workspace Scope | Orta | Manual /api/connector-dispatch defaults to the user's primary address and calls enqueueAddressChange without workspaceId, while enqueueAddressChange can scope by workspaceId only if provided. Evidence: apps/web/src/app/api/connector-dispatch/route.ts and apps/web/src/lib/connector-runtime.ts. | In a shared-workspace context, sync-now can ignore the selected workspace and dispatch only user-owned primary data, diverging from workspace-visible address state. | Pass the resolved active workspace from the request/cookie/header into connector dispatch or require an explicit addressId that is authorized under the same workspace scope.
Web / Cron / Notification Preferences | Orta | scheduled-delivery delivers EMAIL/PUSH/SMS via sendNotification without checking granular notification preferences; sendNotification's EMAIL path uses sendLoggedEmail directly, which does not apply preference or unsubscribe-category checks. | Future queued admin/system notifications can bypass user opt-outs depending on who wrote NotificationQueue, and the worker has no type-to-preference mapping. | Store notification intent/category on queue rows and enforce isWebNotificationEnabled/isPushTypeEnabled in scheduled-delivery before extra channel delivery; reserve bypass only for explicit transactional/emergency types.
Web / Cron / Provider Stats | Orta | provider-stats counts Service rows by providerId and isActive only, without deletedAt/user-deleted filters. Evidence: apps/web/src/app/api/cron/provider-stats/route.ts. | Soft-deleted services or deleted-account grace data can inflate provider popularity/userCount, which feeds public trust/social-proof UI. | Count only deletedAt=null services whose owners are not deleted; consider workspace visibility and anonymous aggregation thresholds.
Web / Cron / Queue Retry | Orta | scheduled-delivery atomically marks NotificationQueue.sent=true before delivery and records error without retry if delivery fails. | This prevents double-send, but transient Resend/Expo/DB errors become permanent missed notifications unless an operator manually requeues rows. | Add explicit states such as CLAIMED/SENDING/SENT/FAILED with attemptCount and retryAfter; keep dedupe keys for at-most-once side effects while allowing safe retry of rows with no successful channel.
Web / Cron / Weekly Digest | Orta | Web Vercel and Ofelia configs schedule weekly-digest only on Monday, but the route supports user-selected digestDay and GitHub Actions intentionally calls it daily. Evidence: apps/web/vercel.json, docker/ofelia.ini, .github/workflows/cron.yml, apps/web/src/app/api/cron/weekly-digest/route.ts. | Users who select Tuesday-Sunday digest days receive nothing on scheduler targets that still run Monday only. | Align Vercel/Ofelia with the daily 09:00 UTC GitHub schedule or remove stale scheduler files from production paths.
Web / Move Tasks / Legal Gate | Orta | Move-task generation/update uses requireDbUserId only and can mutate service-related state through local effects. | A partially onboarded or legally incomplete user can create/update app data and indirectly sync service state. | Use the app mutation guard and add tests for legal-gated mutation endpoints.
Web / Moving | Orta | Moving plan moveDate and inline destination startDate are not validated as real dates. | Invalid strings can pass into date conversion and scheduling logic, causing broken reminders or runtime errors. | Validate ISO dates with zod/refine and reject invalid/inverted move timelines.
Web / Payment / Plan Change Consent | Orta | Subscription change-plan and switch-cycle endpoints accept only acceptedSubscriptionTerms=true and do not persist a versioned disclosure/price/proration snapshot. Evidence: apps/web/src/app/api/subscription/change-plan/route.ts and switch-cycle/route.ts. | Later billing disputes cannot reconstruct exactly what the user accepted for immediate proration, deferred downgrade, new price, or renewal timing. | Store terms/policy versions, target plan/interval, effective date, price label, proration/deferred copy, IP/user-agent hash, and acceptedAt for every plan/cycle change.
Web / Payment / Stripe Version | Orta | Stripe SDK/API is pinned to stripe 16.12.0 and API version 2024-06-20, while the Stripe skill's current reference expects latest 2026-02-25.clover. Evidence: apps/web/package.json and apps/web/src/lib/stripe-api-version.ts. | Older API behavior can drift from current Stripe docs, and preview flexible-billing calls increase upgrade risk. | Plan a Stripe SDK/API upgrade, run webhook fixture regression tests, and remove preview API usage once stable equivalents are available.
Web / Payment / Tax | Orta | Stripe Checkout sessions do not enable automatic_tax, billing address collection, or tax ID collection. Evidence: apps/web/src/app/api/stripe/checkout/route.ts sessionParams. Varsayım: web subscriptions may require sales/SaaS tax handling in some jurisdictions. | Tax may be under-collected or unsupported for jurisdictions where SaaS/subscription tax applies, and invoices may lack address/tax evidence. | Decide tax nexus requirements; if needed, enable Stripe Tax automatic_tax, collect/update billing addresses, and test tax-inclusive/exclusive receipts.
Web / Pricing / Signup | Orta | Pricing selected plan/billing interval is not preserved through the sign-up/subscription entry. | A user can choose a price on the marketing page but land in a generic sign-up/onboarding path, increasing conversion drop-off and making plan intent ambiguous. | Carry plan and interval through URL/session state into onboarding and checkout; validate it server-side before using it.
Web / Providers / Privacy | Orta | Public provider popularity cohort does not filter soft-deleted address/service rows. | Deleted user data can still affect provider popularity counts and recommendations. | Add deletedAt filters to all cohort aggregations.
Web / Providers / Product Trust | Orta | Provider detail labels global userCount as state-specific usage. | Users may make provider decisions from a misleading localized trust signal. | Separate global usage from state usage, or compute the displayed metric from the selected state cohort.
Web / SEO / FAQ Schema | Orta | /faq raw HTML did not contain the expected Organization, WebSite, FAQPage, or BreadcrumbList ld+json scripts in the Playwright request test; FaqJsonLd is a client component using useServerInsertedHTML. Evidence: apps/web/src/app/faq/page.tsx, faq-json-ld.tsx, tests/e2e/public-pages.spec.ts. | FAQ rich-result eligibility and brand/entity signals can be absent from crawler-visible HTML; tests already catch the missing scripts. | Render FAQ JSON-LD with the shared server JsonLd component instead of client useServerInsertedHTML; make the e2e test assert FAQPage/BreadcrumbList independent of dev noindex sitewide schemas.
Web / Services | Orta | contractEndDate API validation and web edit renewal fields are inconsistent. | Users can store renewal/contract data that the UI cannot fully edit or validate, causing stale billing reminders and inaccurate plans. | Normalize service contract schema, expose all editable renewal fields, and validate the same shape on client/server.
Web / Services / Legal Gate | Orta | Service mutations use requireVerifiedUser only and do not enforce legal/onboarding mutation gate. | Users can persist operational service data before completing the legal/onboarding state expected elsewhere. | Replace ad hoc guards with the app mutation guard used for other persisted user data.
Web / Support & Notifications / Legal Gate | Orta | Notification preference writes and support ticket create/reply/close use requireDbUserId only. Evidence: apps/web/src/app/api/notifications/route.ts, notifications/preferences/route.ts, tickets/route.ts, tickets/[id]/route.ts. | Unverified or legally incomplete accounts can persist communication preferences and support content even though other app mutations require stronger gates. | Decide whether support must remain available pre-verification; otherwise use requireVerifiedUser/requireAppMutationUser. If support remains open, explicitly document and limit the exception.
Web / Workspace / Data Integrity | Orta | Invalid or unauthorized X-Workspace-Id / lf_workspace_id silently falls back to the caller's oldest workspace, including on mutation paths through resolveWorkspaceDataScope. | A stale client selection can write new addresses/services/tasks into a different workspace than the user intended; it is security-safe but data-destructive/confusing. | For explicit X-Workspace-Id on mutating requests, return 403/409 and ask the client to reselect; reserve silent fallback for read requests or stale cookies only.
Web / Workspace / Limits | Orta | Workspace creation cap is checked before create outside the transaction. Evidence: apps/web/src/app/api/workspaces/route.ts checks ownedCount then creates workspace. | Concurrent POST requests can exceed MAX_OWNED_WORKSPACES, multiplying workspace seat pools beyond the intended cap. | Enforce the cap with a serializable transaction, owner-scoped lock, or database-backed quota invariant.
Admin / Backup / Coverage Metadata | Düşük | Backup list and verify endpoints have hard-coded table counter maps that do not cover every table in BACKUP_TABLE_ORDER, including workspaces, workspaceMembers, notificationPreferences, supportTickets, ticketMessages, affiliate tables, sponsoredPlacements, helpArticles, faqs, stateRules, ipRules, and waitlistSignups. | Operators can see zero/missing stats or incomplete verify count context even though the archive/import registry includes those tables, which reduces restore-readiness confidence. | Derive count functions from the BACKUP_TABLES registry or add an invariant test that every BACKUP_TABLE_ORDER entry has a list/verify counter.
Admin / Help Center / Public Content Change Control | Düşük | Help-center FAQ/article create/update/delete affects public support content but has no password/MFA step-up, unlike state rules, sponsored placements, connectors, and acquisition campaigns. | A low-friction admin session can alter public help and SEO/support guidance; React renders content as text, so this is integrity/quality risk rather than XSS. | Add step-up for publish/delete or at least for isPublished=true changes, and audit whether content changed from draft to public.
Admin / Workspace / Metrics | Düşük | Workspace list applies multiOnly after paginating all workspaces, and detail counts workspace addresses/services/budgets without deletedAt filters. | Admin household pages can show sparse/empty pages for multi-seat filters and over-count records that users deleted. | Move multiOnly into the database query or paginate after a filtered count, and add deletedAt:null to per-member record counts.
Mobile / Custom Provider Links | Düşük | Custom provider detail opens provider.website directly, unlike provider detail which normalizes missing schemes to https://. | User-entered websites like example.com can fail to open or behave inconsistently across iOS/Android. | Normalize custom-provider website URLs before Linking.openURL and validate schemes server-side.
Mobile / Offline Coverage | Düşük | React Query is offlineFirst for reads, but caches are memory-only and mutations are network-only; only dashboard/widget snapshots persist. | On flaky/offline devices, most CRUD screens lose unsaved form state and have no retry queue despite offline being in scope. | Add draft persistence for long forms and an explicit retry queue for safe/idempotent mutations, or document offline as read-only dashboard recovery.
Shared / DB / Invitation Integrity | Düşük | WorkspaceInvitation duplicate-pending kuralı DB'de partial unique ile korunmuyor | API serializable transaction ile canlı pending invite tekrarını engelliyor, ancak DB unique [workspaceId, invitedEmail, expiresAt] farklı expiresAt ile çift canlı invite'ı yapısal olarak engellemiyor | pendingActiveKey gibi nullable unique alan veya generated active key ekle; app guard'ı koru
Shared / Runtime / Local QA | Düşük | Testler Node 24.12 üzerinde engine 22.x uyarısıyla çalışıyor | Web/admin/shared/connectors testleri geçti ama runtime farkı native/mobile ve Next davranışlarında CI/prod ile sapma yaratabilir | Yerel çalışma ortamını Node 22.x'e pinle; .node-version/Volta ve CI engine check ekle
Web / Account Security / Impersonation | Düşük | Security sessions list sets impersonated flag false because it does not select impersonatedById. | Users cannot see that a session was created through admin impersonation, reducing transparency. | Select impersonatedById and surface it in the session response/audit UI.
Web / Connectors / Rate Limit Race | Düşük | enqueueAddressChange enforces connector per-minute/per-day caps by count queries before creating dispatch rows, outside the transaction that inserts rows. | Concurrent enqueue requests can both pass the cap and create more live ConnectorDispatch rows than the connector manifest allows. | Move cap enforcement into a transaction with a durable per-user/per-connector counter or unique time-bucket guard.
Web / Cron / Blog Publish Race | Düşük | blog-publish reads due posts, then updates by id inside a transaction without a status='SCHEDULED' guarded claim; scheduled-delivery has a guarded implementation for the same publish task. | Concurrent cron invocations can both select the same due post and duplicate revalidation/IndexNow pings; not data-corrupting but noisy and inconsistent with the newer worker. | Replace blog-publish with the guarded scheduled-delivery publish code or use updateMany where id+status=SCHEDULED before pinging.
Web / GEO / LLM Discovery | Düşük | llms.txt silently returns an empty blog list on database error. | AI/LLM discovery metadata degrades without any observable signal, hiding content index issues. | Log/monitor the failure and fall back to a cached/static recent-post list.
Web / Notifications / Rate Limit | Düşük | Notification preference endpoints have no per-user/IP rate limit. | A compromised session or buggy client can churn preference rows and create unnecessary DB load. | Add a modest mutation throttle for /api/notifications and /api/notifications/preferences PUT/POST.
Web / Offline / PWA | Düşük | /offline exists and the service worker references it, but middleware public page allow-list does not include it. | Offline fallback can redirect through auth middleware instead of rendering the offline page. | Add /offline to public routes and test service worker fallback.
Web / Payment / Portal | Düşük | Stripe portal session creation checks only stripeCustomerId, not provider/status/active Stripe subscription. Evidence: apps/web/src/app/api/stripe/portal/route.ts. | Store-managed/admin-granted or stale historical Stripe customers can see an irrelevant Stripe portal, confusing cancellation/update-payment flows. | Require provider=STRIPE and a current stripeSubscriptionId/status appropriate for portal, or return store/admin-specific guidance.
Web / Payment / UX | Düşük | Subscription UI says yearly-to-monthly switch credits unused annual time toward future monthly invoices, but the code schedules year-to-month downgrade at period end with proration_behavior none. Evidence: apps/web/src/components/settings/subscription-management.tsx and apps/web/src/app/api/subscription/switch-cycle/route.ts. | Users may expect a credit that is not created because access remains annual until period end and monthly starts after that. | Change copy to say monthly billing starts at period end with no credit or charge today, or implement immediate downgrade with credits if that is the intended policy.
Web / Providers / User Preferences | Düşük | Provider shortlist and recommendation feedback mutations use only a session gate. | Preference writes bypass the app's stronger mutation eligibility rules and can persist data for incomplete accounts. | Use requireVerifiedUser or the app mutation guard for preference writes.
Web / QA / Auth Form Selectors | Düşük | Sign-in and sign-up E2E tests use getByLabel(/password/i), which matches both the password input and the "Show password" toggle button, causing Playwright strict-mode failure despite visible fields. | The smoke suite reports auth form failures that are selector ambiguity, not necessarily user-facing broken render, reducing CI signal quality. | Target getByRole("textbox", { name: "Password" }) or use an exact label matcher for the input; keep a separate assertion for the show/hide password button.
Web / QA / E2E Environment | Düşük | Public E2E tests expect production robots output with "User-agent" and "Sitemap:", but local/dev robots intentionally returns noindex "User-Agent: * / Disallow: /" when APP_ENV/NODE_ENV is non-production. | The SEO smoke suite false-fails in local/dev and can hide real failures such as missing FAQ JSON-LD among environment mismatches. | Make the robots test environment-aware or set production-like SITE_URL/APP_ENV for SEO e2e; use case-insensitive User-Agent matching if casing matters only to the test.
Web / Workspace / Validation | Düşük | Workspace rename validation is inconsistent: /api/workspaces/[id] enforces 1-60 chars while /api/workspaces/[id]/rename slices to 120 chars. | Different clients can persist names the settings UI rejects, causing inconsistent display and validation behavior. | Keep one rename endpoint or share a single zod/schema validator and max length.

Genel Saglik Ozeti
- Web tarafinda genis unit test kapsami var ve public link crawl temiz, ancak Family/Pro, workspace scope, export/provider/budget ayrimi, odeme consent/tax ve SEO/a11y yuzeylerinde canli kullanici etkisi yuksek aciklar var.
- Admin temel guvenlik mimarisi guclu: JWT, DB session, CSRF, IP kurallari, CSP, step-up ve audit patternleri mevcut. Buna ragmen force-password-change, manuel entitlement, IAP revalidation, workspace ownership transfer ve bildirimlerde yuksek riskli is mantigi bosluklari var.
- Mobile uygulama typecheck acisindan temiz; en buyuk riskler web-cookie akislarina bridge eksikligi, workspace header tasimama, IAP recovery test kirigi, store privacy metadata ve preview buildlerin prod backend kullanmasi.
- Shared/connector cekirdegi iyi tasarlanmis ve connector package testleri temiz; risk daha cok DB tekillik garantisi, soft-delete yan etkileri, env/deploy parity ve scheduler kaynaklarinin ayrismasinda.
- Odeme katmani web, admin ve mobile arasinda urun/plan matrisi olarak buyumus; artik tek bir billing readiness ve workspace-seat reconciliation kapisi olmadan canliya cikmak riskli.

Oncelikli Aksiyon Listesi
1. Family/Pro launch gate: workspace flag/backfill, price IDs, env/compose parity, checkout consent snapshot ve seat reconciliation tek release checklist altinda kapatilsin.
2. Workspace context: web export/budget/provider/moving driftleri ve mobile X-Workspace-Id eksikligi ayni scope contract ile duzeltilsin; invalid workspace id mutasyonlarda fallback yapmasin.
3. Billing/IAP: Stripe tax/consent/API version, admin manual entitlement validation, mobile IAP readiness ve Apple/Google dogrulama akislari oncelikli ele alinsin.
4. Privacy/soft-delete: Notification, push token, connector dispatch ve unsubscribe preference mapping hesap silme/opt-out ile tek noktadan uyumlu hale getirilsin.
5. Admin high-risk actions: force-password-change, ownership transfer, bulk provider changes, fallback connector links ve broadcast notifications icin step-up + stricter validation uygulanmali.
6. Cron/deploy: GitHub/Vercel/Ofelia tek manifestten uretilsin; scheduled-delivery retry/prefs modeli netlestirilsin.
7. SEO/a11y: serious contrast ihlalleri, FAQ JSON-LD render boslugu, robots test ortami farki ve GEO/llms.txt hata gorunurlugu duzeltilsin.
8. Mobile release: preview/staging backend ayrimi, App Privacy manifest, Apple nonce, widget App Group write ve IAP __DEV__ test kirigi kapatilsin.
9. Data integrity: PartnerConsent aktif grant tekilligi, workspace invite duplicate yapisi, primary address delete race ve workspace creation cap race icin DB/transaction seviyesinde koruma eklensin.
10. Regression suite: web/admin unit yesil durum korunurken Playwright a11y/SEO smoke ve mobile IAP recovery testleri CI bloklayici hale getirilsin.
