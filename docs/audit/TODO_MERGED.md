# Merged Audit TODO (Claude + ChatGPT, reconciled)

> Single deduplicated, prioritized backlog combining the Claude codebase audit
> (`docs/audit/reports/{high,medium,low}.md`, `11_FIX_PRIORITY_ROADMAP.md`) and the
> reconciled ChatGPT audit (verified-claims JSON).
> Evidence is current-code only. Severities reflect the *current* codebase state.

## How to read

- **Source tags:**
  - `[CLAUDE]` — finding originates from the Claude audit only.
  - `[CHATGPT]` — finding originates from the reconciled ChatGPT audit only.
  - `[BOTH]` — same underlying issue independently found by both audits; the row cites both ids.
- **ChatGPT item status:** only ChatGPT claims reconciled as `confirmed_valid` or `partially_valid`
  are carried into the backlog. For `partially_valid` items, the row scopes the task to the
  *residual* part that is still real in current code. Items reconciled as `already_fixed` or
  `inaccurate` are NOT actionable and are listed in the Appendix.
- **Phase ordering** follows `11_FIX_PRIORITY_ROADMAP.md`:
  prod security → data loss/corruption → auth/permission → critical user-flow breaks →
  API validation/error handling → revenue (billing/subscription) → UI/UX → accessibility →
  performance → tests → dead code → refactor.
- **ChatGPT reconciliation counts (of 38 verified claims):**
  `confirmed_valid` = 7, `partially_valid` = 11, `already_fixed` = 18, `inaccurate` = 1
  (1 = SURF-12, dropped). 18 ChatGPT items carried in (7+11); 19 dropped (18 fixed + 1 inaccurate).
  (Note: SEC-12 and SURF-14 overlap on mobile-IAP readiness; merged into one task.)

---

## ✅ Sprint 1 — COMPLETED (branch `fix/audit-sprint1`, 7 commits — see `FIX_LOG.md`)

Applied, tested (Vitest green), typecheck-clean (web+admin 0 errors). Not yet pushed/merged.

- [x] Partner `/partners/apply,/portal` + `/unsubscribe` → `PUBLIC_PATHS` — `route-map-01/02`
- [x] CCPA Do-Not-Sell enforced (affiliate/click skip-write; lead-dispatch suppress; exemptions documented) — `account-deletion-export-04` *(decision: suppress pending)*
- [x] EmailLog purge on self-service GDPR Art.17 erasure — `account-deletion-export-01`
- [x] `/api/build-info` gated + `/api/ready` recon docstring — `app-bootstrap-config-04/05`
- [x] Google OAuth single-use replay guard — `auth-session-01` / `signup-login-01`
- [x] Invite accept: verified-email gate + rate-limit — `workspace-invitation-household-01/02`
- [x] Dark-mode contrast on EmptyState CTA + skip-link — `component-theme-system-02`

**Deferred (flagged):** `database-schema-03` (Lead/AddressChangeEvent PII purge — shared helper); `component-theme-system-01` (badge contrast — needs tokens + runtime check, → `docs/ui-renewal/`); invite decline/validate hardening; build-info external-monitor confirm. Remaining Phase-1 items below are still open (trusted-proxy IP is next-highest).

---

## Phase 1 — Production Security & Compliance (highest blast radius)

- [ ] **Pin trusted-proxy IP resolution; fail closed in prod** (Source: [CLAUDE] admin-auth-security-02 / security-surface-04; Severity: High; Category: Security; Files: `packages/shared/src/trusted-client-ip.ts`, `apps/web/src/lib/client-ip.ts`, `apps/web/src/lib/production-readiness.ts`, `apps/admin/src/middleware.ts`, `.env.example`; Why: default `compat` mode trusts client-supplied `cf-connecting-ip`/`x-real-ip`/`x-forwarded-for` with no allowlist, letting clients forge source IP and defeat IP rules, per-IP login lockout, fingerprint buckets, and audit accuracy; Validation: spoofed header ignored in prod-like; readiness fails when `TRUSTED_PROXY_HEADERS` unset; Est. complexity: M; Risk of change: Med — wrong edge value breaks IP attribution, confirm deployment edge first.)

- [ ] **Enforce CCPA Do-Not-Sell opt-out in every data-sharing path** (Source: [CLAUDE] account-deletion-export-04; Severity: High; Category: Security/Compliance; Files: `apps/web/src/lib/ccpa.ts`, `apps/web/src/app/api/affiliate/click/route.ts`, `apps/web/src/app/api/sponsored/click/route.ts`, `apps/web/src/app/api/affiliate/postback/[network]/route.ts`, `apps/web/src/app/api/cron/lead-dispatch/route.ts`; Why: `hasCcpaOptOut()` has zero callers, so the recorded CPRA opt-out is inert and data can still be shared after opt-out; Validation: opted-out user skips every share path, opted-in unaffected; Est. complexity: M; Risk of change: Low — additive guard.)

- [ ] **Complete self-service GDPR/Art.17 erasure of no-FK PII (EmailLog + Lead/LeadDispatch/AddressChangeEvent)** (Source: [CLAUDE] account-deletion-export-01 + database-schema-03; Severity: High; Category: Data/Security; Files: `apps/web/src/lib/account-deletion.ts:364-371`, `apps/admin/src/lib/hard-delete-user.ts:309-316`, `packages/db/prisma/schema.prisma:1564-1585,2385-2429,2114-2143`; Why: self-service erasure purges only WaitlistSignup+NotificationQueue, leaving plaintext `EmailLog.to`, encrypted `Lead` PII, and plaintext `AddressChangeEvent.fullName` until an unrelated sweep — incomplete and inconsistent with admin path; Validation: schema test asserts every no-FK PII table is in purge list; delete leaves no residue; Est. complexity: M; Risk of change: Med — destructive deletes, reuse admin tested queries.)

- [ ] **Populate legal-entity / mailing-address placeholders + production boot guard; remove unsourced "4.9 on the App Store" chip** (Source: [CLAUDE] marketing-seo-content-01 + marketing-seo-content-04; Severity: High/Med; Category: Data/Compliance; Files: `apps/web/src/lib/legal-info.ts:1-15`, `apps/web/src/app/contact/page.tsx:124-130`, `apps/web/src/app/page.tsx:249`, `.env.example:138-139`, `apps/web/src/lib/production-readiness.ts`; Why: Terms/Privacy/DPA/CCPA/Contact can go live with product name instead of legal entity and no address; homepage shows an unsourced app-store rating (FTC/endorsement risk); Validation: readiness check fails if placeholder persists; rating chip removed or sourced; Est. complexity: S; Risk of change: Low.)

- [ ] **Encrypt remaining plaintext PII at rest (legacy Subscription.purchaseToken backfill, mobile AsyncStorage snapshots)** (Source: [CLAUDE] database-schema-04 + mobile-app-01; Severity: Med; Category: Security; Files: `apps/web/src/lib/iap-common.ts` + backfill job, `packages/db/prisma/migrations/20260621110000_subscription_purchase_token_encrypted/migration.sql`, `apps/mobile/src/lib/offline-cache.ts`, `apps/mobile/src/lib/local-cleanup.ts`; Why: dormant subscribers keep plaintext Play bearer tokens; mobile writes addresses/costs/tier to unencrypted AsyncStorage readable on rooted/imaged device; Validation: plaintext purchaseToken count → 0; no PII keys in plaintext storage; Est. complexity: M; Risk of change: Med.)

- [ ] **Harden field-encryption + dev passthrough; tighten secret hard-fail in self-host compose** (Source: [CLAUDE] database-schema-08 + app-bootstrap-config-01; Severity: Med; Category: Security; Files: `packages/shared/src/encryption.ts`, `docker-compose.prod.yml:136-155`, `apps/web/src/lib/production-readiness.ts:225-243`; Why: encryption falls back to plaintext when `NODE_ENV!==production` (internet-facing staging risk) and several catalog-required secrets use `${VAR:-}`/bare defaults so a Docker deploy can boot with security/billing/email secrets empty; Validation: prod-like requires `FIELD_ENCRYPTION_KEY`; compose-lint asserts `:?` on every required key; Est. complexity: M; Risk of change: Med.)

- [ ] **Gate public recon surfaces (/api/build-info, web /api/ready key names)** (Source: [CLAUDE] app-bootstrap-config-04 + app-bootstrap-config-05; Severity: Low; Category: Security; Files: `apps/web/src/app/api/build-info/route.ts`, `apps/admin/src/app/api/build-info/route.ts`, `apps/web/src/app/api/ready/route.ts:54-82`; Why: commit SHA/branch/environment and failing-config key names exposed unauthenticated (fingerprinting/recon aid); Validation: web `/api/ready` matches admin count-only shape; build-info gated; Est. complexity: S; Risk of change: Low.)

---

## Phase 2 — Auth & Permissions

- [ ] **Invoke per-mutation impersonation audit; scope, device-bind & verify adminId on impersonated sessions** (Source: [CLAUDE] admin-impersonation-02 + admin-impersonation-04 + admin-impersonation-05 + admin-auth-security-01; Severity: High/Med; Category: Security/Logic; Files: `apps/web/src/lib/impersonation-audit.ts`, mutating routes under `apps/web/src/app/api/**`, `apps/web/src/app/api/internal/impersonate/route.ts`, `apps/web/src/app/api/auth/impersonate-handoff/route.ts`, `apps/web/src/lib/user-auth.ts`; Why: `recordImpersonatedMutation` is never called (only START/HANDOFF logged), the minted session is full-privilege, omits the `fp` claim (replayable 15 min from any device), and the internal endpoint trusts an unverified `adminId` from the body; Validation: every impersonated write emits AdminAuditLog; spoofed adminId rejected; cookie device-bound; Est. complexity: L; Risk of change: Med.)

- [ ] **Add step-up (password/MFA) to remaining privileged actions** (Source: [CLAUDE] authorization-workspaces-02 + admin-management-01 + auth-session-02 + auth-session-07; Severity: Med; Category: Security; Files: `apps/web/src/app/api/workspaces/[id]/members/[memberId]/route.ts`, `apps/web/src/lib/workspace-step-up.ts`, `apps/admin/src/app/api/notifications/route.ts:384-414`, `apps/web/src/lib/user-step-up.ts`, `apps/web/src/app/api/auth/password/change/route.ts`; Why: member promote-to-ADMIN, single-user admin EMAIL/PUSH send (phishing primitive), step-up TOTP/backup (no `mfa_verify` lockout), and password-change lack auth-grade gating; Validation: each action requires step-up; MFA brute-force locks via `mfa_verify`; Est. complexity: M; Risk of change: Low.)

- [ ] **Add server-side single-use replay state to Google OAuth (mirror Apple's OAuthState consume)** (Source: [CLAUDE] auth-session-01 / signup-login-01; Severity: Med; Category: Security; Files: `apps/web/src/app/api/auth/oauth/google/route.ts`, `apps/web/src/app/api/auth/oauth/google/callback/route.ts`; Why: Google callback validates cookie-only state with no DB-backed single-use record while Apple atomically consumes an OAuthState row; captured state+code replayable; Validation: replayed state rejected; Est. complexity: S; Risk of change: Low.)

- [ ] **Require email-verification on workspace token-accept; rate-limit invite routes** (Source: [CLAUDE] Phase-2.3 (workspace-invitation-household-01/02); Severity: Med; Category: Security; Files: `apps/web/src/app/api/invitations/[token]/accept/route.ts`, `[token]/route.ts`, `pending/[id]/accept/route.ts`; Why: token-accept bypasses `requireVerifiedUser` and accept/decline/validate routes are unthrottled; Validation: unverified token-accept rejected; flood returns 429; Est. complexity: S; Risk of change: Low.)

- [ ] **Add page-level role guards to privileged admin pages (fail-closed before render)** (Source: [CLAUDE] route-map-03; Severity: Med; Category: Security; Files: `apps/admin/src/app/(admin)/{users,waitlist,providers,reports,settings/health,analytics,help-center,moving,provider-governance,state-rules,support}/page.tsx`, `apps/admin/src/lib/page-guard.ts`; Why: privileged pages rely only on layout `requirePageAdmin()` + API 403, so a VIEWER loads chrome/pre-API data; Validation: under-privileged role redirected before render; Est. complexity: M; Risk of change: Low.)

- [ ] **Harden unsubscribe token: add expiry, bind kind into signature, require dedicated secret** (Source: [CLAUDE] email-pipeline-01 + email-pipeline-02 (= notification-email-digest-02); Severity: Med; Category: Security; Files: `apps/web/src/lib/unsubscribe.ts:19-67`, `apps/web/src/app/api/unsubscribe/route.ts`, `apps/web/src/lib/email-service.ts:155-176`, `.env.example`; Why: HMAC covers only userId with no expiry and `k` defaults to `all`, so any held link can permanently opt a user out of ALL mail; secret silently falls back to `USER_JWT_SECRET`; Validation: expired token rejected; `EMAIL_UNSUBSCRIBE_SECRET` required in prod; Est. complexity: M; Risk of change: Med — token-format change invalidates outstanding links.)

- [ ] **Add CI/lint backstop asserting public-by-prefix routes call their guard; snapshot public allowlist** (Source: [BOTH] [CLAUDE] security-surface-02/repo-overview-03 + [CHATGPT] SURF-01 (partially_valid); Severity: Low; Category: Security/Architecture; Files: `apps/web/src/middleware.ts:26-153`, `apps/admin/src/middleware.ts`, new middleware test; Why: `/api/internal|cron|webhooks` are fail-open by omission and a new route under a public prefix is silently unauthenticated; edge middleware verifies JWT signature only, so a route forgetting `requireDbUserId`/`getUserSession` skips DB revocation checks. Note: SEC-05 (same allowlist concern) is `already_fixed` — middleware.test.ts negative guards exist — so this task is the *lint/prefix* backstop SURF-01 still wants, not the public-route snapshot SEC-05 already covers; Validation: test fails if a route under those prefixes omits its guard; Est. complexity: S; Risk of change: Low.)

- [ ] **Address residual admin/web limiter config gaps (multi-instance Redis provisioning + fail-closed)** (Source: [BOTH] [CLAUDE] security-platform-02/security-surface-03/repo-overview-04 + [CHATGPT] AUD-007 (partially_valid); Severity: Low; Category: Security/Scalability; Files: `apps/web/src/lib/rate-limit.ts:12-19`, `apps/web/src/lib/rate-limit-policy.ts`, `apps/admin/src/middleware.ts:431`, `apps/admin/src/app/api/auth/login/route.ts`; Why: distributed Upstash limiter is now primary and fail-closed (AUD-007 core resolved), but a multi-instance prod deploy that never provisions Upstash falls back to a per-process Map per replica; web rate-limiter reads Upstash creds from `process.env` while the same keys are managed runtime-config keys; Validation: prod-like with multiple replicas + no Redis surfaces a readiness failure/alert; Est. complexity: M; Risk of change: Med — fail-closed could deny if Redis flaps.)

- [ ] **Tighten remaining admin auth hardening (CSRF header-absent window, login timing oracle, IP-rule fail-open lag, fingerprint binding, MFA self-disable, dev-secret fallback)** (Source: [CLAUDE] admin-auth-security-03/04/05/06/07/08; Severity: Med/Low; Category: Security; Files: `apps/admin/src/middleware.ts`, `apps/admin/src/app/api/auth/login/route.ts`, `apps/admin/src/lib/ip-rules.ts`, `apps/admin/src/lib/session-fingerprint.ts`, `apps/admin/src/app/api/auth/mfa/disable/route.ts`, `apps/admin/src/app/api/auth/sessions/route.ts`; Why: non-logout mutations pass when Origin/Referer/Sec-Fetch-Site all absent; unknown-email login skips bcrypt enabling a timing oracle; IP-rule cache fail-open with ~60s ban lag; fingerprint is fully client-influenceable; required-role admin can self-disable MFA; revoke-handle has a dev-literal secret fallback; Validation: per-item negative tests; Est. complexity: M; Risk of change: Low.)

- [ ] **Fix workspace data-scope leaks on direct-URL/server pages and migration entitlement** (Source: [CLAUDE] budget-expenses-01 + moving-tasks-06 (= address-change-relocation-01); Severity: Med; Category: Security; Files: `apps/web/src/app/(app)/budget/[month]/page.tsx:31-36`, `apps/web/src/app/api/moving/migration/route.ts:31-99`, `apps/web/src/lib/workspace-data-scope.ts`; Why: `/budget/[month]` ignores workspace scope and omits `deletedAt:null` (soft-deleted budgets render via direct URL; shared budgets invisible to members); `/api/moving/migration` never calls `resolveWorkspaceDataScope` and evaluates entitlement against the member not the owner; Validation: member sees shared data; soft-deleted budget hidden; Est. complexity: M; Risk of change: Med.)

- [ ] **Tighten miscellaneous data-sharing/PII surfaces (sponsored-click inflation, analytics arbitrary metadata, audit free-text PII scrub, workspace cookie/invite-link hardening)** (Source: [CLAUDE] providers-connectors-01 + analytics-flags-runtime-01 + security-surface-01 + authorization-workspaces-03/04; Severity: Med/Low; Category: Security; Files: `apps/web/src/app/api/sponsored/click/route.ts`, `apps/web/src/app/api/tracking/event/route.ts`, `packages/shared/src/audit-redaction.ts`, `apps/web/src/lib/workspace-context.ts:29-33`, `apps/web/src/app/api/workspaces/[id]/invitations/route.ts:193`; Why: sponsored clicks inflatable by any authed user (CTR/billing corruption); non-Phase-1 events persist near-arbitrary attacker metadata (dashboard pollution / stored-XSS if rendered unencoded); audit free-text not value-scrubbed for PII; `lf_workspace_id` cookie not httpOnly/secure; plaintext invite token leaks via `devInviteUrl` when `NODE_ENV!==production`; Validation: per-item tests; Est. complexity: M; Risk of change: Low.)

---

## Phase 3 — Critical User-Flow Breaks

- [ ] **Restore partner self-service routes (/partners/apply, /partners/portal)** (Source: [CLAUDE] route-map-01; Severity: High; Category: Logic; Files: `apps/web/src/middleware.ts:26-59`; Why: both routes are absent from `PUBLIC_PATHS` so the entire partner acquisition surface redirects to `/sign-in` when logged out; Validation: logged-out GET of both routes renders 200, portal still gates actions; Est. complexity: S; Risk of change: Low — keep in-page partner-session gate.)

- [ ] **Add /unsubscribe page to PUBLIC_PATHS (its API is already public)** (Source: [CLAUDE] route-map-02; Severity: Med; Category: Logic; Files: `apps/web/src/middleware.ts:26-59`; Why: logged-out recipients clicking one-click unsubscribe are redirected to `/sign-in` (CAN-SPAM/UX); Validation: logged-out GET renders the page; Est. complexity: S; Risk of change: Low.)

- [ ] **Unify account-delete & data-export step-up across web (MFA + OAuth-only support)** (Source: [CLAUDE] dashboard-web-app-01 + dashboard-web-app-02 + dashboard-web-app-08 + account-deletion-export-08; Severity: Med; Category: UI/UX/Logic; Files: `apps/web/src/app/(app)/settings/page.tsx`, `apps/web/src/app/(app)/settings/export/page.tsx`, `apps/web/src/components/settings/delete-account-dialog.tsx`, `apps/web/src/app/api/export/route.ts`, `apps/web/src/app/api/export/pdf/route.ts`; Why: inline `/settings` delete and web export collect no MFA code, so MFA users hit a dead end and OAuth-only users get an unsatisfiable step-up — blocking GDPR Art.15/17 rights on web; two divergent delete UIs drift; Validation: MFA & OAuth-only users can delete and export; Est. complexity: M; Risk of change: Low — consolidate on `DeleteAccountDialog`.)

- [ ] **Server-validate onboarding COMPLETED + unify scoping + persist isMilitary** (Source: [CLAUDE] onboarding-flow-01 + onboarding-flow-02 + onboarding-02/03 + onboarding-flow-03; Severity: Med; Category: Logic; Files: `apps/web/src/lib/onboarding-progress.ts:39-61`, `apps/web/src/app/api/onboarding/progress/route.ts`, `apps/web/src/lib/post-auth-redirect.ts`, `apps/web/src/app/onboarding/onboarding-client.tsx`, `apps/web/src/lib/onboarding-profile-payload.ts`; Why: COMPLETED short-circuits before the address check (dashboard invariant broken); gate vs `/api/profile` use different scoping (workspace member redirect loops); `isMilitary` (SENSITIVE) hardcoded false on web and overwritten on save; Validation: COMPLETED rejected without prerequisites; member resumes correct step; military flag persists; Est. complexity: M; Risk of change: Med — redirect-loop risk.)

- [ ] **Gate notification fan-out on opt-out/prefs in scheduled/broadcast worker; let web users mute push** (Source: [BOTH] [CLAUDE] notifications-push-01 + notification-email-digest-04 + notifications-push-07 + [CHATGPT] SURF-10 (partially_valid); Severity: Med; Category: Logic/Compliance; Files: `apps/web/src/app/api/cron/scheduled-delivery/route.ts:111-137`, `apps/web/src/lib/notifications.ts:31-90`, `apps/admin/src/app/api/notifications/route.ts`, `apps/admin/src/lib/notify-dispatch.ts`; Why: `deliverToUser` fans out EMAIL/PUSH with no `isEmailTypeOptedOut`/`isPushTypeEnabled` check (CAN-SPAM/consent), broadcast pages whole user table; SURF-10 residual: NotificationQueue failures recorded but never re-delivered and stuck rows only show in a raw list; Validation: opted-out user gets only IN_APP; push toggle works from web; queue failures surfaced; Est. complexity: M; Risk of change: Med — must not suppress transactional types.)

- [ ] **Surface backend errors instead of empty-success across client fetches** (Source: [CLAUDE] notifications-push-05 + dashboard-web-app-04 + budget-expenses-13 + addresses-validation-03/06 + dashboard-web-app-09; Severity: Med/Low; Category: UI/UX/Reliability; Files: `apps/web/src/components/layout/notification-center.tsx`, `apps/web/src/app/(app)/dashboard/dashboard-client.tsx`, `apps/web/src/app/(app)/budget/page.tsx`, `apps/web/src/components/address/address-autocomplete-input.tsx`, support pages; Why: `res.ok` unchecked and catches set empty state, so server errors render as "all caught up"/empty/zero-actuals with no retry, masking outages; preference saves fire-and-forget; Validation: simulated 500 shows error/retry not empty state; Est. complexity: M; Risk of change: Low.)

- [ ] **Validate inline move-destination addresses through USPS correction** (Source: [CLAUDE] address-change-relocation-04; Severity: Med; Category: Logic; Files: `apps/web/src/app/api/moving/route.ts:213`, `apps/web/src/app/(app)/moving/new/page.tsx:216`; Why: inline destinations skip the USPS validation the `addresses/new` flow runs, so the target of all relocation tasks/coverage is lower-quality; Validation: destination runs USPS correction; Est. complexity: S; Risk of change: Low.)

- [ ] **Prune stale CLASSIFIER move tasks on service removal; entitlement-gate reminder cron; fix duplicate-task-on-state-change** (Source: [CLAUDE] moving-tasks-01 + moving-tasks-02 + moving-tasks-04; Severity: Med; Category: Logic; Files: `apps/web/src/lib/move-task-generation.ts:285-327,443-454,547-631`, `apps/web/src/app/api/cron/task-reminders/route.ts:86-120`; Why: removing a service leaves open CLASSIFIER tasks firing reminders forever; state-suffixed idempotency keys spawn duplicate tasks on destination edit; the reminder cron is not entitlement-gated so lapsed-free users get reminders they cannot open; Validation: removed service prunes open CLASSIFIER tasks (not COMPLETED/DISMISSED/edited); lapsed user stops reminders; no dup on state change; Est. complexity: M; Risk of change: Med.)

- [ ] **Fix EV-charging integration host (developer.nlr.gov → NREL developer.nrel.gov)** (Source: [CLAUDE] external-data-integrations-01; Severity: High [needs verification of live NREL host]; Category: Reliability; Files: `apps/web/src/lib/nlr-alt-fuel-stations.ts:9,61,70,79`, `apps/web/src/lib/nlr-alt-fuel-stations.test.ts:75`, `packages/shared/src/runtime-config.ts:651-678`; Why: the EV-charging lookup targets a likely-wrong host so when enabled it fails DNS and the dossier section silently never appears; Validation: live call returns stations; host-pin test repointed; Est. complexity: S; Risk of change: Low.)

---

## Phase 4 — API Validation, Data Integrity & Reliability

- [ ] **Wrap multi-step writes in transactions (register, profile, budget actuals, erasure, move-task generation, move-task complete)** (Source: [CLAUDE] signup-login-03 + dashboard-web-app-06 + budget-expenses-17 + account-deletion-export-09 + moving-tasks-03 + address-change-relocation-02; Severity: Med; Category: Reliability/Data; Files: `apps/web/src/app/api/auth/register/route.ts`, `apps/web/src/app/api/profile/route.ts`, `apps/web/src/app/api/budget/actuals/route.ts`, `apps/web/src/lib/account-deletion.ts`, `apps/web/src/lib/move-task-generation.ts`, `apps/web/src/lib/move-task-local-effects.ts`; Why: non-transactional sequences orphan accounts on signup failure, leave name changed while profile fails, drift actuals vs mirror, partial erasure, and concurrent generate/complete can violate unique constraints or double-create services; Validation: mid-sequence failure leaves no partial state; concurrent generate yields one row set; Est. complexity: M; Risk of change: Med.)

- [ ] **Add per-minute user-keying, timeouts, and shape/version guards to address/autocomplete paths** (Source: [CLAUDE] addresses-validation-01 + addresses-validation-02 + addresses-validation-04; Severity: Med; Category: Security/Reliability; Files: `apps/web/src/app/api/address-autocomplete/cost-controls.ts:46`, `apps/web/src/lib/address-autocomplete.ts:172,219`, `apps/web/src/lib/address-data-cache.ts:117`; Why: per-minute Places cost limiter is IP-keyed (authed IP-rotator defeats 45/min; NAT users starve each other); no `AbortSignal.timeout` on Places fetches; durable area cache returns unchecked JSON cast with no shape/version guard; Validation: same-user-different-IP test; timeout returns empty predictions; cache shape mismatch discarded; Est. complexity: M; Risk of change: Low.)

- [ ] **Confirm playstore & resend webhook signature + body-size coverage; add Resend idempotency store** (Source: [CLAUDE] security-surface-09 + email-pipeline-06; Severity: Med; Category: Security/Reliability; Files: `apps/web/src/app/api/webhooks/playstore`, `apps/web/src/app/api/webhooks/resend`, `apps/web/src/lib/resend-webhook.ts`; Why: middleware exempts `/api/webhooks/*` from CSRF/rate-limit/body-size and playstore+resend signature/cap were not confirmed; Resend has no idempotency store (replay within 300s tolerance); Validation: forged event rejected; oversized body capped; replayed svix-id short-circuited; Est. complexity: M; Risk of change: Low.)

- [ ] **Validate/derive client-supplied financial & enum inputs server-side (budget actualExpenses, monthlyCost bounds, enum-like VarChar)** (Source: [CLAUDE] budget-expenses-02 + budget-expenses-05 + database-schema-05; Severity: Med/Low; Category: Data; Files: `apps/web/src/app/api/budget/route.ts:182,197`, `apps/web/src/lib/validators.ts`, `packages/shared/src/budget-planning.ts`, `packages/db/prisma/schema.prisma`; Why: client `actualExpenses` persisted while `savingsRate` is server-derived (contradictory stored rows); negative/non-finite `monthlyCost` flows unclamped into projections; enum-like fields stored as plain VarChar without DB constraint; Validation: server derives/clamps; invalid enum rejected at boundary; Est. complexity: M; Risk of change: Low.)

- [ ] **Reconcile soft-delete include/select join-time leakage + schema-compat silent fallback alerting** (Source: [CLAUDE] database-schema-01 + database-schema-09 + payments-billing-web-01; Severity: Med/Low; Category: Data/Reliability; Files: `packages/db/src/soft-delete.ts`, `apps/web/src/lib/db-schema-compat.ts`, `apps/web/src/app/api/webhooks/stripe/route.ts:321-345,451-460`; Why: `withSoftDelete` only filters top-level finds (a new include/select join can leak soft-deleted-owner rows, e.g. emails to grace-deleted users); missing-column fallback degrades to legacy path and silently drops the Stripe out-of-order write guard with only a once-per-process warn; Validation: regression test soft-deleted parent not returned through child include; fallback emits Sentry error in prod; Est. complexity: M; Risk of change: Med.)

- [ ] **Add idempotency/uniqueness guards to non-idempotent create paths (address create, account-deletion request, connector double-enqueue)** (Source: [CLAUDE] address-change-relocation-06 + account-deletion-export-05 + provider-connector-dispatch-02; Severity: Med/Low; Category: Reliability; Files: `apps/web/src/app/api/addresses/route.ts:68`, `apps/web/src/lib/account-deletion.ts:158-197`, `apps/web/src/lib/connector-runtime.ts:220,258-332`; Why: rapid double-submit creates duplicate addresses; concurrent first-time delete requests both create GDPRRequests; concurrent connector enqueues bypass the per-user/day cap (two real Change-of-Address filings); Validation: double-submit deduped; one active DELETE request; cap enforced inside transaction; Est. complexity: M; Risk of change: Med.)

- [ ] **Standardize cron batch bounds (bill-reminders/bill-overdue unbounded findMany) + cursor pagination** (Source: [BOTH] [CLAUDE] database-schema-10 + [CHATGPT] AUD-006/AUD-012/SURF-02 (confirmed_valid); Severity: Low; Category: Performance/Reliability; Files: `apps/web/src/app/api/cron/bill-reminders/route.ts:52`, `apps/web/src/app/api/cron/bill-overdue/route.ts:62`, `apps/web/src/app/api/cron/contract-reminders/route.ts`, `apps/web/src/lib/daily-digest.ts:322,374`, `apps/web/src/app/api/export/route.ts`, `apps/web/src/app/api/export/pdf/route.ts`, `apps/web/src/lib/tax-report-data.ts`; Why: bill-reminders/bill-overdue do fully unbounded `findMany` with no `take`/`maxDuration` (others cap at 500/1000 — inconsistent standard); full export/PDF/tax queries are unbounded and synchronous in-request; Validation: every reminder/export query is bounded; large account does not produce unbounded scan; Est. complexity: M; Risk of change: Low.)

- [ ] **Distinguish soft vs hard email bounces; reconcile stale PENDING; add admin re-enable** (Source: [CLAUDE] notification-email-digest-01 + email-pipeline-03 + email-pipeline-08 + [CHATGPT] SEC-08/SURF-10 (partially_valid); Severity: Med/Low; Category: Reliability/Compliance; Files: `apps/web/src/app/api/webhooks/resend/route.ts:104-121`, `apps/web/src/lib/email-service.ts:192-298`; Why: every Resend bounce/complaint maps to a permanent ALL-mail opt-out (transient bounce silently kills a paying user's reminders); non-transactional send-then-log leaves delivered mail stuck PENDING; no automated outbox/retry worker or actionable admin retry (the `retryAvailable` flag is display-only); Validation: transient bounce doesn't permanently opt-out; PENDING reconciled; admin can retry/re-enable; Est. complexity: M; Risk of change: Low.)

---

## Phase 5 — Revenue (Billing / Subscription)

- [ ] **Bind IAP receipts to the purchasing account (appAccountToken / obfuscatedExternalAccountId)** (Source: [CLAUDE] mobile-iap-billing-01 + mobile-iap-purchase-01; Severity: High; Category: Security/Revenue; Files: `apps/mobile/src/lib/iap.ts:184`, `apps/web/src/app/api/mobile/iap/verify/route.ts`, `apps/web/src/lib/iap-common.ts:574-598`, `apps/web/src/lib/iap-apple.ts:137`, `apps/web/src/lib/iap-google.ts:230`, both store webhooks; Why: `appAccountToken`/`obfuscatedExternalAccountId` are typed but never set or enforced, so an unclaimed signed transaction is claimed by whichever account verifies it first — entitlement theft / buyer lockout; Validation: mismatched token/userId rejected; existing-owner receipt still grants to owner; e2e round-trip; Est. complexity: L; Risk of change: Med — gate verify-side enforcement behind a flag during client rollout.)

- [ ] **Reject FAMILY_SHARED receipts; freshness-bound JWS fallback; add IAP event-ordering guard** (Source: [CLAUDE] mobile-iap-billing-02 + mobile-iap-billing-04 + mobile-iap-billing-05 + mobile-iap-purchase-02/03; Severity: Med; Category: Security/Logic; Files: `apps/web/src/lib/iap-common.ts:348-451`, `apps/web/src/lib/iap-apple.ts:124`, `apps/web/src/app/api/mobile/iap/verify/route.ts:110-123`, store webhook routes, `packages/db/prisma/schema.prisma` (IAP event timestamp); Why: `inAppOwnershipType` (FAMILY_SHARED) never validated (recipient gets full entitlement); App-Store-API-down fallback grants from client JWS with no `signedDate` freshness bound (stale refunded/expired transaction replayable); no IAP event-ordering guard so a stale renewal refresh can undo a refund/revoke; Validation: FAMILY_SHARED rejected; stale JWS rejected; refund not re-activated; Est. complexity: M; Risk of change: Med.)

- [ ] **Unify production-like env predicates so sandbox/test-purchase allowlist is enforced on staging/preview** (Source: [BOTH] [CLAUDE] mobile-iap-billing-03 + app-bootstrap-config-06 + [CHATGPT] SEC-12 (partially_valid); Severity: Med; Category: Security/Config; Files: `apps/web/src/lib/billing-config.ts:3-9`, `apps/web/src/lib/iap-common.ts:515,548`, `apps/web/src/app/api/webhooks/appstore/route.ts`, `apps/web/src/app/api/webhooks/playstore/route.ts`, `apps/web/src/lib/production-readiness.ts`, `packages/shared/src/env-catalog.ts`; Why: `isBillingProductionLike` returns false for staging/preview while webhook `isProductionLikeRuntime` returns true, so a staging/preview deploy holding live store creds would not enforce the sandbox allowlist — any user could claim a $0 sandbox subscription as real premium (three divergent prod-like detectors overall); Validation: staging enforces sandbox allowlist; one shared `isProductionLike()` helper; Est. complexity: M; Risk of change: Low.)

- [ ] **Verify customer↔user binding & price integrity in Stripe webhooks; gate side-effects on payment_status** (Source: [CLAUDE] subscription-payment-web-01 + payments-billing-web-02 + payments-billing-web-03; Severity: Med; Category: Security/Data/Revenue; Files: `apps/web/src/app/api/webhooks/stripe/route.ts:675-797,995-1006`, `apps/web/src/lib/billing.ts:79-108`, `apps/web/src/app/api/stripe/checkout/route.ts:60-85`; Why: `checkout.session.completed` entitles `metadata.userId` without asserting `session.customer` matches stored `stripeCustomerId`, fires activation email/admin alert/redemption flip without reading `payment_status`, and maps price→plan by identity only (a repointed/edited price still grants the full plan); Validation: customer mismatch alerts; unpaid checkout sends no activation; price drift alerts; Est. complexity: M; Risk of change: Med.)

- [ ] **Harden webhook idempotency release & connector dispatch idempotency observability** (Source: [CLAUDE] subscription-payment-web-02 + provider-connector-dispatch-03; Severity: Low; Category: Reliability; Files: `apps/web/src/app/api/webhooks/stripe/route.ts:1316-1322`, `apps/web/src/lib/webhook-idempotency.ts`, `apps/web/src/app/api/connectors/[key]/webhook/route.ts:118-134`; Why: best-effort reservation-release is unmonitored (a failed release permanently drops one-shot side effects on Stripe retry); connector webhook idempotency keys on raw-body hash rather than a stable business key (re-serialized retries bypass dedupe). Note: core idempotency standardization (SEC-02, AUD-004) is `already_fixed`; Validation: failed release emits Sentry alert; dedupe on parsed dispatch ref; Est. complexity: S; Risk of change: Low.)

- [ ] **Hot-path connector mode-derivation query reduction (5 sequential runtime-config reads per row)** (Source: [CLAUDE] provider-connector-dispatch-01; Severity: Low; Category: Performance; Files: `apps/web/src/lib/connector-runtime.ts:133-164,448`, `apps/web/src/lib/runtime-config.ts:28-54`; Why: `isApiSyncConnector` does 5 uncached `getRuntimeConfigValue` reads per dispatch row per attempt, serializing the batch; Validation: per-row query count drops with no behavior change; Est. complexity: S; Risk of change: Low.)

---

## Phase 6 — UI/UX & Accessibility

- [ ] **Fix dark-mode contrast on solid badges & accent-on-white CTAs** (Source: [CLAUDE] component-theme-system-01 + component-theme-system-02; Severity: Med/High (a11y); Category: Accessibility; Files: `apps/web/src/components/ui/badge.tsx:14-16`, `apps/web/src/components/shared/empty-state.tsx:36`, `apps/web/src/components/layout/app-shell.tsx:108`, `apps/web/src/styles/globals.css:89-96`; Why: solid success/warning/info badges and the EmptyState CTA + AppShell skip-link render white text on light dark-theme fills (2.05–2.33:1, WCAG AA needs 4.5); status/payment signals and the keyboard skip link are unreadable in the default theme; Validation: dark-mode contrast tests pass WCAG AA; Est. complexity: S; Risk of change: Low.)

- [ ] **Accessibility primitives: Dialog scroll-lock/asChild/aria, progressbar ARIA, aria-current, form-error wiring, StatusBadge role** (Source: [CLAUDE] component-theme-system-05 + component-theme-system-06 + component-theme-system-08 + budget-expenses-11 + ui-ux-12 + ui-ux-15; Severity: Med/Low; Category: Accessibility/Reliability; Files: `apps/web/src/components/ui/dialog.tsx`, `apps/web/src/components/shared/confirm-dialog.tsx:51-59`, `apps/web/src/components/ui/status-badge.tsx:86-104`, `apps/web/src/components/ui/input.tsx`, `apps/web/src/components/ui/textarea.tsx`, `apps/web/src/components/ui/label.tsx`, `apps/web/src/app/(app)/budget/page.tsx`, `apps/web/src/components/layout/mobile-nav.tsx`; Why: Dialog has no scroll-lock, ignores `asChild`, computes focusables once; ConfirmDialog swallows `onConfirm` errors and closes anyway (failed delete looks successful); progress bars/inputs lack ARIA; StatusBadge misuses `role=status`; mobile nav lacks `aria-current`; Validation: a11y audit on key flows; failed confirm keeps dialog open; Est. complexity: M; Risk of change: Low.)

- [ ] **i18n remaining hardcoded English strings + light-mode theme-color meta** (Source: [CLAUDE] component-theme-system-07 + component-theme-system-09 + dashboard-web-app-07 + component-theme-system-10; Severity: Low; Category: Accessibility/i18n; Files: `apps/admin/src/components/theme-toggle.tsx:26-44`, `apps/web/src/components/marketing/landing-theme-toggle.tsx:14-23`, `apps/web/src/components/ui/dialog.tsx:108`, `apps/web/src/app/layout.tsx:149-152,180`, `apps/web/src/app/(app)/support/[id]/page.tsx`, `apps/mobile/src/lib/theme.ts:348-367`; Why: theme/dialog controls and support dates are hardcoded English/en-US while apps use next-intl; web has a single static theme-color and no `colorScheme`; mobile dark default contradicts its no-flash docstring; Validation: es locale renders translated; light-mode chrome matches; Est. complexity: M; Risk of change: Low.)

- [ ] **Reconcile pricing/free-tier marketing consistency (gate CONSUMER_FREE copy; fix OG brand glyph)** (Source: [CLAUDE] marketing-seo-content-02 + marketing-seo-content-05 + marketing-seo-content-03; Severity: Med/Low; Category: Logic/UI; Files: `apps/web/src/app/why-free/page.tsx:7-44`, `apps/web/src/app/page.tsx:236,592`, `apps/web/src/app/faq/faq-data.ts:27-62`, `apps/web/src/app/faq/structured-data.ts:37-54`, `apps/web/src/app/opengraph-image.tsx:46`; Why: `/why-free` and homepage market `$0/no credit card` ungated while `/pricing` and FAQ (and FAQPage JSON-LD) describe paid plans (contradictory/deceptive-pricing exposure); default OG image shows a leftover "M" brand glyph; Validation: a single pricing model rendered consistently across pages and rich results; OG shows correct mark; Est. complexity: M; Risk of change: Low.)

- [ ] **Notifications inbox pagination + single mark-read PATCH** (Source: [CLAUDE] dashboard-web-app-05 + notifications-push-04; Severity: Low; Category: UI/UX/Performance; Files: `apps/web/src/app/(app)/notifications/page.tsx`, `apps/web/src/app/api/notifications/feed/route.ts`, `apps/web/src/lib/notification-feed-client.ts`; Why: inbox never requests pages beyond the first 20; mark-read fires two PATCHes per row; Validation: older notifications reachable; one PATCH per mark-read; Est. complexity: S; Risk of change: Low.)

- [ ] **Surface push-availability state to mobile users when NOTIFICATION_PUSH_ENABLED is off** (Source: [BOTH] [CHATGPT] SEC-09 (confirmed_valid) + AUD-010 (partially_valid); Severity: Low; Category: UX/Ops; Files: `apps/web/src/app/api/notifications/preferences/route.ts:34-44`, `apps/mobile/app/settings/notifications.tsx:73-111`, `apps/admin/src/lib/integration-status.ts:89-186`; Why: backend correctly fails closed when the push master switch is off (unset by default), but the mobile settings screen accepts and saves push toggles with no "unavailable" hint and admin health omits the push/SMS channel state, so users silently receive nothing; Validation: `pushAvailable` boolean surfaced to client and admin health; mobile shows unavailable state; Est. complexity: S; Risk of change: Low.)

- [ ] **Add backup-code input to web sign-in MFA challenge** (Source: [CLAUDE] auth-session-09; Severity: Low; Category: UI/UX; Files: `apps/web/src/app/sign-in/page.tsx`, `apps/web/src/lib/password-login.ts`; Why: the API accepts a backup code but the web MFA step only sends `mfaCode`, so a user who lost their authenticator cannot recover from web; Validation: backup-code login works from web; Est. complexity: S; Risk of change: Low.)

---

## Phase 7 — Performance

- [ ] **Reduce hot-path query/IO amplification (workspaces N+1, onboarding serial POSTs, admin auth row reads, move-task catalog scan)** (Source: [CLAUDE] authorization-workspaces-08 + onboarding-04 + admin-auth-security-09 + moving-tasks-09; Severity: Low; Category: Performance; Files: `apps/web/src/app/api/workspaces/route.ts:37-96`, `apps/web/src/app/onboarding/onboarding-client.tsx:716-763`, `apps/admin/src/lib/auth.ts`, `apps/web/src/lib/move-task-generation.ts:100-122`; Why: GET /api/workspaces does per-membership counts (N+1); onboarding saves N sequential POSTs; admin auth path does 3 findUnique + a write per gated request; move-task sync loads up to 1000 providers + serviceability enrichment on every sync; Validation: per-request query count drops, no behavior change; Est. complexity: M; Risk of change: Low.)

- [ ] **Cache per-cron/per-run heavy work (email template/app-URL reads, mobile tracking-consent query, area-data cache stale gating)** (Source: [CLAUDE] email-pipeline-11 + analytics-flags-runtime-05 + addresses-validation-11; Severity: Low; Category: Performance; Files: `apps/web/src/lib/email-service.ts`, `apps/web/src/lib/tracking-consent.ts:32`, `apps/web/src/lib/address-data-cache.ts:135`; Why: reminder/digest senders re-read app-URL + template rows per recipient; mobile analytics consent hits DataConsent per event; stale fallback serves prior DEGRADED/EMPTY as a cache hit regardless of real status; Validation: template/URL cached per cron run; consent cached per session; stale gated on prior status; Est. complexity: M; Risk of change: Low.)

- [ ] **Feature-flag cache invalidation + deterministic PERCENTAGE evaluation** (Source: [CLAUDE] analytics-flags-runtime-02 + analytics-flags-runtime-03; Severity: Med/Low; Category: Logic/Reliability; Files: `apps/web/src/lib/feature-flags.ts:11-13,76`, `apps/admin/src/app/api/feature-flags/route.ts`; Why: 60s flag cache is never invalidated (a kill-switch/monetization rollback lags up to 60s in the web process) and PERCENTAGE flags fall back to `Math.random()` without a stable subject (per-call coin flip in anonymous/server contexts); Validation: invalidation hook works or 60s SLA documented; PERCENTAGE fails closed without a subject; Est. complexity: S; Risk of change: Low.)

---

## Phase 8 — Tests & Regression Safety

- [ ] **Add web API route tests for the still-uncovered high-risk groups (cron reminders, workspaces member/rename/invitations, blog, public provider, help)** (Source: [BOTH] [CLAUDE] addresses-validation-09 + analytics-flags-runtime-11 + auth-session-10 + [CHATGPT] AUD-008/SURF-03/SURF-07 (confirmed_valid); Severity: Low; Category: Testing; Files: `apps/web/src/app/api/cron/{bill-reminders,bill-overdue,contract-reminders,move-reminders,task-reminders}/route.ts`, `apps/web/src/app/api/workspaces/[id]/{members/leave,rename,invitations/[invId]}/route.ts`, `apps/web/src/app/api/{help,providers/[id],blog/posts}/route.ts`, `apps/web/src/lib/usps-address-validation.ts`, `apps/web/src/app/api/addresses/validate/route.ts`, `apps/web/src/lib/feature-flags.ts`, `apps/web/src/lib/user-step-up.ts`, `apps/web/src/app/api/auth/oauth/google/callback/route.ts`; Why: ~51/171 web routes lack adjacent tests; reminder crons, USPS orchestrator/validate route, Google OAuth callback, step-up brute-force, and feature-flag determinism are untested; Validation: each route asserts guard/dedupe/preference-matrix; suite fails pre-fix, passes after; Est. complexity: L; Risk of change: Low — additive.)

- [ ] **Add admin API route tests for uncovered permission/content/analytics surfaces** (Source: [BOTH] [CHATGPT] AUD-013 (partially_valid); Severity: Low; Category: Testing; Files: `apps/admin/src/app/api/{analytics,analytics/overview,blog/posts,blog/categories,help-center,provider-governance}/route.ts`; Why: 56/125 admin routes lack adjacent tests; analytics (0/5), blog, help-center, and provider-governance base route enforce `requirePermission` but never assert it — a permission-tuple or validation regression could ship undetected (tickets is now covered); Validation: each test mocks `requirePermission` to throw and expects 401/403, plus validation paths; Est. complexity: M; Risk of change: Low.)

- [ ] **Add authenticated app + admin E2E journeys (login → address → service → move → export; admin login)** (Source: [BOTH] [CLAUDE] 6.x roadmap + [CHATGPT] SURF-08/AUD-008 (confirmed_valid); Severity: Low; Category: Testing/E2E; Files: `apps/web/tests/e2e/`, `apps/web/playwright.config.ts`, `apps/admin` (new playwright config); Why: only `public-pages` and `accessibility` specs exist, no auth-setup project/storageState; admin app has no E2E at all despite a real login surface; Validation: storageState auth project + core journey specs wired into CI; Est. complexity: L; Risk of change: Low.)

- [ ] **Add mobile emulator/screen smoke suite (Detox or Maestro) for login, IAP sandbox, push register, offline resume** (Source: [BOTH] [CHATGPT] AUD-014/SURF-14 (partially_valid); Severity: Low; Category: Testing/Mobile; Files: `apps/mobile/package.json`, `.github/workflows/ci.yml`, new mobile e2e harness; Why: all 34 mobile tests are vitest unit tests of pure functions; no emulator/screen-level or store-live release-gate test exists, so native runtime/UI and live store sandbox behavior are unguarded; Validation: one Android emulator smoke job gates release; Est. complexity: L; Risk of change: Low.)

- [ ] **Add Stripe concurrent-duplicate + out-of-order webhook regression tests** (Source: [BOTH] [CHATGPT] SURF-06 (partially_valid); Severity: Low; Category: Testing/Concurrency; Files: `apps/web/src/app/api/webhooks/stripe/route.test.ts`, `apps/web/src/app/api/webhooks/stripe/route.ts`; Why: livemode/IAP-race/connector dup are covered, but the Stripe route lacks a `Promise.all` concurrent-race test and a positive out-of-order test that actually trips the stale-skip branch (`lastStripeEventAt` future) — the guards exist in code but are unverified; Validation: concurrent deliveries run side-effects once; stale event skipped; Est. complexity: S; Risk of change: Low.)

---

## Phase 9 — Dead Code, Stale Docs & Refactor

- [ ] **Add deprecation banner + current-architecture snapshot to RELOCATION_MANAGER_SPEC.md** (Source: [BOTH] [CHATGPT] AUD-009 (confirmed_valid); Severity: Low; Category: Documentation; Files: `RELOCATION_MANAGER_SPEC.md`, `SYSTEM_STATUS.md`; Why: the spec's "Complete Prisma Schema"/"Tech Stack" still present Postgres/SQLite + Clerk + Tesseract/Cloudinary + Next 15 + Document/OCR/Badge models as authoritative with no banner, none of which match current MySQL + custom-jose-JWT + R2 + Next 16; Validation: prominent banner + pointer to a current-architecture doc; Est. complexity: S; Risk of change: Low.)

- [ ] **Remove dead tooling, unused deps & dead exports** (Source: [CLAUDE] dead-02/03/05/07 + database-schema-11 + app-bootstrap-config-07 + auth-session-08 + analytics-flags-runtime-09 + dashboard-web-app-10 + admin-impersonation-01 + admin-auth-security-11; Severity: Low; Category: Dead Code; Files: `packages/db/prisma/_migrate-to-mysql.ts`, `packages/db/prisma/_migration-data.json`, `packages/db/prisma/legacy-sqlite-migrations/`, `packages/db/check-admin.mjs`, `apps/web/package.json`/`apps/admin/package.json` (recharts), root `NEW GENERATION/`/`design-src/`/`previews/`, `apps/web/src/lib/sentry.ts:17-40` (initSentry), `apps/web/src/lib/oauth.ts` (decodeJwtPayload), `apps/web/src/lib/feature-flags.ts:99` (getAllFlags); Why: 653 KB dead migration JSON + obsolete tooling, ad-hoc admin-dump script, unused recharts, prototype dirs, dead non-PII-scrubbing Sentry init, unverified-JWT helper, and unused exports clutter the tree; Validation: build/test green after removal; confirm no dynamic imports; Est. complexity: M; Risk of change: Low.)

- [ ] **Fix stale comments/constraints & misleading config docs** (Source: [CLAUDE] authorization-workspaces-07/10 + app-bootstrap-config-03/08 + admin-impersonation-08; Severity: Low; Category: Documentation/Logic; Files: `apps/web/src/lib/workspace-context.ts:1-12,115-118`, `docker/web.prod.Dockerfile`, `docker/admin.prod.Dockerfile`, `packages/shared/src/env-catalog.ts:114`, `apps/web/src/lib/impersonation-audit.ts:38`; Why: file header claims a hot-path resolver is "unused/tested"; Dockerfiles claim a `BUILD_PHASE` boot gate that does not exist; env-catalog says FIELD_ENCRYPTION_KEY silently writes plaintext (it throws in prod); impersonation audit slices action to 20 vs schema 64; Validation: comments match code; either implement the boot gate or correct the docs; Est. complexity: S; Risk of change: Low.)

- [ ] **Collapse duplicated/drifted shared code (validators web vs shared, email sanitizer, recommendation engine, deep-relative shared-* wrappers, design tokens)** (Source: [CLAUDE] dead-01 + email-pipeline-09 + module-map-02 + dead-06 + module-map-01/component-theme-system-03; Severity: Low; Category: Refactor; Files: `apps/web/src/lib/validators.ts`, `packages/shared/src/validators.ts`, `apps/admin/src/lib/email-template-sanitizer.ts`, `packages/shared/src/email-html-sanitizer.ts`, `apps/{web,mobile,admin}/src/lib/recommendation-engine.ts`, `apps/web/src/lib/shared-*.ts`, `packages/shared/src/design-tokens.ts`, `apps/web/src/styles/globals.css`, `apps/admin/src/app/globals.css`; Why: web/shared validators have drifted (visa enum, provider fields); email sanitizer duplicated byte-for-byte (a security fix to one misses the other); recommendation engine reimplemented in 3+ places; design tokens hand-synced across 3 palettes; Validation: thin re-exports; token-parity test; Est. complexity: L; Risk of change: Med — do incrementally.)

- [ ] **Structural tenant-isolation backstop (Prisma $extends/lint scope token; workspaceId NOT NULL after backfill)** (Source: [CLAUDE] database-schema-02 + authorization-workspaces-05; Severity: Low (uncertain, downgraded High→Low); Category: Refactor/Security; Files: `apps/web/src/lib/workspace-data-scope.ts`, `packages/db/prisma/schema.prisma`, `packages/db/src/soft-delete.ts`; Why: tenant isolation is application-only — every author must call `scopedRecordWhere`/`assertScopedRecordAction`, with no DB/RLS/middleware backstop, so one forgotten scope in a future route is a cross-tenant leak; `workspaceId` is nullable; Validation: cross-tenant IDOR tests return 404/403; lint fails on unscoped scoped-model query; Est. complexity: L; Risk of change: Med — large surface, do after Phases 1–8 stable.)

- [ ] **Standardize design-token source of truth & shared web/admin UI primitives** (Source: [CLAUDE] component-theme-system-04 + ui-ux-08 + ui-ux-14 + module-map-01; Severity: Low; Category: Refactor; Files: `packages/shared/src/design-tokens.ts`, `apps/web/src/styles/globals.css`, `apps/admin/src/app/globals.css`, `apps/web/src/styles/aurora.css`, `apps/web/src/components/ui/`, `apps/admin/src/components/`; Why: admin re-implements ConfirmDialog/EmptyState/theme-provider/theme-toggle with no shared UI layer and three theme toggles; tokens drift across design-tokens.ts vs web vs admin (e.g. `--bg #070B14` vs `#171E2B`); no shared Table/Tabs/Dropdown/Spinner primitives; Validation: codegen CSS vars from design-tokens.ts; shared primitives consumed by both apps; Est. complexity: L; Risk of change: Med.)

---

## Appendix: ChatGPT items dropped as already-fixed or inaccurate

**Already-fixed (18) — recommended mitigation already present in current code; no action carried:**

- AUD-001 — Consumer "Documents" promise + dead `documentUploadSchema`/UI removed; no Service documents relation remains.
- AUD-002 — "Snap a bill" copy replaced with manual-entry "Log a bill" language; no camera/OCR promise.
- AUD-003 — "USPS forwarding setup, automatically" copy replaced with "guided step by step"; connector section flag-gated + regression-tested.
- AUD-004 — Stripe webhook now reserves event id before any side-effect (reserve-before-act), closing the check-then-act race.
- SEC-02 — Idempotency standardized (unique-constraint + reserve-before-act + P2002) across all webhook/notification/connector flows.
- SEC-03 — Soft-delete now enforced by the `withSoftDelete` Prisma client extension on the default client, not per-route (+ schema-parity regression test).
- SEC-04 — Family/Pro checkout 503-on-missing-price is intentional fail-closed; `/api/ready` now gates the price keys.
- SEC-05 — Public-route allowlist regression risk closed: `middleware.test.ts` positive + negative (prefix-accident, out-of-namespace) guards in CI.
- SEC-06 — Destructive admin routes confirmed gated by `requirePasswordConfirm` (+ MFA/OTP on the most dangerous); only reversible content soft-deletes are RBAC-only by design.
- SEC-07 — Multi-layer tested log/Sentry/audit redaction wired across all sinks; flagged auth diagnostics log only booleans/enums.
- SEC-10 — SMS is a fail-closed placeholder with no provider and is not offered/promised in any user surface.
- SEC-11 — Sentry PII scrubbing (`sendDefaultPii:false`, body/cookie/header strip, key+value scrubbers) implemented and tested across web/admin/mobile.
- SURF-05 — Connector/address-change automation copy rewritten to guided-first; runtime fail-closed; regression tests lock it in.
- SURF-09 — Notification feed ownership correctly scoped AND dedupe weakness fixed via `dedupeKey` column + `@@unique([userId,channel,dedupeKey])`.
- SURF-13 — Stripe checkout runtime price-config readiness check now exists in `production-readiness.ts` + `/api/ready` (503).
- SURF-15 — Admin destructive-action password/MFA step-up coverage is broad, fail-closed, and route-tested.
- SURF-16 — Move/contract reminder crons now do per-user timezone resolution + local-8am gating + UTC-pinned display formatting.
- SURF-11 — (partially_valid on the literal "fields absent" half, but the *core* atomic-reservation concern is already_fixed via the unique-PK reserve/release marker; the missing status/startedAt/attempts/lastError columns are an optional observability enhancement only — not carried as an actionable defect).

**Inaccurate (1) — premise does not hold against current code:**

- SURF-12 — No Service UI/type/API references a `documents` relation and no `Document` model is expected by any service surface; the "UI expects a documents relation that doesn't exist" premise is false (only the unrelated Partner/Mover verification document models exist, which are fully backed).
