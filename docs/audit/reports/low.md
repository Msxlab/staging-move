# Low & Info Findings (239)

| ID | Area | Category | Finding | Evidence | Impact | Recommendation | Files | Verdict |
|---|---|---|---|---|---|---|---|---|

| account-deletion-export-06 | Account Deletion / Data Export (CCPA) | Data | Data export creates no DSAR fulfillment record (Art. 15) | Export routes write only per-call AuditLog rows and never create a GDPRRequest; only DELETE creates one; AuditLog purged at 365 days. | Weaker long-term traceability for data-access request fulfillment compared to deletion. | Optionally persist EXPORT-type GDPRRequest (or fulfillment record) on successful export. | apps/web/src/app/api/export/route.ts:146-153,593-606; apps/web/src/lib/account-deletion.ts:169-197 |  |

| account-deletion-export-07 | Account Deletion / Data Export (CCPA) | Security | JSON/CSV export responses lack Cache-Control: no-store | JSON/CSV export responses set only Content-Type and Content-Disposition (export/route.ts:661-723); PDF route sets Cache-Control private no-store. | Exported PII could be cached by intermediaries or browser disk cache. | Add Cache-Control private no-store to JSON/CSV export responses, matching PDF route. | apps/web/src/app/api/export/route.ts:661-723; apps/web/src/app/api/export/pdf/route.ts:224-233 |  |

| account-deletion-export-09 | Account Deletion / Data Export (CCPA) | Architecture | Self-service erasure is non-transactional (admin path uses $transaction) | processAccountDeletionRequest sequential deletes/updates with no $transaction (account-deletion.ts:311-378); admin hard-delete wraps same steps in $transaction; data-retention cron retries PROCESSING requests, self-healing partial failures. | Mid-sequence crash leaves partial state until next cron run; no corruption observed. | Consider wrapping erasure in $transaction to match admin path. | apps/web/src/lib/account-deletion.ts:311-378; apps/admin/src/lib/hard-delete-user.ts:247-320 |  |

| address-change-relocation-05 | Address Change / Relocation flow | Logic | New move destination counts against address cap with a generic limit error | plan-limits.ts:396 canCreateMovingDestinationAddress delegates to canCreateAddress; moving/route.ts:142 returns ADDRESS_LIMIT_REACHED. | User creating a move with a new destination blocked mid-flow by address cap with address-limit message. | Confirm intended; if so surface move-specific message; consider exempting/separately counting destination. | apps/web/src/lib/plan-limits.ts:396, apps/web/src/app/api/moving/route.ts:142 |  |

| address-change-relocation-06 | Address Change / Relocation flow | Reliability | Address create has no idempotency; rapid double-submit creates duplicates | addresses/route.ts:68 POST creates Address with no idempotency key, only 20/min rate limit; client loading flag but no server dedupe. | Double-submit/retry creates duplicate address records. | Add lightweight server dedupe (recent identical street+zip) or idempotency token; keep client submit-lock. | apps/web/src/app/api/addresses/route.ts:68, apps/web/src/app/(app)/addresses/new/page.tsx:82 |  |

| address-change-relocation-07 | Address Change / Relocation flow | Security | POST /api/moving IP rate-limit key omits userId | moving/route.ts:78 getRateLimitKey(request,moving:create) no identity (IP-keyed); parallel per-user key runs alongside. | Shared-NAT users share IP bucket and could 429 each other; mitigated by per-user key. | Pass {userId} to IP-keyed call or drop redundant IP key. | apps/web/src/app/api/moving/route.ts:78 |  |

| address-change-relocation-08 | Address Change / Relocation flow | Reliability | Primary-promotion-on-delete resolved outside the transaction (single-actor assumption) | addresses/[id]/route.ts:230 reads next-primary candidate read-only before delete transaction, comment assuming single-threaded user. | In shared workspace two concurrent deletes could leave zero or two primary addresses, degrading provider-detail/compare/connector surfaces. | Resolve promotion candidate inside the transaction (or add post-condition guard) for workspace case. | apps/web/src/app/api/addresses/[id]/route.ts:230 |  |

| address-change-relocation-09 | Address Change / Relocation flow | Data | Address CREATE writes no AuditLog (UPDATE/DELETE do) | addresses/route.ts:120 creates Address with no createAuditLog call, whereas PATCH and DELETE emit audit entries. | Audit trail gap for creation of PII-bearing address records. | Emit createAuditLog CREATE Address on create for parity. | apps/web/src/app/api/addresses/route.ts:120 |  |

| address-change-relocation-10 | Address Change / Relocation flow | Architecture | Mixed auth import paths for requireDbUserId across flow routes | addresses/route.ts:3 imports requireDbUserId from @/lib/auth; addresses/validate/route.ts:3 from @/lib/user-auth; auth.ts re-exports same symbol. | Cosmetic inconsistency only. | Standardize on a single import path. | apps/web/src/app/api/addresses/route.ts:3, apps/web/src/app/api/addresses/validate/route.ts:3 |  |

| addresses-validation-05 | Addresses & Address Validation | Security | Daily-cap overrides read from process.env, not runtime-config | cost-controls.ts:27-30 readDailyLimit uses process.env[key] while enable flag and Google key use getRuntimeConfigValue. | Operators tuning PLACES_*_DAILY_*_LIMIT via admin runtime-config see no effect; caps silently stay at fallbacks. | Read caps via getRuntimeConfigValue, or document env-only. | apps/web/src/app/api/address-autocomplete/cost-controls.ts:27 |  |

| addresses-validation-06 | Addresses & Address Validation | UI/UX | Search errors and 429 daily-cap message never surfaced in UI | address-autocomplete-input.tsx:87-98 checks response.ok then reads only predictions/enabled; on error or 429 throws into silent catch clearing predictions. | Transient 500s look like empty results; cost-cap guidance copy is dead on web client. | Distinguish error/cap from empty-results; show cap message. | apps/web/src/components/address/address-autocomplete-input.tsx:87 |  |

| addresses-validation-07 | Addresses & Address Validation | Security | USPS validate fail-open returns 200 on auth failure | addresses/validate/route.ts:31-36 returns safe() (200) when requireDbUserId throws instead of 401. Intentional fail-open. | Masks a future auth/entitlement regression from 401-expecting tests; no data leak (suggestion runs after gates). | Keep fail-open but add test asserting unauth/unentitled never receive a suggestion payload. | apps/web/src/app/api/addresses/validate/route.ts:31 |  |

| addresses-validation-08 | Addresses & Address Validation | Performance | details lookup has no client in-flight guard (double-bill on rapid re-select) | address-autocomplete-input.tsx:122 handleSelect no guard; rapid double-click fires two /details calls, each billable. | Minor extra Places details spend; mitigated by daily cap. | Disable option buttons while loading. | apps/web/src/components/address/address-autocomplete-input.tsx:122 |  |

| addresses-validation-11 | Addresses & Address Validation | Logic | getOrFetchSection serves prior row on throw regardless of REAL status | address-data-cache.ts:135-139 returns existing on fetcher throw without checking existing.status; prior DEGRADED/EMPTY served as STALE hit. | Degraded/empty data classified as cache hit on later upstream failure; low impact (null-shaped data). | Optionally gate stale fallback on prior status===REAL. | apps/web/src/lib/address-data-cache.ts:135 |  |

| addresses-validation-10 | Addresses & Address Validation | Security | PII/secret handling and SSRF surface verified clean | formattedAddress encrypted on write; census logs status only; Places/USPS hosts hardcoded with encodeURIComponent + session-token sanitization; USPS https/apis.usps.com allow-list; creds server-only. | No SSRF, no secret leak, no PII logging found. | Keep outbound URLs constant; confirm log retention covers userId+ip logged on cost-cap hits. | apps/web/src/lib/usps-address-validation.ts:44, apps/web/src/lib/census-geocoder.ts:222 |  |

| admin-auth-security-06 | Admin Auth & Security | Security | Coarse, header-derived session fingerprint weakens anti-hijack binding | admin session-fingerprint.ts:12-49 binds IP /24 or /64 plus normalized UA/accept-language/sec-ch-ua, stored as JWT fp, recomputed from request; all client-influenceable; with finding 02 IP forgeable. | Stolen cookie replayed from same /24 + same UA (or spoofed IP headers) passes fingerprint check. Defense-in-depth. | Add server-issued high-entropy secret stored on session row to binding; keep IP/UA heuristic secondary. | apps/admin/src/lib/session-fingerprint.ts; apps/admin/src/middleware.ts |  |

| admin-auth-security-07 | Admin Auth & Security | Logic | MFA-required role can self-disable MFA (transient required-role-without-MFA state) | admin auth/mfa/disable/route.ts:50-63 lets any admin (incl ADMIN/SUPER_ADMIN) disable MFA after step-up; gates re-force enrollment on next request. | Brief required-role MFA-off state plus forced re-setup loop; operational friction not direct bypass. | For ADMIN/SUPER_ADMIN block self-disable or require enrolling replacement factor in same transaction. | apps/admin/src/app/api/auth/mfa/disable/route.ts |  |

| admin-auth-security-08 | Admin Auth & Security | Security | Session revoke-handle HMAC has a hardcoded dev-default secret fallback | admin auth/sessions/route.ts:17-23 chains ADMIN_SESSION_HANDLE_SECRET\|\|ADMIN_JWT_SECRET\|\|JWT_SECRET\|\|AUTH_SECRET\|\|dev literal; ADMIN_JWT_SECRET required >=32 elsewhere so literal effectively unreachable. | In misconfigured env lacking all four, revoke-handle signatures forgeable (still gated by requireAdmin + step-up). | Remove literal fallback and throw if no secret configured. | apps/admin/src/app/api/auth/sessions/route.ts |  |

| admin-auth-security-09 | Admin Auth & Security | Performance | Per-request admin-row read/write amplification in the auth path | admin auth.ts requireAdmin findUnique + lastActivity updateMany every gated request; requireRole second findUnique; checkPermission third findUnique with permissions. | Avoidable DB read/write amplification on every admin API call. | Memoize admin row across requireAdmin/requireRole/checkPermission per request; throttle lastActivity updates. | apps/admin/src/lib/auth.ts |  |

| admin-auth-security-10 | Admin Auth & Security | Reliability | Anomaly detection (security-monitor) is in-memory only | admin security-monitor.ts:35-72 keeps brute-force/credential-stuffing/sensitive-op counters in process Maps; reset on deploy, no cross-instance aggregation. | Supplementary anomaly alerting best-effort per-instance; hard limiters remain real control. | Back anomaly counters with Redis or document as supplementary. | apps/admin/src/lib/security-monitor.ts |  |

| admin-auth-security-11 | Admin Auth & Security | Dead Code | Minor dead code / cleanup in the auth module | auth-cookie.test.ts exists but no auth-cookie.ts source; PUBLIC_PREFIX_PATHS empty array makes isPublicPath prefix branch unreachable; validateFingerprint exported but live check inline in middleware. | Minor clutter / confusion; no functional impact. | Confirm targets and prune or rename. | apps/admin/src/lib/auth-cookie.test.ts; apps/admin/src/middleware.ts; apps/admin/src/lib/auth.ts |  |

| admin-impersonation-01 | Admin Impersonation | Dead Code | Impersonation start endpoint has no UI caller (flow unreachable through product) | POST /api/users/:id/impersonate fully implemented but grep across apps/**/*.tsx finds no client fetch to /impersonate or handoff URL; user-detail-client only displays Impersonated badge, no Impersonate button/handler. | Documented end-to-end flow cannot be triggered through UI; unfinished feature shipping live backend, or undocumented API-only capability bypassing UX safeguards. | Confirm intended exposure: build UI entry or remove/feature-flag the endpoint. | apps/admin/src/app/api/users/[id]/impersonate/route.ts; apps/admin/src/app/(admin)/users/[id]/user-detail-client.tsx | confirmed (sev High→Low) |

| admin-impersonation-06 | Admin Impersonation | Reliability | Impersonation transparency email suppressed by 5-minute notification debounce | impersonate/route.ts:195-204 notifyUserOfAdminChange impersonationStarted; user-notify.ts:92-126 skips email (keeps in-app) when any ACCOUNT_UPDATED_BY_ADMIN within EMAIL_DEBOUNCE_MS (5 min). | If admin edited user within prior 5 min then impersonates, user receives no impersonation email, weakening transparency. | Exempt impersonationStarted from debounce, or send dedicated always-on impersonation email. | apps/admin/src/lib/user-notify.ts; apps/admin/src/app/api/users/[id]/impersonate/route.ts |  |

| admin-impersonation-07 | Admin Impersonation | Security | Operator reason stored/sent unredacted to user but only length-logged in audit | impersonate/route.ts:200 interpolates raw reason into user-facing notification; user-notify.ts:119-123 persists full changes (incl reason) in Notification.metadata; audit row stores only reasonLength. | Free-text with internal/case context exposed to user and stored unredacted, yet audit trail omits it. | Adopt one policy: redact/cap reason on user-facing surface and/or include redacted reason in audit. | apps/admin/src/app/api/users/[id]/impersonate/route.ts; apps/admin/src/lib/user-notify.ts |  |

| admin-impersonation-08 | Admin Impersonation | Data | AdminAuditLog.action length cap mismatch (web helper slices to 20, schema is 64) | impersonation-audit.ts:38 action.slice(0,20); AdminAuditLog.action @db.VarChar(64); admin writer slices to 64. | No current bug (20<64) but inconsistent; future action label >20 chars via this helper silently truncated. | Align slice to 64 to match schema and admin writer. | apps/web/src/lib/impersonation-audit.ts; packages/db/prisma/schema.prisma |  |

| admin-impersonation-09 | Admin Impersonation | Reliability | Admin impersonate route is non-idempotent | impersonate/route.ts:136-149 calls internal mint on every request with no dedupe; internal route creates new UserLoginSession each time. | Multiple short-lived impersonation sessions per (admin,user); all expire within 15 min. | Optionally dedupe/expire prior active impersonation sessions for same (admin,user) before minting. | apps/admin/src/app/api/users/[id]/impersonate/route.ts; apps/web/src/app/api/internal/impersonate/route.ts |  |

| analytics-flags-runtime-05 | Analytics, Feature Flags & Runtime Config | Performance | Mobile tracking consent runs a DataConsent query on every event/batch (hot path) | tracking-consent.ts:32 hasCurrentAnalyticsConsent -> prisma.dataConsent.findFirst per request for mobile; web uses cookie. | Extra DB round-trip on high-frequency tracking ingestion for mobile. | Cache mobile analytics-consent per session/short TTL. | apps/web/src/lib/tracking-consent.ts:32 |  |

| analytics-flags-runtime-06 | Analytics, Feature Flags & Runtime Config | Reliability | Unload flush uses plain fetch (no keepalive/sendBeacon) and can drop the final batch | analytics.ts:238-276 flushEvents uses fetch() no keepalive; bound to visibilitychange/beforeunload. | Last queued analytics batch may be cancelled by browser on tab close. | Use navigator.sendBeacon or fetch keepalive:true for unload flushes. | apps/web/src/lib/analytics.ts:238-276 |  |

| analytics-flags-runtime-07 | Analytics, Feature Flags & Runtime Config | Reliability | Event batch silently truncated to 50; client re-queue capped at 100 | tracking/event/route.ts:139 events.slice(0,50); analytics.ts:260 only re-queues failed batch if queue<100. | Under burst/sustained failure, events beyond caps dropped without server ack. | Surface/observe drops; consider server-acked delivery or larger bounded buffering. | apps/web/src/app/api/tracking/event/route.ts:139; apps/web/src/lib/analytics.ts:260 |  |

| analytics-flags-runtime-08 | Analytics, Feature Flags & Runtime Config | Logic | Experiment bucketing uses a weak, slightly biased non-crypto hash | feature-flags.ts:67-74 folds 32-bit hash then Math.abs(hash%100). | Variant split approximate (not perfectly uniform); fine for low-stakes UX, risky if billing-affecting. | Document as approximate/non-crypto; restrict to low-stakes experiments. | apps/web/src/lib/feature-flags.ts:67-74 |  |

| analytics-flags-runtime-09 | Analytics, Feature Flags & Runtime Config | Dead Code | getAllFlags() is unused and ignores flag targeting | feature-flags.ts:99 returns {name:f.enabled} ignoring targetType/targetValue; no production caller. | Misleading helper if adopted later (reports raw enabled not effective state). | Confirm and remove, or document debug-only semantics. | apps/web/src/lib/feature-flags.ts:99 |  |

| analytics-flags-runtime-10 | Analytics, Feature Flags & Runtime Config | API | Admin flag GET (ADMIN role) returns raw targetValue/userIds while runtime-config requires SUPER_ADMIN and masks | admin feature-flags/route.ts:9-13 returns flags unmodified incl targetValue (userIds = internal app DB IDs) to ADMIN; runtime-config GET requires SUPER_ADMIN and masks. | Inconsistent privilege floor and disclosure discipline between two control planes; low real risk. | Confirm ADMIN intended floor; consider masking/SUPER_ADMIN parity. | apps/admin/src/app/api/feature-flags/route.ts:9-13 |  |

| analytics-flags-runtime-11 | Analytics, Feature Flags & Runtime Config | Test | No unit tests for feature-flags.ts, user-event-sampling.ts, or the event-route sanitizer | Tests exist for retention/telemetry/readiness/runtime-config/phase1/ux-experiments but none for feature-flags or user-event-sampling; feature-flags only mocked. | Core gating (fail-closed, determinism, cache TTL) and sampling unverified by direct tests. | Add unit tests for feature-flags determinism/fail-closed/cache, sampling, route sanitization. | apps/web/src/lib/feature-flags.ts; apps/web/src/lib/user-event-sampling.ts; apps/web/src/app/api/tracking/event/route.ts |  |

| app-bootstrap-config-02 | App Bootstrap / Config / Env | Reliability | Admin app has no startup env-readiness warn-log | apps/admin/instrumentation.ts only loads Sentry; no apps/admin/src/instrumentation.ts; web runs buildEnvReadinessWarnings() at boot. | Misconfigured admin container emits no loud boot log; only signal is /api/ready 503 an operator must probe. | Add apps/admin/src/instrumentation.ts running buildEnvReadinessWarnings() for admin keys. | apps/admin/instrumentation.ts; apps/web/src/instrumentation.ts:14-35 |  |

| app-bootstrap-config-03 | App Bootstrap / Config / Env | Architecture | Documented BUILD_PHASE strict-secret startup gate does not exist in code | docker Dockerfiles claim runtime validator throws on first request when BUILD_PHASE unset; grep BUILD_PHASE across **/*.ts zero matches; real protection per-operation (encryption.ts:38-46, user-jwt-secret.ts:5-10) + /api/ready 503. | False sense of fail-fast boot gate; deploy missing STRIPE_WEBHOOK_SECRET/RESEND_API_KEY boots and serves, failing only when path executes. | Implement real register()-time required-secret throw on BUILD_PHASE, or correct Dockerfile comments; add test. | docker/web.prod.Dockerfile:40-47,117-119; docker/admin.prod.Dockerfile:36-42,105-107; apps/web/src/instrumentation.ts |  |

| app-bootstrap-config-04 | App Bootstrap / Config / Env | Security | Public /api/build-info discloses commit SHA, branch, environment unauthenticated | web and admin /api/build-info GET return readBuildInfo (commitSha, sourceBranch, builtAt, environment) no auth; both list as public. | Attacker fingerprints exact deployed commit/branch to target known-vuln versions or map internal naming. | Gate behind admin/internal auth, or reduce to short build id without branch. | apps/web/src/app/api/build-info/route.ts; apps/admin/src/app/api/build-info/route.ts |  |

| app-bootstrap-config-05 | App Bootstrap / Config / Env | Security | Web /api/ready exposes failing config key names publicly | apps/web ready/route.ts:54-82 returns failures:[{key,message}] no auth; admin /api/ready returns only missingRequiredCount. | Enumerates which secrets/keys are mis-set on a degraded deploy (recon aid). | Match admin count-only public shape; move key-level detail behind auth. | apps/web/src/app/api/ready/route.ts:54-82; apps/web/src/lib/production-readiness.ts:555-562 |  |

| app-bootstrap-config-06 | App Bootstrap / Config / Env | Logic | Three divergent production-like detectors (inconsistent preview handling) | env-catalog.ts:1465-1473 treats production/staging/preview+NODE_ENV+DIGITALOCEAN as prod-like; production-readiness.ts uses production/staging only; admin ready uses production/staging/preview. | Preview env prod-like to catalog but only warn-level to production-readiness; inconsistent strictness. | Extract single shared isProductionLike(env) helper used in all three. | packages/shared/src/env-catalog.ts:1465-1473; apps/web/src/lib/production-readiness.ts:26,109-115; apps/admin/src/app/api/ready/route.ts:9-12 |  |

| app-bootstrap-config-07 | App Bootstrap / Config / Env | Dead Code | Dead initSentry wrapper performs a second, non-PII-scrubbing Sentry.init | sentry.ts:17-40 initSentry() bare Sentry.init lacking beforeSend scrubber; referenced only in test mocks, never called in app code. | If wired in, would create competing init bypassing PII scrubber; currently dead with latent footgun. | Remove initSentry() and setup-wizard doc comment; keep captureException/captureMessage. | apps/web/src/lib/sentry.ts:17-40; apps/web/src/lib/sentry-options.ts |  |

| app-bootstrap-config-08 | App Bootstrap / Config / Env | Logic | env-catalog FIELD_ENCRYPTION_KEY description is stale (claims silent plaintext write) | env-catalog.ts:114 says FIELD_ENCRYPTION_KEY Missing silently writes un-encrypted PII; encryption.ts:38-46 throws ENCRYPTION_KEY_MISSING in production. | Misleading operator-facing documentation about a critical secret failure mode. | Update catalog description to reflect throw-in-production behavior. | packages/shared/src/env-catalog.ts:114; packages/shared/src/encryption.ts:38-46 |  |

| auth-session-03 | Authentication & Session | Security | Apple OAuth state/nonce cookies SameSite=None and callback CSRF-exempt; rests on DB-state consume | apple/route.ts sets state/nonce cookies sameSite none+secure; middleware exempts apple/callback from CSRF; forgery protection relies on single-use OAuthState consume + nonce match. | Risk concentrates on correctness of OAuthState consume; a bug there removes primary anti-forgery control. | Keep DB-state consume authoritative; confirm unique index on OAuthState.stateHash; retain consume tests. | apps/web/src/app/api/auth/oauth/apple/route.ts; apps/web/src/app/api/auth/oauth/apple/callback/route.ts |  |

| auth-session-04 | Authentication & Session | Security | impersonate-handoff is a public POST with no rate limit and no admin-liveness re-check | impersonate-handoff in PUBLIC_API_EXACT; verifies admin JWT and finds active impersonation session by tokenHash but no per-route rate limit, no re-verify admin rights at exchange. | High-value token redemption not throttled; revoking admin between mint and exchange does not block exchange. Mitigated by single-use row + <=15min TTL. | Add coarse rate limit; optionally re-check AdminUser active/permission at exchange. | apps/web/src/app/api/auth/impersonate-handoff/route.ts; apps/web/src/middleware.ts |  |

| auth-session-07 | Authentication & Session | API | Password change endpoint has no dedicated rate-limit policy | auth/password/change PATCH calls requireDbUserId/verifyPassword/hashPassword but no enforceRateLimitPolicy; only generic user_write (120/min). | Current-password guessing against hijacked session throttled only by coarse write limiter. | Add per-user mfa_verify-class policy to password-change route. | apps/web/src/app/api/auth/password/change/route.ts; apps/web/src/lib/rate-limit-policy.ts |  |

| auth-session-08 | Authentication & Session | Dead Code | Unused unverified-JWT helper decodeJwtPayload (and possibly legacy token reader) is dead code | oauth.ts:352-361 decodeJwtPayload parses JWT body without verification, grep-unreferenced; user-auth.ts:447-467 legacy token reader appears unused. | Latent foot-gun: future caller could trust unverified claims. | Remove decodeJwtPayload or rename to make non-verification explicit; confirm/remove legacy reader. | apps/web/src/lib/oauth.ts; apps/web/src/lib/user-auth.ts |  |

| auth-session-09 | Authentication & Session | UI/UX | Web sign-in MFA challenge has no backup-code field though API supports it | sign-in/page.tsx:108-110 only sends mfaCode; password-login.ts accepts backupCode; web form exposes no backup-code input. | User who lost authenticator cannot use backup code from web sign-in, creating recovery friction. | Add backup-code input toggle to MFA step on web sign-in. | apps/web/src/app/sign-in/page.tsx; apps/web/src/lib/password-login.ts |  |

| auth-session-10 | Authentication & Session | Test | Missing tests for Google OAuth callback and step-up brute-force | oauth/google/callback has no route.test.ts (apple/callback does); no test exercises step-up TOTP/backup lockout. | Critical login path and 2FA step-up gate can regress undetected. | Add Google callback tests (state mismatch, replay, id_token verify, link/create, handoff) and step-up brute-force tests. | apps/web/src/app/api/auth/oauth/google/callback/route.ts; apps/web/src/lib/user-step-up.ts |  |

| auth-session-05 | Authentication & Session | Security | Login/exchange responses expose account flags (post-auth only) | password-login.ts:430-446 and mobile/auth/exchange return mfaEnabled/hasPasswordLogin/needsPasswordSetup only after successful auth; pre-auth paths generic and timing-equalized. | Negligible: disclosure to already-authenticated user; not a pre-auth enumeration oracle. | No change required. | apps/web/src/lib/password-login.ts; apps/web/src/app/api/mobile/auth/exchange/route.ts |  |

| authorization-workspaces-02 | Authorization & Workspaces | Security | member.changeRole / promote-to-ADMIN lacks step-up auth (delete/transfer have it) | members/[memberId] PATCH/DELETE do role/status/removal with only can() checks, no requireWorkspaceStepUp; transfer/delete/restore do call it. | Hijacked OWNER/ADMIN session can promote controlled account to ADMIN or suspend/remove members without re-auth. | Add member_promote_admin (and member_remove) operation to step-up set; enforce on promote-to-ADMIN. | apps/web/src/app/api/workspaces/[id]/members/[memberId]/route.ts; apps/web/src/lib/workspace-step-up.ts |  |

| authorization-workspaces-03 | Authorization & Workspaces | Security | lf_workspace_id selection cookie is not httpOnly/secure | workspace-context.ts:29-33 WORKSPACE_COOKIE_OPTIONS omits httpOnly/secure; same at workspaces/route.ts:160 and invite accept routes. | Selector readable/writable by client JS; not an authz token (membership re-validated server-side), so no cross-tenant access; weaker defense-in-depth. | Mark httpOnly+secure; centralize cookie options in one helper. | apps/web/src/lib/workspace-context.ts:29-33; apps/web/src/app/api/workspaces/route.ts:160 |  |

| authorization-workspaces-04 | Authorization & Workspaces | Security | Plaintext invite token returned as devInviteUrl when NODE_ENV is not production | workspaces/[id]/invitations/route.ts:193 returns devInviteUrl with raw wsi_ token when NODE_ENV!==production && !emailSent. | Staging/preview build with NODE_ENV not production surfaces working accept links in API responses/logs. | Gate dev link on explicit ALLOW_DEV_INVITE_LINK flag default off; never log token. | apps/web/src/app/api/workspaces/[id]/invitations/route.ts:193 |  |

| authorization-workspaces-05 | Authorization & Workspaces | Architecture | Tenant isolation depends on manual per-query scope discipline, no DB/middleware backstop | Isolation relies on every author calling scopedRecordWhere/assertScopedRecordAction; no Prisma middleware/$extends/RLS on scoped models. | Single forgotten scope assertion in a future route = cross-tenant leak; current routes clean but non-structural. | Add Prisma $extends/middleware requiring explicit scope token, or lint rule. | apps/web/src/lib/workspace-data-scope.ts; all scoped routes under apps/web/src/app/api |  |

| authorization-workspaces-06 | Authorization & Workspaces | Reliability | ensureWorkspaceDefaults swallows all errors; a slipped user hits 403 on scoped routes | workspace-provisioning.ts:26-44 try/catch swallows everything; user whose membership creation failed gets NO_WORKSPACE_ACCESS 403 once flag on. | New signups that slip provisioning locked out of workspace features until idempotent backfill re-runs, no telemetry. | Add failure telemetry and lazy self-repair re-attempt on NO_WORKSPACE_ACCESS. | apps/web/src/lib/workspace-provisioning.ts:26-44 |  |

| authorization-workspaces-07 | Authorization & Workspaces | Dead Code | Stale admin ?workspace= override comment references code not present in scope | workspace-context.ts:115-118 comment says ?workspace= override admin-only handled separately; grep found no implementation. | Harmless stale doc, or pointer to out-of-scope admin path that could bypass membership if not admin-gated. | Remove comment or link to real handler; if exists, audit strict admin-only enforcement. | apps/web/src/lib/workspace-context.ts:115-118 |  |

| authorization-workspaces-08 | Authorization & Workspaces | Performance | N+1 queries in GET /api/workspaces | workspaces/route.ts:37-96 planSummaryForOwner + workspaceMember.count per membership, then four more counts per personal-solo candidate. | Several sequential queries per request; bounded by per-user workspace count. | Batch owner-subscription and member-count lookups. | apps/web/src/app/api/workspaces/route.ts:37-96 |  |

| authorization-workspaces-09 | Authorization & Workspaces | Test | No IDOR / role-matrix / seat-concurrency / redaction test coverage for the authz core | Only workspace-context-resolver.test.ts covers this lib; no cross-workspace 404, can() matrix, last-seat concurrency, or viewSensitive redaction tests. | Regressions in the most security-critical module can land uncaught. | Add IDOR matrix, can() golden table, seat-concurrency, CHILD self-only, redaction tests. | apps/web/src/lib; apps/web/src/app/api (scoped routes) |  |

| authorization-workspaces-10 | Authorization & Workspaces | Dead Code | workspace-context.ts header claims tested, unused but it is widely used | workspace-context.ts:1-12 header says resolver tested, unused; grep shows 31 files reference it and 30+ routes call resolveWorkspaceDataScope. | Misleading maintainer signal that hot-path code is inert. | Update file header to reflect resolver is live. | apps/web/src/lib/workspace-context.ts:1-12 |  |

| budget-expenses-04 | Budget & Expenses | Data | Tax/property report builder is userId-only (no workspace scope) | tax-report-data.ts:64-100 filters all three queries by {userId,deletedAt:null}; export routes gate on individual plan not workspace. | Not cross-tenant leak, but in workspace mode a Pro member tax export can omit workspace-owned records under different userId. | Decide intended scope; if workspace-inclusive, thread resolved scope through buildTaxReportData and export gates. | apps/web/src/lib/tax-report-data.ts, apps/web/src/app/api/export/route.ts |  |

| budget-expenses-05 | Budget & Expenses | Logic | Negative/non-finite stored monthlyCost flows into projection unguarded | budget-planning.ts:180-201 normalizeServiceCost only checks amount>0 for hasCost; negative stored monthlyCost excluded but monthlyAmountForCycle/sums not clamped; actuals path guards <0, projection relies on services module. | If negative monthlyCost persisted, projected/committed totals could skew; defensive depth missing in shared engine. | Add defensive non-finite/negative guard in engine; validate monthlyCost bounds at service write path. | packages/shared/src/budget-planning.ts |  |

| budget-expenses-07 | Budget & Expenses | Logic | Client currentBudget match ignores scopeKey/workspace | budget/page.tsx:345-353 matches on budgetMonthKey + bare addressId; server keys monthlyBudgetLimit on scopeKey (workspace:<id>:global in workspace mode). | Workspace member with legacy personal budget same month+address may match wrong row client-side, inconsistent limit/notes. | Match on scopeKey parity (include workspace context) when selecting currentBudget. | apps/web/src/app/(app)/budget/page.tsx |  |

| budget-expenses-08 | Budget & Expenses | UI/UX | Over/Under cell shows $0.00 when no budget limit is set | budget/page.tsx:829 renders formatCurrency(0) when budgetDelta is null in Budget vs Committed split. | Reads as exactly on budget rather than no limit set. | Render em-dash or Not set when budgetDelta is null. | apps/web/src/app/(app)/budget/page.tsx |  |

| budget-expenses-11 | Budget & Expenses | Accessibility | Progress bars lack progressbar ARIA semantics | budget/page.tsx (616-620,791-793,814-815) render width-styled div bars with no role=progressbar / aria-valuenow. | Screen-reader users receive no value for budget usage / category spend bars. | Add role=progressbar with aria-valuenow/min/max or visually-hidden text. | apps/web/src/app/(app)/budget/page.tsx |  |

| budget-expenses-13 | Budget & Expenses | Reliability | Actuals fetch swallows errors and renders zero actuals | budget/page.tsx:323 .catch(()=>setMonthActuals({})); mobile budget/index.tsx:254 sets {} on any error. | Transient 500 on /api/budget/actuals makes month look fully reconciled-to-zero, masking failures and misleading savings. | Surface non-blocking error/retry instead of treating failure as no-actuals. | apps/web/src/app/(app)/budget/page.tsx, apps/mobile/app/budget/index.tsx |  |

| budget-expenses-15 | Budget & Expenses | Performance | Full 3-endpoint reload after every actual save | budget/page.tsx:415 loadBudgetData() after each persistServiceActual despite optimistic local update; mobile mirrors with fetchAll(). | Logging N actuals triggers N full budget+addresses+services reloads. | Rely on optimistic local update; refresh History lazily or debounce reload. | apps/web/src/app/(app)/budget/page.tsx, apps/mobile/app/budget/index.tsx |  |

| budget-expenses-17 | Budget & Expenses | Reliability | Actuals write (log + scalar mirror + snapshot refresh) is not transactional | budget/actuals/route.ts:162-183 serviceCostLog.upsert then service.update then refreshBudgetSnapshotsForServiceMonth as separate awaits. | Failure between log upsert and scalar mirror leaves legacy actualMonthlyCost stale; snapshot-refresh failure 500s whole POST. | Wrap log upsert + scalar mirror (+ snapshot refresh) in $transaction. | apps/web/src/app/api/budget/actuals/route.ts |  |

| component-theme-system-07 | Component & Theme System | Accessibility | Hardcoded English strings in theme/dialog controls bypass i18n | Admin theme-toggle.tsx:26-44, LandingThemeToggle:14-23, Dialog Close aria-label:108, ConfirmDialog defaults:20-24 all literal English while apps use next-intl. | Non-English users see English controls in parts of the UI. | Route these strings through next-intl like web header ThemeToggle. | apps/admin/src/components/theme-toggle.tsx:26-44; apps/web/src/components/marketing/landing-theme-toggle.tsx:14-23; apps/web/src/components/ui/dialog.tsx:108 |  |

| component-theme-system-08 | Component & Theme System | Accessibility | StatusBadge misuses role=status (live region) on static badges | ui/status-badge.tsx:86-104 renders static span with role=status plus aria-label duplicating visible label; SR re-announce on every render. | Verbose/incorrect SR announcements for non-live status chips. | Drop role=status for static usage; reserve live regions for dynamic updates. | apps/web/src/components/ui/status-badge.tsx:86-104 |  |

| component-theme-system-09 | Component & Theme System | Theme | Single static theme-color and missing web colorScheme viewport | layout.tsx:180 sets one theme-color #0A0F18 with no light media variant; web viewport (149-152) omits colorScheme while admin sets colorScheme:dark light. | In light mode mobile browser chrome stays dark navy; UA controls/scrollbars may not match. | Add light-mode theme-color media variant and colorScheme:dark light to web viewport. | apps/web/src/app/layout.tsx:149-152,180; apps/admin/src/app/layout.tsx:69 |  |

| component-theme-system-10 | Component & Theme System | Logic | Mobile theme default contradicts its no-flash docstring | theme.ts:348-359 comment claims first render returns system+OS scheme; initial preference hardcoded dark (365) with hydrated=false; fresh install on light device renders dark first. | Light-device users see dark flash/wrong default; misleading comment. | Align comment with dark default, or default to system to honor device as documented. | apps/mobile/src/lib/theme.ts:348-367 |  |

| component-theme-system-11 | Component & Theme System | Data | Mobile rose token resolves to coral/danger, diverging from web brand rose | apps/mobile/tailwind.config.ts:43-48 rose DEFAULT #E25C5C (coral); theme.ts maps rose->danger tone; web brand rose is Gold/Sapphire. | Risk of mis-coloring when porting components; rose ambiguous cross-platform. | Document intentional divergence; consider renaming mobile alias. | apps/mobile/tailwind.config.ts:43-48; apps/mobile/src/lib/theme.ts:110,197 |  |

| component-theme-system-12 | Component & Theme System | Test | Contrast test covers light mode only; provider/aurora tests are string-match | design-tokens-contrast.test.ts only blends light tones; no dark-mode coverage, no text-white-on-solid-fill check; theme-provider/aurora tests assert literal strings not behavior. | Dark-mode contrast regressions (default theme) ship untested; findings 01/02 not caught. | Add dark-mode contrast tests, token-parity test across three sources, primitive a11y tests. | apps/web/src/lib/design-tokens-contrast.test.ts; apps/web/src/components/theme-provider.test.tsx |  |

| dashboard-web-app-03 | Consumer Dashboard / Account (web app) | Data | Data export filtered by raw userId, ignoring workspace scope (incomplete export) | Every query in export/route.ts uses where:{userId,deletedAt:null}; export/pdf likewise; neither uses resolveWorkspaceDataScope unlike api/profile. | In workspace mode export under-shares; incomplete GDPR Art.15 export. No cross-tenant leak (more restrictive). | Decide intended scope and apply workspace-data-scope helpers (childSelfOnly for CHILD). | apps/web/src/app/api/export/route.ts; apps/web/src/app/api/export/pdf/route.ts |  |

| dashboard-web-app-07 | Consumer Dashboard / Account (web app) | Accessibility | Support detail dates hard-coded en-US; settings delete label not linked to input | support/[id]/page.tsx formats dates with toLocaleDateString(en-US) while list localizes; settings/page.tsx Type DELETE label not associated via htmlFor/id. | Minor i18n inconsistency for Spanish users and small screen-reader label-association gap. | Use active locale for support dates; add matching htmlFor/id. | apps/web/src/app/(app)/support/[id]/page.tsx; apps/web/src/app/(app)/settings/page.tsx |  |

| dashboard-web-app-09 | Consumer Dashboard / Account (web app) | Reliability | Preference/widget persistence is fire-and-forget (silent failure) | dashboard-client.tsx persistPrefs posts to /api/user/preferences with trailing .catch(()=>{}) and no feedback. | Failed save of dashboard layout/toggles silently lost; user believes preference saved. | Surface subtle failure toast or retry on persistence error. | apps/web/src/app/(app)/dashboard/dashboard-client.tsx; apps/web/src/app/api/user/preferences/route.ts |  |

| dashboard-web-app-10 | Consumer Dashboard / Account (web app) | Dead Code | recordUserSecurityAudit may be unused by in-scope routes | lib/user-security-audit.ts exports recordUserSecurityAudit but audited routes call createAuditLog directly; no in-scope caller observed. | Possible dead abstraction adding indirection. | Confirm callers app-wide; remove if genuinely unused. | apps/web/src/lib/user-security-audit.ts |  |

| database-schema-02 | Database Schema & Data Layer | Security | Tenant isolation is application-only (no DB-level row scoping; workspaceId nullable) | workspaceId/userId nullable/plain on Address/Service/MovingPlan/Budget; isolation only via scopedRecordWhere/recordBelongsToScope/assertScopedRecordAction; no RLS, no composite unique tying child to tenant; workspaceId NULLABLE (Phase 1). | A route building a Prisma where without scope helper can read/mutate another tenant rows (cross-tenant IDOR / leak). | Promote workspaceId NOT NULL after backfill; funnel all scoped reads/writes through helpers; lint/test failing when scoped model queried without tenant predicate; cross-tenant integration tests. | apps/web/src/lib/workspace-data-scope.ts; packages/db/prisma/schema.prisma | uncertain (sev High→Low) |

| database-schema-05 | Database Schema & Data Layer | Data | Enum-like fields stored as plain VarChar without DB constraint | status/role/category/platform fields String @db.VarChar with comment-documented allowed values but no DB enum/CHECK; only BlogStatus is a real Prisma enum. | Invalid enum values can be persisted; app and DB can drift; typos not rejected at DB. | Validate at app boundary with zod for each; consider DB enums/CHECK for stable sets. | packages/db/prisma/schema.prisma |  |

| database-schema-06 | Database Schema & Data Layer | Data | AuditLog.userId and GDPRRequest.userId are FK-less loose refs | AuditLog (1123-1144) and GDPRRequest (1702-1717) declare userId String with no relation to User. | Orphaned audit/GDPR rows possible; AuditLog orphaning intentional; GDPRRequest retains userId after erasure. | Keep AuditLog FK-less by design; verify GDPRRequest retention and residual PII scrubbed. | packages/db/prisma/schema.prisma:1123-1144,1702-1717 |  |

| database-schema-07 | Database Schema & Data Layer | Data | Dual source of truth: Service.actualMonthlyCost vs ServiceCostLog | schema.prisma:516-601 documents actualMonthlyCost as LEGACY scalar superseded by per-month ServiceCostLog but retained and possibly still mirroring current month. | Budget actuals can drift if any reader treats legacy scalar as source of truth. | Trace all budget-actuals read paths; if none use scalar as truth, plan removal; else reconcile. | packages/db/prisma/schema.prisma:516-601 |  |

| database-schema-08 | Database Schema & Data Layer | Security | Field encryption falls back to plaintext/log-raw outside production | encryption.ts:38-46,63-99 returns plaintext when FIELD_ENCRYPTION_KEY unset and NODE_ENV!==production, logs raw encrypted value on decrypt failure in dev. | Misconfigured internet-facing staging (NODE_ENV not production) could persist PII in plaintext. | Gate passthrough on explicit dev-only flag (ALLOW_PLAINTEXT_PII); startup assertion key present in non-local env. | packages/shared/src/encryption.ts |  |

| database-schema-09 | Database Schema & Data Layer | Reliability | db-schema-compat silently swallows missing-column errors | db-schema-compat.ts isMissingDbColumnError matches P2022; callers degrade to legacy path; warnSchemaCompatibilityFallback only console.warns once per scope per process. | Genuinely un-applied production migration degrades silently to legacy path, masking schema drift. | Emit metric/alert whenever fallback fires in production; treat as error-level. | apps/web/src/lib/db-schema-compat.ts |  |

| database-schema-10 | Database Schema & Data Layer | Performance | contract-reminders cron caps scan at take:1000 without cursor pagination | contract-reminders/route.ts:51 uses take:1000 on service scan with no cursor follow-up. | Very large eligible-service sets could be silently truncated, missing reminders. | Cursor-paginate the cron scan to guarantee full coverage. | apps/web/src/app/api/cron/contract-reminders/route.ts |  |

| database-schema-11 | Database Schema & Data Layer | Dead Code | Dead SQLite-to-MySQL migration tooling references removed models | _migrate-to-mysql.ts TABLES list includes ~13 models absent from current schema; accompanied by _migration-data.json (653 KB) and legacy-sqlite-migrations/. | Confusing dead one-shot tooling in active prisma directory. | Archive the SQLite-to-MySQL one-shot tooling out of active prisma dir. | packages/db/prisma/_migrate-to-mysql.ts; packages/db/prisma/_migration-data.json; packages/db/prisma/legacy-sqlite-migrations/ |  |

| dead-04 | Dead Code & Tech Debt | Architecture | Parallel/orphaned seed scripts alongside canonical seed-master | Canonical seed-master.ts imports modular seed-data/; standalone seed-providers*.ts with own datasets coexist; seed-providers-phase2.ts referenced only by comment; seed.ts/seed-state-rules.ts have no package.json script. | Two competing provider-seed sources risk divergence; superseded seeders obscure authoritative path. | Confirm seed-master.ts + seed-data/ single source then retire superseded seeders. | packages/db/prisma/seed-master.ts, packages/db/prisma/seed-providers-phase2.ts, packages/db/prisma/seed.ts, packages/db/prisma/seed-data/ |  |

| dead-05 | Dead Code & Tech Debt | Dead Code | packages/db/check-admin.mjs ad-hoc admin-dump debug script committed | check-admin.mjs one-liner Prisma-queries every adminUser and console.logs id/email/isActive; not referenced by any script; leading UTF-8 BOM. | Ad-hoc debug tooling in repo; running it dumps full admin-user table to stdout. | Remove or move to gitignored local-scripts; if kept, document as maintenance tool. | packages/db/check-admin.mjs |  |

| dead-06 | Dead Code & Tech Debt | Architecture | Deep-relative shared-*.ts re-export wrappers bypass the package alias | shared-encryption.ts re-exports from ../../../../packages/shared/src/encryption (and shared-billing/relocation/runtime-config/autocomplete, admin equivalents); actively used but brittle relative paths instead of @locateflow/shared. | Maintenance friction and fragile relative paths; two ways to import same shared symbols. | Standardize on @locateflow/shared subpath exports and collapse wrappers, or import via package alias. | apps/web/src/lib/shared-encryption.ts, apps/web/src/lib/shared-billing.ts, apps/admin/src/lib/shared-encryption.ts |  |

| dead-07 | Dead Code & Tech Debt | Architecture | Root-level prototype/scratch directories outside the workspace | Repo root contains NEW GENERATION/, design-src/, previews/; pnpm-workspace.yaml only globs apps/* and packages/*; no app/turbo/package.json references them. | Repo clutter with unclear ownership; risk of stale prototype mistaken for production assets. | Relocate to clearly-labeled /design area or external store, or gitignore/remove if superseded. | NEW GENERATION/, design-src/, previews/, pnpm-workspace.yaml |  |

| email-pipeline-05 | Email Pipeline | Security | RESEND_WEBHOOK_SECRET and EMAIL_REPLY_TO undocumented in .env.example (webhook fails closed) | webhooks/resend/route.ts:39-43 reads RESEND_WEBHOOK_SECRET returns 503 if unset; email.ts:104,128 reads EMAIL_REPLY_TO; neither in .env.example. | Operator never setting RESEND_WEBHOOK_SECRET gets permanently non-functional bounce/complaint pipeline silently. | Add both keys to .env.example and env-catalog/production-readiness. | apps/web/src/app/api/webhooks/resend/route.ts:39; apps/web/src/lib/email.ts:104; .env.example |  |

| email-pipeline-06 | Email Pipeline | Security | Resend webhook has no idempotency store; replay protected only by 300s timestamp tolerance | webhooks/resend/route.ts does not record svix-id to ProcessedWebhookEvent; replay protection only TIMESTAMP_TOLERANCE_SECONDS=300. | Captured validly-signed bounce/complaint event replayable within 5 min; op idempotent so limited to duplicate writes. | Persist svix-id to ProcessedWebhookEvent and short-circuit duplicates. | apps/web/src/app/api/webhooks/resend/route.ts; apps/web/src/lib/resend-webhook.ts |  |

| email-pipeline-07 | Email Pipeline | Security | Bespoke non-DOM HTML sanitizer feeds an un-sandboxed admin preview iframe | email-html-sanitizer.ts admits not covering all edge cases (CDATA, mismatched tags); admin renders stored body via iframe srcDoc with no sandbox attr. | Sanitizer bypass would be stored-XSS against operator browser in preview iframe; gated by ADMIN+password+MFA+audit. | Add sandbox attribute (no allow-scripts) to preview iframe; consider vetted sanitizer shared across web/admin. | packages/shared/src/email-html-sanitizer.ts; apps/admin/src/app/(admin)/email-templates/email-templates-client.tsx:169 |  |

| email-pipeline-08 | Email Pipeline | Reliability | BOUNCED status write is best-effort and only matches by providerMessageId | webhooks/resend/route.ts:112-121 updateMany where providerMessageId=email_id, best-effort try/catch. | Sends whose providerMessageId never persisted or dev no-key path never marked BOUNCED, undercounting admin metric. | Add fallback match by recent EmailLog to recipient when providerMessageId lookup empty. | apps/web/src/app/api/webhooks/resend/route.ts:112-121 |  |

| email-pipeline-09 | Email Pipeline | Dead Code | Email HTML sanitizer duplicated verbatim in shared and admin packages | packages/shared/src/email-html-sanitizer.ts and apps/admin/src/lib/email-template-sanitizer.ts byte-for-byte identical; admin imports local copy, web imports shared. | Drift risk: a security fix to one copy does not reach the other. | Have admin import from shared package and delete duplicate. | apps/admin/src/lib/email-template-sanitizer.ts; packages/shared/src/email-html-sanitizer.ts |  |

| email-pipeline-10 | Email Pipeline | UI/UX | Unsubscribe API errors return raw plain text instead of the styled page | api/unsubscribe/route.ts:66,76 return NextResponse plain text; confirm-form path does not redirect to styled invalid/not-found states. | User clicking stale link and submitting confirm sees unbranded browser error page with no path to manage preferences. | On confirm-form path 303 back to /unsubscribe with error flag; render styled states. | apps/web/src/app/api/unsubscribe/route.ts:66,76; apps/web/src/app/unsubscribe/page.tsx |  |

| email-pipeline-11 | Email Pipeline | Performance | Per-recipient sends repeat app-URL and template DB reads with no per-run caching | Reminder/digest senders call resolveAppUrl and renderTemplate (emailTemplate.findUnique + localized lookup) per recipient; resolveLocalizedSlug adds extra findUnique for non-English. | On high-volume crons several extra DB round-trips per recipient with no batching/caching. | Cache resolveAppUrl and active template rows per cron invocation; pass into senders. | apps/web/src/lib/email-service.ts |  |

| email-pipeline-12 | Email Pipeline | Security | Webhook suppresses recipient from payload to without cross-checking EmailLog | webhooks/resend/route.ts:88-105 + resend-webhook.ts:104-111 read event.data.email/to and suppress; no check the address was one we sent to; gated by Svix HMAC. | No user-facing abuse path: only holder of RESEND_WEBHOOK_SECRET can craft suppression. | Optionally cross-check recipient against recent EmailLog row before suppressing. | apps/web/src/app/api/webhooks/resend/route.ts:88-105; apps/web/src/lib/resend-webhook.ts:104-111 |  |

| external-data-integrations-02 | External Data Integrations | Security | SSRF surface is defensively closed (validated coords, encoded params, NWS allowlist, FCC origin-pin) | Coords validated via Number.isFinite; ArcGIS uses URLSearchParams; epa-water encodeURIComponent; NWS step-2 forecast allowlisted to .weather.gov; FCC base URL-parsed/protocol-checked/origin-pinned. | No reachable SSRF today; flagged to lock invariant against future regressions. | Add SSRF negative tests for NWS allowlist and FCC base normalizer; document invariant. | apps/web/src/lib/nws-weather.ts:158-169; apps/web/src/lib/fcc-isp.ts:360-371; apps/web/src/lib/epa-water.ts:267 |  |

| external-data-integrations-03 | External Data Integrations | Security | API keys passed in query strings for four libs (currently never logged) | airnow.ts:202, electric-utility.ts:384, nlr-alt-fuel-stations.ts:268, census-acs.ts:392 pass key= in URL; fetchJson throws status-only errors, never includes URL. | Keys-in-URL can leak via upstream access logs/proxies, or future code logging full URL/error object. | Prefer header auth; add log-scrub rule for key params; lint/test asserting libs never log key-bearing URL. | apps/web/src/lib/airnow.ts:202; apps/web/src/lib/electric-utility.ts:384; apps/web/src/lib/nlr-alt-fuel-stations.ts:268; apps/web/src/lib/census-acs.ts:392 |  |

| external-data-integrations-05 | External Data Integrations | Performance | FCC/electric/NHTSA caches are per-process only; multi-instance deployments amplify upstream calls | Per-process Map LRUs in fcc-isp.ts, electric-utility.ts, nhtsa.ts; dossier libs get durable cross-instance section cache but serviceability and NHTSA do not. | Across N instances up to N upstream calls per key before caches warm; cost/rate-limit amplification for FCC/OpenEI quotas. | Optionally layer shared/durable cache behind same signatures for FCC/electric/NHTSA. | apps/web/src/lib/fcc-isp.ts:128-159; apps/web/src/lib/electric-utility.ts:98-131; apps/web/src/lib/nhtsa.ts:110-147 |  |

| external-data-integrations-06 | External Data Integrations | Reliability | Integration outcomes are counted but not alerted; a fully-broken source degrades silently | integration-telemetry.ts records per-source daily statusCounts into IntegrationDailyStat for admin panel, but no threshold alert. | Slow detection of upstream breakage; users see absent sections with no operator signal. | Add admin/cron alert when source daily error ratio crosses threshold. | apps/web/src/lib/integration-telemetry.ts |  |

| external-data-integrations-07 | External Data Integrations | Test | community-popularity.ts has no unit test (privacy threshold, scaling, cache, failure path) | No community-popularity.test.ts; lib enforces MIN_DISTINCT_USERS=5 privacy floor, 0-20 scaling, Redis hit/miss, failure->undefined. | Regressions in privacy floor or scaling could ship unnoticed. | Add unit test covering threshold, scaling, Redis hit/miss, failure->undefined. | apps/web/src/lib/community-popularity.ts |  |

| external-data-integrations-04 | External Data Integrations | Security | Map static proxy is a hardened reference implementation (positive control) | maps/static/route.ts: auth 401, per-user 60/min + per-IP 120/min, strict coord parse+range, realMap plan gate, 4s timeout, never forwards upstream body, key only in upstream URL, private no-CDN cache headers. | Positive control; demonstrates intended secure-proxy pattern. | None; reuse as template for future external proxy routes. | apps/web/src/app/api/maps/static/route.ts |  |

| external-data-integrations-08 | External Data Integrations | Architecture | fetchJson + LRU boilerplate duplicated across ~14 libs | Near-identical timed fetchJson and Map TTL cache in every lookup lib with per-source tuning. | Maintenance overhead and copied error-handling; intentional isolation aids per-source tuning. | Optional: extract shared timedFetchJson + createTtlCache; low priority. | apps/web/src/lib/airnow.ts; apps/web/src/lib/fema-flood.ts; apps/web/src/lib/nhtsa.ts |  |

| mobile-app-02 | Mobile App (Expo / React Native) | Security | Cleartext HTTP allowed for local proxy when EXPO_PUBLIC_ENV=development | api.ts:62-74 enforceProductionApiUrl returns URL unchanged when publicEnv===development and host 10.0.2.2/localhost/127.0.0.1; api.ts:55 derives http://expoHost:3000/api in __DEV__; eas.json production sets production env. | If non-dev artifact carried EXPO_PUBLIC_ENV=development, Bearer token+PII sent over cleartext HTTP to LAN host. | Gate cleartext exception on __DEV__ only; extend release-config.test.ts to assert no release profile uses cleartext/dev env. | apps/mobile/src/lib/api.ts, apps/mobile/src/lib/release-config.test.ts |  |

| mobile-app-03 | Mobile App (Expo / React Native) | Security | Custom-scheme OAuth callback interception (mitigated by PKCE) | OAuth redirect can land on locateflow://oauth?code; on Android hostile app registering same scheme could receive code; mitigated by PKCE verifier in SecureStore + server sha256 enforcement; https path autoVerify; codes single-use. | Redirect hijack on Android exploitable without PKCE; with PKCE attacker still needs verifier from legit app Keychain. | Confirm server REQUIRES code_verifier for mobile exchange and rejects verifier-less; prefer verified https universal link. | apps/mobile/src/lib/mobile-oauth.ts, apps/mobile/src/lib/pkce.ts |  |

| mobile-app-07 | Mobile App (Expo / React Native) | Reliability | Sentry environment mis-tagged + analytics locale hardcoded en | sentry.ts:84 environment=process.env.NODE_ENV not EAS profile; SessionTracker.tsx:38 hardcodes language:en despite i18n en/es. | Mis-bucketed crash data hampers triage; analytics under-reports non-English usage. | Source Sentry environment from expoConfig.extra.environment\|\|EXPO_PUBLIC_ENV; pass resolved i18n locale. | apps/mobile/src/lib/sentry.ts, apps/mobile/src/components/SessionTracker.tsx |  |

| mobile-app-08 | Mobile App (Expo / React Native) | Test | AuthGuard routing state machine and AppLockGate flows largely untested | auth-navigation.test.ts only asserts setup-password screen registered + comment substring; does not exercise _layout.tsx redirect logic nor AppLockGate background-grace/lock/unlock recovery. | Regressions in critical auth routing or app-lock gate could ship uncaught. | Integration tests with mocked expo-router/zustand asserting each redirect branch; cover AppLockGate and IAP verify-fail. | apps/mobile/app/_layout.tsx, apps/mobile/src/components/AppLockGate.tsx |  |

| mobile-app-09 | Mobile App (Expo / React Native) | UI/UX | Hardcoded English strings on otherwise-i18n screens | addresses/[id]/index.tsx:145,241,264 (Seasonal/Past/Active/Needs attention); subscription.tsx savings strings not routed through i18n. | Spanish-locale users see English labels in several places. | Move literal strings to i18n keys (en/es). | apps/mobile/app/addresses/[id]/index.tsx, apps/mobile/app/settings/subscription.tsx |  |

| mobile-app-04 | Mobile App (Expo / React Native) | Security | IAP entitlement granted only after server verification (positive control) | iap.ts purchase/restore/reconcile all POST store proof to /api/mobile/iap/verify and only finishTransaction+grant on res.data.success; subscription.tsx reads server entitlement. | Strong store-to-web entitlement parity; client cannot self-grant. | Keep server validation authoritative; test failed verify never finishTransaction. | apps/mobile/src/lib/iap.ts, apps/mobile/app/settings/subscription.tsx |  |

| mobile-app-10 | Mobile App (Expo / React Native) | Dead Code | Transitional fonts and vestigial post-auth-route helper | _layout.tsx:489-511 loads Fraunces/Geist/Geist-Mono during reskin transition alongside new faces; post-auth-route.ts ignores its user arg returning /onboarding. | Extra boot/bundle cost; confusing helper signature. | Remove unused transitional faces after verifying no references; simplify/document getPostAuthMobileRoute. | apps/mobile/app/_layout.tsx, apps/mobile/src/lib/post-auth-route.ts |  |

| mobile-iap-billing-06 | Mobile IAP Billing (Apple/Google) | Reliability | Apple App Store Server API calls have no request timeout | iap-apple.ts:374-392 bare fetch no AbortController, unlike Google fetchWithGoogleTimeout (10s). | Hung Apple endpoint stalls verify request/webhook, tying up serverless invocations. | Wrap Apple fetches in AbortController timeout. | apps/web/src/lib/iap-apple.ts:374-392 |  |

| mobile-iap-billing-07 | Mobile IAP Billing (Apple/Google) | Performance | Products endpoint unauthenticated, uncached, 12 config reads per hit | mobile/iap/products/route.ts:18 no auth, no rate limit, force-dynamic, 12 getRuntimeConfigValue reads per request. | Cheap unauthenticated amplifier against runtime-config store; no confidentiality issue. | Add short-lived caching/revalidate or basic rate limit. | apps/web/src/app/api/mobile/iap/products/route.ts |  |

| mobile-iap-billing-08 | Mobile IAP Billing (Apple/Google) | UI/UX | Cross-platform manage dead-end and conflated restore states | subscription.tsx:603-627,943-966 cross-platform store subs only informational Alert no deep-link; restore handler shows nothing to restore even when all items errored. | Confusing management UX; users cannot distinguish errored-restore from empty; cross-platform store subs dead end. | Surface store deep-link; distinguish all-errored from empty in restore results. | apps/mobile/app/settings/subscription.tsx:603-627,835-843,943-966 |  |

| mobile-iap-billing-09 | Mobile IAP Billing (Apple/Google) | Dead Code | Likely-unused export verifyAndLookupSignedTransaction | iap-apple.ts:434-444 exports verifyAndLookupSignedTransaction; verify route inlines verifyAppleJws + refreshAppleSubscriptionFor; no production caller. | Maintenance noise / confusion about verification entry point. | Confirm no caller and remove, or document retention. | apps/web/src/lib/iap-apple.ts:434-444 |  |

| mobile-iap-billing-10 | Mobile IAP Billing (Apple/Google) | Reliability | Cold-start reconciler call site unconfirmed | reconcilePendingPurchases (iap.ts:430-471) recovery path for charged-but-unverified iOS purchases; app-start invocation not located. | If not invoked on launch with session, charged-but-unverified iOS purchase only recoverable via manual Restore. | Confirm reconcilePendingPurchases called on app start when session exists. | apps/mobile/src/lib/iap.ts:430-471 |  |

| mobile-iap-purchase-04 | Mobile IAP Purchase flow | Reliability | Ownership pre-checks and upsert are not in a single DB transaction | applyIapStateToUser (iap-common.ts:583-685) does findUnique(originalTransactionId) + findSubscriptionByPurchaseTokenIdentifiers + findUnique(userId) then separate upsert, not in $transaction. | Explicit cross-account ownership guard advisory under concurrency; correctness relies on unique constraints (present, P2002 handled). | Wrap ownership check + upsert in interactive transaction, or document unique constraints are authoritative guard. | apps/web/src/lib/iap-common.ts:583,669 |  |

| mobile-iap-purchase-05 | Mobile IAP Purchase flow | Logic | Google linkedPurchaseToken not used to supersede predecessor subscription row | linkedPurchaseToken parsed into GoogleSubscriptionV2Response (iap-google.ts:225) but no consumer; normalizeGoogleResult/applyIapStateToUser key row on userId/purchaseToken without reconciling linked predecessor. | After in-store upgrade/downgrade/re-subscribe, stale Subscription row keyed to old token can linger until later webhook/refresh overwrites. | On Google verify, when linkedPurchaseToken present, look up and reconcile/cleanup predecessor token row. | apps/web/src/lib/iap-google.ts:225, apps/web/src/lib/iap-common.ts:453 |  |

| mobile-iap-purchase-06 | Mobile IAP Purchase flow | Reliability | Charged-but-timed-out purchase recovery depends on reconcile/restore | purchaseSubscription 120s timeout resolves IAP_PURCHASE_FAILED and tears down listener (iap.ts:202-204); recovery relies on reconcilePendingPurchases on cold start or manual Restore. | Edge timeout can leave charged user temporarily without entitlement until next reconcile or manual restore. Path exists but untested. | Add regression test that reconcilePendingPurchases finishes and grants a timed-out purchase; clearer retry/restore CTA in timeout branch. | apps/mobile/src/lib/iap.ts:202,430 |  |

| mobile-iap-purchase-07 | Mobile IAP Purchase flow | Architecture | iOS verify body overloads the field name purchaseToken for the JWS | getIosSignedTransaction reads purchase.purchaseToken FIRST then jwsRepresentation variants (iap.ts:133-141); buildVerifyBodyForPurchase maps into signedTransaction. | Field-name overloading correct but fragile: future expo-iap shape change putting non-JWS in purchaseToken on iOS would silently send wrong proof. | Prefer explicit jwsRepresentationIOS fields and keep purchaseToken last in iOS fallback; add JWS-shape sanity check. | apps/mobile/src/lib/iap.ts:133 |  |

| moving-tasks-05 | Moving & Move Tasks | Logic | Classifier due dates stored at server-local-noon vs UTC-midnight elsewhere | buildMoveTaskDueDate uses setHours(12,0,0,0) server-local; checklist due dates use moveDateMs+offset*DAY_MS; reminder cron treats dueDate as UTC-midnight. | Classifier vs checklist due dates render/trigger on inconsistent day boundaries if server tz not UTC. | Normalize all due dates to UTC-midnight. | apps/web/src/lib/move-task-generation.ts:238-265 |  |

| moving-tasks-07 | Moving & Move Tasks | UI/UX | Plan status change swallows errors and updates badge optimistically | handleStatusChange (moving-plan-detail-client.tsx:354-363) empty catch and flips status badge on res.ok without re-fetch. | Badge can show IN_PROGRESS/COMPLETED while DB rejected transition, desyncing UI on next reload. | Toast on failure; update local state only from server-returned plan. | apps/web/src/app/(app)/moving/plan/[id]/moving-plan-detail-client.tsx:354-363 |  |

| moving-tasks-08 | Moving & Move Tasks | Security | MoveTask idempotency/ownership keyed on plan-creator userId with cascade delete | MoveTask @@unique([userId,idempotencyKey]) and userId=plan.userId; User onDelete:Cascade on MoveTask; no workspaceId column. | Workspace move tasks keyed on individual user; deleting plan creator cascades and removes shared plan tasks others rely on. | Add workspaceId column with workspace-aware unique key, or document cascade semantics. | packages/db/prisma/schema.prisma:943,1008; apps/web/src/lib/move-task-generation.ts:46,578 |  |

| moving-tasks-10 | Moving & Move Tasks | Performance | Sync loop issues per-task sequential queries | syncSuggestedMoveTasks per-task findUnique/findFirst then create/update in a loop (classifier 547-631, checklist 430-525), no batching/transaction. | Dozens of sequential DB round-trips per plan for a mover with many services + full checklist. | Batch existing-task lookups with one findMany over key set; use createMany / transaction. | apps/web/src/lib/move-task-generation.ts:430-631 |  |

| moving-tasks-T1 | Moving & Move Tasks | Test | No tests for lifecycle, classifier, completion side-effects, or prune behavior | Only move-task-generation.test.ts and move-tasks/route.test.ts exist; classifyMoveServiceTransition, buildMoveTaskLifecyclePatch, completeMoveTaskWithLocalEffect have no dedicated tests. | Regressions in classification, lifecycle, completion side-effects, re-sync/prune can ship undetected. | Add unit tests per classifier branch and lifecycle transition; integration tests for re-sync no-dup, prune-on-removal, concurrent complete, workspace migration access. | apps/web/src/lib/move-task-generation.test.ts; apps/web/src/app/api/move-tasks/route.test.ts |  |

| moving-tasks-U2 | Moving & Move Tasks | Accessibility | Assign dropdown lacks a11y semantics and outside-click close | Assignment listbox (moving-plan-detail-client.tsx:642-674) has role=listbox but option buttons lack role=option/aria-selected; no outside-click/Escape. | Minor accessibility/UX papercut for keyboard/AT users. | Add role=option, aria-selected, Escape and outside-click close. | apps/web/src/app/(app)/moving/plan/[id]/moving-plan-detail-client.tsx:642-674 |  |

| notification-email-digest-03 | Notification / Email / Digest flow | Security | Unsubscribe page discloses current email-on-file to any holder of a (non-expiring) token | unsubscribe/page.tsx:66-98,138-141 render user.email for any verified token; token never expires (see -02). | Forwarded/leaked email link exposes recipient current email address (PII) indefinitely, even after address change. | Mask email on confirm/done view, or bind token to email it was issued for. | apps/web/src/app/unsubscribe/page.tsx:66-98,138-141 |  |

| notification-email-digest-06 | Notification / Email / Digest flow | Reliability | Push channel silently gated behind NOTIFICATION_PUSH_ENABLED, undocumented in .env.example (default off) | notifications.ts:80-90 returns false/skip with only console.warn when NOTIFICATION_PUSH_ENABLED!==true; .env.example lacks NOTIFICATION_PUSH_ENABLED, RESEND_WEBHOOK_SECRET, DAILY_DIGEST_ENABLED. | Operators unaware of flag see all push silently no-op; digest push and reminder pushes dropped with no visible error. | Document the three keys in .env.example; emit health/startup warning when push requested but disabled. | apps/web/src/lib/notifications.ts:80-90; .env.example |  |

| notification-email-digest-05 | Notification / Email / Digest flow | Security | One-click unsubscribe POST is unauthenticated and rate-limited by IP only | api/unsubscribe/route.ts:25-31 rateLimit 60/min on email:unsubscribe; no auth (token is consent per RFC 8058). | Acceptable per RFC 8058; HMAC forgery is real barrier; 60/min/IP allows token probing. | Optionally lower one-click limit or partly key on token; acceptable as-is. | apps/web/src/app/api/unsubscribe/route.ts:25-31 |  |

| notification-email-digest-07 | Notification / Email / Digest flow | Logic | Digest parity is soft-due-only; task hard-deadline escalation deliberately excluded (correct by design) | task-reminders/route.ts:132-139 suppresses only soft-due under digestOwnsSend; hard-deadline tier (229-316) keeps sending per-item; daily-digest.ts models only soft-due. | No defect; rollup does not bundle legally-critical deadline nudge which keeps firing per-item. | Preserve exemption when modifying digest parity; no change required. | apps/web/src/app/api/cron/task-reminders/route.ts:132-139,229-316; apps/web/src/lib/daily-digest.ts:249-309 |  |

| notification-email-digest-08 | Notification / Email / Digest flow | Architecture | In-app notification read APIs have no rate limit | notifications/feed/route.ts:6-35 and notifications/route.ts have no rateLimit (auth-gated only). | Low abuse surface (auth-gated, cheap) but unbounded paginated count queries per request. | Optionally add light per-user rate limit. | apps/web/src/app/api/notifications/feed/route.ts:6-35; apps/web/src/app/api/notifications/route.ts:14-28 |  |

| notifications-push-02 | Notifications & Push | Security | Push device token is sole identity; reassigned on any login with no transfer audit or format validation | push/register/route.ts:44-48 upsert where:{token} reassigns userId on conflict; token 10-255 char no format check; PushDevice.token @unique no audit on owner change. | If victim Expo token leaks, authenticated attacker registers it under their account, detaching victim device from notifications (targeted push DoS). | Validate Expo token format; audit-log ownership transfers. | apps/web/src/app/api/push/register/route.ts |  |

| notifications-push-03 | Notifications & Push | Data | Feed response returns full metadata JSON (internal ids) to the client | notifications/feed/route.ts:14-19 returns findMany with no select, serializing metadata (taskId, movingPlanId, queueId, dedupeKey). | Minor disclosure of user own internal entity ids (no cross-tenant exposure). | Add explicit select for UI fields only, excluding metadata/dedupeKey. | apps/web/src/app/api/notifications/feed/route.ts |  |

| notifications-push-07 | Notifications & Push | Logic | Web users cannot mute PUSH; email unsubscribe does not affect PUSH channel | POST /api/notifications only upserts EMAIL pref definitions; PUSH prefs only via mobile PUT; isPushTypeEnabled defaults ON; email unsubscribe flips only EMAIL rows. | Web-registered device (default push ON) cannot be silenced from web UI; email unsubscribe does not stop reminder PUSHes. | Add PUSH toggles to web settings or bridge unsubscribe to disable corresponding PUSH types. | apps/web/src/app/api/notifications/route.ts; apps/web/src/lib/notification-preferences.ts; apps/web/src/lib/unsubscribe.ts |  |

| notifications-push-08 | Notifications & Push | Reliability | Feed mark-read / mark-all-read endpoints are not rate-limited | notifications/feed/route.ts PATCH (read-all updateMany) and feed/[id] PATCH have no rateLimit, unlike push/register (20/60s). | Cheap repeated-mutation abuse vector against the feed. | Apply modest per-user rate limit to feed mutations. | apps/web/src/app/api/notifications/feed/route.ts; apps/web/src/app/api/notifications/feed/[id]/route.ts |  |

| notifications-push-09 | Notifications & Push | Reliability | User ids embedded in cron error payloads and logs | daily-digest/route.ts:118-120, task-reminders/route.ts:225, lifecycle-nudges push user-id strings into errors[] returned in JSON cron response. | User-id exposure in cron HTTP responses (not PII but unnecessary surface). | Log ids internally only; keep out of returned payloads. | apps/web/src/app/api/cron/daily-digest/route.ts; apps/web/src/app/api/cron/task-reminders/route.ts |  |

| notifications-push-10 | Notifications & Push | Reliability | Scheduled-delivery records legit suppression as No channel delivered failure | scheduled-delivery/route.ts:181-189 sets error:No channel delivered and increments failed when push disabled / no email, conflating skip with error. | Misleading failure metrics obscuring real delivery errors. | Distinguish suppressed/skipped from failed in worker accounting. | apps/web/src/app/api/cron/scheduled-delivery/route.ts |  |

| notifications-push-06 | Notifications & Push | Architecture | Duplicate notification presentation mapping between dropdown and full feed | notificationPresentation in notification-center.tsx and presentationFor in notifications/page.tsx implement separate divergent type->icon/tone maps. | Styling drift between the two feed surfaces over time. | Extract a single shared presentation map. | apps/web/src/components/layout/notification-center.tsx; apps/web/src/app/(app)/notifications/page.tsx |  |

| partners-affiliate-movers-01 | Partners, Affiliate & Movers Portal | Logic | Affiliate postback can cross-attribute a conversion to a provider that does not own the echoed click | affiliate/postback/[network]/route.ts:67-86 sets affiliateClickId=click.id but providerId overridden from click only when empty; no check click.providerId===providerId. | Conversion written for provider B while linked to click owned by provider A; corrupted per-provider attribution. HMAC-gated. | When clickId resolves and explicit providerId differs, reject (400) or drop affiliateClickId link; prefer click provider. | apps/web/src/app/api/affiliate/postback/[network]/route.ts |  |

| partners-affiliate-movers-02 | Partners, Affiliate & Movers Portal | Security | Portal magic-link token travels in URL query and is not single-use | {partners,movers}/portal/enter/route.ts:11 reads token from searchParams; consume*Token swaps for cookie but link token valid until 24h TTL/logout/supersede. | Leaked emailed link (forwarded email, history, logs) grants portal access until expiry. | Split single-use short-TTL link token from session token; revoke link token on first consumption. | apps/web/src/app/partners/portal/enter/route.ts, apps/web/src/app/movers/portal/enter/route.ts |  |

| partners-affiliate-movers-03 | Partners, Affiliate & Movers Portal | Security | Portal mutation routes rely on SameSite=Lax with no CSRF/Origin check | Portal cookies sameSite:lax; form-POST mutations (leads-optin, logout, placements/request) have no anti-CSRF token or Origin check. | On browsers with weak SameSite enforcement a forged cross-site form could toggle lead opt-in or log out partner. | Add Origin/Sec-Fetch-Site check or per-session CSRF token to portal POST handlers. | apps/web/src/app/api/partners/portal/leads-optin/route.ts, apps/web/src/app/api/movers/portal/logout/route.ts |  |

| partners-affiliate-movers-04 | Partners, Affiliate & Movers Portal | Security | /api/leads rate limit is fail-open unlike billing-sensitive routes | api/leads/route.ts:41-47 rateLimit 10/h no failClosed, while affiliate/click and acquisition/redeem use failClosed:true. | Under Redis outage on multi-instance, authenticated user could exceed lead cap, fanning PII to partners and accruing CPL charges. | Pass failClosed:true on leads limiter to match redeem/affiliate posture. | apps/web/src/app/api/leads/route.ts |  |

| partners-affiliate-movers-05 | Partners, Affiliate & Movers Portal | Performance | Lead matching loads all approved partners/movers and filters in memory | match-movers.ts:52-63 and match-partners.ts:69-74 fetch all APPROVED+leadsOptIn rows then filter by state in JS, no state index. | Per-lead matching O(n) over full approved set as roster grows, on synchronous lead-submission path. | Move to state-scoped query or add index before approved set grows. | apps/web/src/lib/leads/match-movers.ts, apps/web/src/lib/leads/match-partners.ts |  |

| partners-affiliate-movers-06 | Partners, Affiliate & Movers Portal | Reliability | Mover application with all document uploads failing yields a PENDING app with no proof and only a console.error | api/movers/apply/route.ts:143-162 uploads each doc best-effort; failed upload skipped with console.error, no rollback; fully-failed returns 201 documentsUploaded=0. | Admin reviews application that should have proof docs but has none, with no surfaced signal. | Emit admin signal/flag when files attached but documentsUploaded===0. | apps/web/src/app/api/movers/apply/route.ts |  |

| partners-affiliate-movers-07 | Partners, Affiliate & Movers Portal | Logic | Stale comment claims a missing @@unique([userId, campaignId]) that already exists | acquisition/redeem/route.ts:83-101 comment says schema has no unique index yet; schema.prisma:~387 already defines @@unique([userId,campaignId]); catch handles P2002. | Misleading documentation; guard itself correct. | Update comment to reflect existing constraint. | apps/web/src/app/api/acquisition/redeem/route.ts, packages/db/prisma/schema.prisma |  |

| partners-affiliate-movers-08 | Partners, Affiliate & Movers Portal | UI/UX | Partner portal lead-optin toggle has no loading/disabled state | partners/portal/page.tsx:99-107 plain form/button no pending state; double submit posts twice (server idempotent). | Cosmetic double-POST; no corruption. | Add pending/disabled state. | apps/web/src/app/partners/portal/page.tsx |  |

| partners-affiliate-movers-09 | Partners, Affiliate & Movers Portal | Security | Lead-dispatch partner email uses a hand-rolled escaper handling only <>& | dispatch-leads.ts:54-118 esc() escapes <,>,& only; user-controlled name/email/notes interpolated into table text nodes (safe in current template). | Low; would become stored-HTML-injection only if value moved into attribute context without quote-escaping. | Keep interpolation in text nodes or switch to shared email renderer escaping. | apps/web/src/lib/leads/dispatch-leads.ts |  |

| partners-affiliate-movers-10 | Partners, Affiliate & Movers Portal | Test | No portal-auth unit tests for revocation-on-status-change or token expiry | No *-portal-auth.test.ts for partner/mover portal-auth libs containing APPROVED-status/company-active revocation and TTL. | Regression risk on session invalidation semantics. | Add unit tests covering expiry, revokedAt, partner/company status-change invalidation. | apps/web/src/lib/partner-portal-auth.ts, apps/web/src/lib/mover-portal-auth.ts |  |

| payments-billing-web-04 | Payments & Billing (Stripe / Web) | Logic | Plan-resolution precedence inconsistent between webhook handlers | checkout.session.completed prefers session.metadata.plan over mapped price (709); customer.subscription.* prefers mapped price over metadata (812-815). Both server-set today. | If metadata.plan drifts from price, the two paths persist different plans. | Standardize on price-derived plan first everywhere; metadata only as fallback. | apps/web/src/app/api/webhooks/stripe/route.ts:709,812-815 |  |

| payments-billing-web-05 | Payments & Billing (Stripe / Web) | Logic | invoice.paid zero-amount guard only covers subscription_create | route.ts:997 skips only amount_paid===0 && billing_reason===subscription_create; $0 proration with other billing_reason still flips ACTIVE/PAID. | Benign for access but may mislabel accessType on $0 proration invoice. | Broaden zero-amount short-circuit or branch on billing_reason. | apps/web/src/app/api/webhooks/stripe/route.ts:997 |  |

| payments-billing-web-06 | Payments & Billing (Stripe / Web) | Performance | STRIPE_PRICE_* config read (7-way fan-out) repeated per webhook, twice on invoice events | mapStripePriceIdToPlanAndInterval issues 7 parallel getRuntimeConfigValue reads; invoice.paid calls twice per event. | Extra DB load per renewal event at volume. | Cache STRIPE_PRICE_* config or batch via getRequiredRuntimeConfigValues. | apps/web/src/lib/billing.ts:79-108; apps/web/src/app/api/webhooks/stripe/route.ts:995-1006 |  |

| payments-billing-web-07 | Payments & Billing (Stripe / Web) | Security | GET /api/stripe/checkout/cancel mutates DB state | GET handler (cancel/route.ts:71-81) calls resetPendingCheckoutForUser updating Subscription then redirects; auth-gated by requireDbUserId. | State-changing GET on auth-gated endpoint (CSRF-adjacent); only resets caller own stale PENDING_CHECKOUT. | Keep GET a pure redirect; rely on POST for authoritative reset. | apps/web/src/app/api/stripe/checkout/cancel/route.ts:71-81 |  |

| payments-billing-web-08 | Payments & Billing (Stripe / Web) | UI/UX | Webhook email currency formatting hardcodes en-US locale | formatCurrency (route.ts:76-86) uses Intl.NumberFormat(en-US) while honoring user currency; emails sent to es-US too. | Spanish-locale recipients see US-formatted amounts in billing emails. | Pass resolved lang into NumberFormat. | apps/web/src/app/api/webhooks/stripe/route.ts:76-86 |  |

| payments-billing-web-09 | Payments & Billing (Stripe / Web) | Data | firstChargeAmount derived by regex-stripping a presentation label | checkout route.ts:637 firstChargeAmount = parseFloat(displayPrice.replace(/[^0-9.]/g,)) \|\| null; label with extra digits corrupts amount. | Mis-stored firstChargeAmount if campaign display label format changes. | Persist dedicated numeric campaign amount field. | apps/web/src/app/api/stripe/checkout/route.ts:637; apps/web/src/lib/acquisition-campaigns.ts |  |

| payments-billing-web-10 | Payments & Billing (Stripe / Web) | Dead Code | Duplicated mapStripeStatus/mapStripeStatusWithRenewal (webhook vs shared lib) | Webhook defines own mapStripeStatus (route.ts:1332-1356); reconcile cron uses shared lib copy (stripe-subscription-mapping.ts:15-38). | Two copies can drift, re-introducing webhook/reconcile status-mapping disagreement. | Import shared lib mapper into webhook route, delete local copy. | apps/web/src/app/api/webhooks/stripe/route.ts:1332-1356; apps/web/src/lib/stripe-subscription-mapping.ts:15-38 |  |

| payments-billing-web-S3 | Payments & Billing (Stripe / Web) | Architecture | ProcessedWebhookEvent primary key is event-id only; source is not in the key | schema.prisma:1693-1700 id (Stripe event id) sole PK, source non-key; markWebhookEventProcessed inserts {id,source}. | If a second webhook source reused a Stripe-style event id, create would collide on PK and mis-treat as duplicate. Not exploitable today. | Use composite PK (id,source) if a second source is added; else document single-namespace assumption. | packages/db/prisma/schema.prisma:1693-1700; apps/web/src/lib/webhook-idempotency.ts |  |

| payments-billing-web-S2 | Payments & Billing (Stripe / Web) | Security | Customer/workspace binding (IDOR) assurance - no IDOR found | User routes scope by requireDbUserId; webhook resolves local row by OR(userId, stripeCustomerId, stripeSubscriptionId) then writes where userId:local.userId; metadata.userId set server-side at checkout; unique columns; signed events. | No cross-user entitlement write path found. | Add regression test asserting metadata.userId pointing at another customer is reconciled by stripeCustomerId. | apps/web/src/app/api/webhooks/stripe/route.ts:183-226; apps/web/src/app/api/stripe/checkout/route.ts:681-711 |  |

| provider-connector-dispatch-04 | Provider / Connector Dispatch | Security | No connector-specific per-user rate limit on dispatch/initiate (global user_write, failClosed:false) | POST /api/connector-dispatch and GET oauth/initiate have no in-route rate limit; rely on middleware user_write (120/min, failClosed:false); writes pass unthrottled when limiter unconfigured/erroring. | Widens double-file window (Finding 02) and permits OAuth-initiate flooding; degrades open under limiter outage. | Add explicit low per-user limit on dispatch/initiate, or route through fail-closed group. | apps/web/src/middleware.ts:372-394; apps/web/src/lib/rate-limit-policy.ts:201-212 |  |

| provider-connector-dispatch-05 | Provider / Connector Dispatch | Logic | Dispatch subject id sourced from session.userId vs scope.actorUserId (dual sourcing) | connector-dispatch/route.ts:40-60 resolves default primary by userId:session.userId while entitlement uses scope object; enqueueAddressChange queries consents by input.userId. Consistent today (actor==subject). | Future change permitting actor!=subject could mis-scope address/consent lookups. | Derive subject id once from resolved scope and pass explicitly to enqueue. | apps/web/src/app/api/connector-dispatch/route.ts:40-60; apps/web/src/lib/connector-runtime.ts:204-215 |  |

| provider-connector-dispatch-06 | Provider / Connector Dispatch | Performance | Worker processes the claimed batch sequentially | runDueDispatches (connector-runtime.ts:614-633) awaits each row in for...of; each attempt up to 15s HTTP timeout. | A few slow/unhealthy partners can serialize a tick and delay healthy rows. | Use bounded concurrency keyed per connector so breakers still apply. | apps/web/src/lib/connector-runtime.ts:614-633; packages/connectors/src/core/http-client.ts:82 |  |

| provider-connector-dispatch-07 | Provider / Connector Dispatch | Logic | Primary-address edit auto-sync never reaches USPS (from === null skip) | addresses/[id]/route.ts:176-184 enqueues with no fromAddressId; enqueueAddressChange skips requiresOrigin connectors when base.from===null; USPS manifest requiresOrigin:true. | Auto-sync on primary-address edit effectively inert for the only live connector; only a real from+to move dispatches. | Confirm intended; consider deriving from prior primary address for genuine moves. | apps/web/src/app/api/addresses/[id]/route.ts:176-184; apps/web/src/lib/connector-runtime.ts:254; packages/connectors/src/usps/index.ts:50 |  |

| provider-connector-dispatch-08 | Provider / Connector Dispatch | Data | Canonical change payload never carries effectiveDate | enqueueAddressChange base (connector-runtime.ts:222-227) omits effectiveDate; buildUspsCoaRequest reads input.effectiveDate which is therefore always undefined. | USPS moveEffectiveDate always omitted; partner may apply own default. | Thread move date from address/move record into payload if relevant. | apps/web/src/lib/connector-runtime.ts:222-227; packages/connectors/src/usps/request.ts:114 |  |

| providers-connectors-02 | Providers & Connectors | Security | Connector egress allowlist is host-string based, not IP/private-range pinned | http-client.ts assertAllowed and redirect re-check match url.host against allowlist (https-only, Authorization stripped cross-host) but never reject private/loopback IPs; same in connector-oauth.ts. | Residual SSRF (DNS rebinding) theoretical since hosts not user-controlled; no IP-range defense-in-depth. | Add private/loopback/link-local IP-range guard at request time and on each redirect hop. | packages/connectors/src/core/http-client.ts; apps/web/src/lib/connector-oauth.ts |  |

| providers-connectors-03 | Providers & Connectors | Security | OAuth CSRF state validated against cookie but not bound to session user | partner-consents/oauth/initiate sets pc_oauth_state cookies; callback compares cookieState===state then attaches consent to session.userId without binding state to user id. | Consent-fixation would require controlling victim cookies; sameSite=lax+httpOnly mitigate. | HMAC a session-bound value into OAuth state; verify server-side at callback. | apps/web/src/app/api/partner-consents/oauth/initiate/route.ts; apps/web/src/app/api/partner-consents/oauth/callback/route.ts |  |

| providers-connectors-04 | Providers & Connectors | Performance | ProviderGovernanceIssue upserts run on the hot recommendations request path | providers/recommendations/route.ts L757-887,889-978 do awaited find+create/update upserts (up to 8 gaps) at L1286-1303 on every request where live FCC/OpenEI source errors or returns unmatched. | Extra serialized DB round-trips and write contention on a read-heavy endpoint; flapping integration causes write storms. | Move governance-issue writes to async queue / sampled, debounced write keyed by (source,status) with TTL. | apps/web/src/app/api/providers/recommendations/route.ts |  |

| providers-connectors-05 | Providers & Connectors | Performance | Duplicate guard scans all active providers in a category in-memory | custom-provider-duplicate-guard.ts L46-64 loads all active ServiceProvider rows in a category and compares normalizeProviderName in JS; per create/edit. | Per-create/edit full-category scan (FINANCIAL_* 100+ providers); bounded but avoidable DB load. | Add normalized-name column/index or narrow candidate set before in-memory compare. | apps/web/src/lib/custom-provider-duplicate-guard.ts |  |

| providers-connectors-06 | Providers & Connectors | Test | No automated ranking-integrity invariant or FTC-label regression test | scoreProviders/compareScoredProviders correctly exclude affiliate/sponsorship from scoring and providers-client renders FTC label separately, but no test asserts invariants. | Future change could let sponsorship leak into organic ranking or drop FTC label without failing test. | Add property test (toggling affiliate/sponsored never reorders organic) and component test for label. | packages/shared/src/recommendation-engine.ts; apps/web/src/app/(app)/providers/providers-client.tsx |  |

| security-platform-04 | Security Platform | Security | internal/cron/webhook routes middleware-public by prefix; only in-route guards protect them | middleware.ts:76-86 lists prefixes in PUBLIC_API_PREFIXES skipping CSRF/session; all 29 cron + 3 internal routes currently implement in-route guard (verified 29/29 guardCronRequest, internal verifyInternalAuth). | Latent footgun: future route under these prefixes forgetting its guard is exposed with no middleware backstop. No present vuln. | Add cheap middleware assertion cron/internal carry a bearer credential; AST/grep test asserting guard usage. | apps/web/src/middleware.ts:76-86,196-198,305,320 |  |

| security-platform-05 | Security Platform | Logic | Anonymous-IP callers collapse into shared limiter buckets and cannot be IP-banned | cron-guard.ts:86 callerBucket=cron when IP anonymous; rate-limit-policy.ts:389 hashes constant anonymous; ip-rules.ts:76 never matches anonymous. | Where trusted IP header absent, legit traffic shares one bucket (false 429s) and IP bans unenforceable. | Resolve via SP-01; monitor/alert when anonymous share exceeds small fraction. | apps/web/src/lib/cron-guard.ts:86; apps/web/src/lib/rate-limit-policy.ts:389; apps/web/src/lib/ip-rules.ts:76 |  |

| security-platform-06 | Security Platform | Reliability | Per-instance in-memory counters dilute enforcement and detection at scale | rate-limit.ts:56 memStore, security-alerts.ts:119 memCounters, security-alert-sink.ts:52 burst state all module-scope Maps; comments acknowledge multi-instance caveat. | Lockout alarms, burst detection, rate limits must be crossed on a single instance; horizontal scaling weakens them. | Keep Redis healthy (SP-02); treat limiterMode===memory in production as paging condition. | apps/web/src/lib/rate-limit.ts:56; apps/web/src/lib/security-alerts.ts:119; apps/web/src/lib/security-alert-sink.ts:52 |  |

| security-platform-07 | Security Platform | Security | AES-256-GCM uses a 16-byte IV (12 recommended) | encryption.ts:13 IV_LENGTH=16; GCM standard nonce 12 bytes; IV+tag stored, random per message, round-trips correct. | No in-app weakness; standards/interop footgun if interoperating with strict 12-byte-IV implementation. | Adopt 12-byte IV in a future enc_v2 format behind version-aware decrypt. | packages/shared/src/encryption.ts:13,48,114,176 |  |

| security-platform-08 | Security Platform | Security | safeEqual length pre-check leaks token-length equality | internal-secrets.ts:37-43 returns false on length mismatch before constant-time loop; same in encryption.ts:213 HMAC verify. | Marginal — secrets high-entropy >=32 chars, length oracle alone not practically exploitable. | Acceptable; if hardening, compare fixed-length HMACs of both inputs. | apps/web/src/lib/internal-secrets.ts:37-43; packages/shared/src/encryption.ts:213 |  |

| security-platform-09 | Security Platform | Security | Kill-switch write-path authorization lives outside this module | kill-switches.ts:24-27 only READS KILL_SIGNUPS/KILL_OUTBOUND_EMAIL; authz to SET them is the admin Runtime-Config write route, out of module scope. | If write path under-protected, attacker could pause all signups or silence outbound email. Cannot confirm from this module. | Confirm admin Runtime-Config write route requires SUPER_ADMIN + step-up + audit for these keys. | apps/web/src/lib/kill-switches.ts:24-27 |  |

| signup-login-02 | Signup / Login / Session flow | Security | OAuth email-match auto-links a provider to an existing password account on email_verified alone | findOrLinkOAuthUserWithStatus branch 2 (user-auth.ts:860-923) links provider to email-matched active user with no authenticated-session/explicit-confirm gate; callbacks require email_verified; post-hoc security email sent. | Provider account proving same verified email silently linked to pre-existing password account; mitigated by email_verified + after-the-fact email. | Keep post-link security email; consider requiring authenticated session or explicit confirmation before linking. | apps/web/src/lib/user-auth.ts, apps/web/src/app/api/auth/oauth/google/callback/route.ts |  |

| signup-login-04 | Signup / Login / Session flow | Reliability | Lockout / rate-limit fall back to per-instance in-memory maps without Redis | login-lockout.ts uses module-level Map (33) and local-only fallback when Upstash absent; isLoginLocked fails closed for reads in prod but failure counter local. | Multi-instance/serverless without Redis: lockout/rate-limit weakens (counters not shared, reset on restart); attacker spreads attempts. | Make Redis mandatory in production (startup assertion) or document/enforce requirement. | apps/web/src/lib/login-lockout.ts, apps/web/src/lib/rate-limit.ts |  |

| signup-login-05 | Signup / Login / Session flow | Logic | Password sign-in client redirect applies only email-verified gate, not legal/onboarding gate | sign-in/page.tsx:120-133 branches only on emailVerified===false then router.replace; OAuth server path applies fuller gate via resolvePostAuthRedirect; login API response does not run getPostAuthUserState. | Returning password user who never completed legal/onboarding could route straight to requested path, relying on destination page server-side gate. | Confirm /onboarding and gated pages enforce gate server-side; else apply getPostAuthUserState on login response. | apps/web/src/app/sign-in/page.tsx, apps/web/src/lib/post-auth-redirect.ts |  |

| signup-login-07 | Signup / Login / Session flow | Reliability | Failed signup verification email leaves user with no unauthenticated resend path | Verification email send fire-and-forget (register:216-222); UI shows check-email regardless; only resend endpoint requires session. | User whose first verification email fails is stuck until sign-in (routes to /verify-email where resend exists). | Add resend affordance on sign-up check-email screen or rate-limited unauthenticated resend-by-email with anti-enumeration response. | apps/web/src/app/sign-up/page.tsx, apps/web/src/app/api/auth/resend-verification/route.ts |  |

| signup-login-06 | Signup / Login / Session flow | Security | Login/register are CSRF-exempt JSON mutations (accepted design) | applyCsrfCheck enforces content-type and only origin/referer checks for logout; login/register pass on JSON content-type with sameSite=lax cookie no token. | Low-risk for login/register (no pre-existing session); recorded as accepted design. | Document accepted risk; optionally enforce same-origin on these routes as logout does. | apps/web/src/middleware.ts |  |

| subscription-payment-web-04 | Subscription Payment (Stripe / Web) | Logic | Initial subscription_create payment failure is terminal locally (UNPAID->canceled) with no grace window | webhooks/stripe:1082-1138 sets status=UNPAID, gracePeriodEndsAt=null when billing_reason===subscription_create; entitlement.ts:462-480 maps UNPAID to CANCELED/no access; invoice.paid later restores. | User whose first charge fails but still inside Stripe initial retry window reads as no-access locally with no distinct incomplete state. | Confirm UX; consider INCOMPLETE state distinct from canceled during initial retry. | apps/web/src/app/api/webhooks/stripe/route.ts:1082-1138, packages/shared/src/entitlement.ts:462-480 |  |

| subscription-payment-web-05 | Subscription Payment (Stripe / Web) | Security | Completed session plan/price not compared against pre-checkout reservation | checkout writes stripePriceId + disclosure hash to row/metadata, but webhooks/stripe:707-720 derives plan purely from live price and never compares to reserved stripePriceId. | Price/plan substitution between session creation and completion accepted silently; limited (price guard at creation, Stripe owns line items, promo codes intentional). | Compare completed recurring price against reserved stripePriceId (tolerating promo) and alert on tier change. | apps/web/src/app/api/webhooks/stripe/route.ts:707-720, apps/web/src/app/api/stripe/checkout/route.ts:631-711 |  |

| subscription-payment-web-06 | Subscription Payment (Stripe / Web) | Reliability | reconcileSeatsForOwner errors swallowed silently on every billing transition | reconcileSeatsForOwner(...).catch(()=>{}) at webhooks/stripe:493,965,1250,1307 and change-plan:181. | Persistent failure to demote over-limit members after downgrade/cancel/refund/dispute leaves them write access on unpaid/over-cap plan, no signal. | Capture error to Sentry instead of discarding. | apps/web/src/app/api/webhooks/stripe/route.ts:493,965,1250,1307, apps/web/src/app/api/subscription/change-plan/route.ts:181 |  |

| subscription-payment-web-07 | Subscription Payment (Stripe / Web) | Architecture | Strong flow defenses observed (positive) | verifyStripeCheckoutPrice; reserve-before-act idempotency + lastStripeEventAt out-of-order guard + version increments; return page polls real state; duplicate-sub cancellation; reconcile cron shares deriveStripeEntitlementFields with webhook. | Invariants prevent wrong-amount charges, double webhook application, false success, double-charge from orphaned subs, webhook/cron drift. | Keep invariants covered by tests; treat as regression-sensitive. | apps/web/src/app/api/stripe/checkout/route.ts:60-85, apps/web/src/app/api/webhooks/stripe/route.ts, apps/web/src/lib/stripe-subscription-mapping.ts |  |

| workspace-invitation-household-04 | Workspace Invitation / Household | Logic | Stale PENDING invites dangle after owner downgrade and dead-end the invitee | acceptWorkspaceInvitation re-checks seats against owner CURRENT entitlement; reconcileWorkspaceSeats demotes members but does not touch PENDING invites which stay actionable until expiry. | Invitee created while owner on FAMILY/PRO may attempt accept after downgrade and hit SEAT_FULL (409); invite still displays pending. | On owner downgrade/reconcile, revoke or flag over-limit PENDING invites. | apps/web/src/lib/workspace-invite-accept.ts; apps/web/src/lib/workspace-ownership.ts |  |

| workspace-invitation-household-05 | Workspace Invitation / Household | Dead Code | Unique index on (workspaceId, invitedEmail, expiresAt) never dedupes; P2002 branch is near-dead | schema.prisma:2268 @@unique includes expiresAt always now()+7d so re-invites differ and never collide; dedup enforced by in-transaction PENDING_INVITE query; P2002 handler unreachable. | Misleading constraint; maintenance risk if assumed to enforce one-invite-per-email. | Drop expiresAt from unique key (handle collisions) or document query-driven dedup and remove dead branch. | packages/db/prisma/schema.prisma; apps/web/src/app/api/workspaces/[id]/invitations/route.ts |  |

| workspace-invitation-household-06 | Workspace Invitation / Household | Logic | Demoting a MEMBER to CHILD silently removes visibility of shared records | permissions.ts:107-111 makes address.view/service.viewBasic self-only for CHILD; settings UI role select offers CHILD with no confirmation. | Routine role change to CHILD/VIEW_ONLY surprise-removes member access to records previously seen, with no warning. | Confirm product intent; add UI confirmation when demoting to CHILD/VIEW_ONLY noting reduced visibility. | packages/shared/src/permissions.ts; apps/web/src/app/(app)/settings/workspace/page.tsx |  |

| workspace-invitation-household-07 | Workspace Invitation / Household | Reliability | Invite/notification URLs fall back to localhost when NEXT_PUBLIC_APP_URL is unset | appBaseUrl() returns NEXT_PUBLIC_APP_URL \|\| http://localhost:3000 in invitations/route.ts:21-23 and members/[memberId]/route.ts:19-21. | Missing/incorrect env in production would mint localhost accept/manage links in emails. | Assert/fail loudly when NEXT_PUBLIC_APP_URL unset in production. | apps/web/src/app/api/workspaces/[id]/invitations/route.ts; apps/web/src/app/api/workspaces/[id]/members/[memberId]/route.ts |  |

| workspace-invitation-household-08 | Workspace Invitation / Household | Data | WorkspaceMember role/status are free-form VarChar(20) | schema.prisma:2213-2214 declares role/status VarChar(20); context resolver fails closed on unknown status via allow-list; unknown role falls through can() deny. | Defensive posture acceptable; invalid values could be written outside documented set. | Consider Prisma enums or DB CHECK constraint for role/status. | packages/db/prisma/schema.prisma; apps/web/src/lib/workspace-context.ts |  |

| api-map-02 | api-map | Security | Public affiliate postback webhook has no rate limit (HMAC only) | affiliate/postback/[network]/route.ts authenticates via HMAC-SHA256 + idempotent upsert but no rateLimit; unsigned floods cost CPU/DB. | Minor DoS surface; write-abuse prevented by signature gate. | Add IP-keyed rateLimit before signature check. | apps/web/src/app/api/affiliate/postback/[network]/route.ts |  |

| api-map-03 | api-map | Reliability | sponsored/click swallows all errors and has no rate limit | sponsored/click/route.ts returns {ok:true} on every path incl catch, no rate limit, best-effort recordSponsoredClick without logging. | Real failures masked (no observability); minor abuse surface. | Emit Sentry breadcrumb on failure; add modest per-user rate limit. | apps/web/src/app/api/sponsored/click/route.ts, apps/web/src/lib/movers.ts |  |

| api-map-04 | api-map | Architecture | ~190 handlers classified by grep signal but not individually opened | Auth/zod/rate-limit presence by grep; [NV] cells not opened per-method; deep-dived handlers sound. | Residual low: exact verbs, validation location, specific gates unverified for [NV] set. | Follow-up pass opening each [NV] handler. | docs/audit/04_API_MAP.md |  |

| api-map-05 | api-map | Reliability | Schema-compat fallbacks transiently weaken webhook/impersonation guards during rolling deploys | webhooks/stripe, internal/impersonate, auth/impersonate-handoff fall back to unguarded writes via isMissingDbColumnError + warnSchemaCompatibilityFallback. | During migration window, out-of-order/idempotency protection and impersonation scoping degraded. | Ensure migrations land before code; alarm on warnSchemaCompatibilityFallback. | apps/web/src/app/api/webhooks/stripe/route.ts, apps/web/src/lib/db-schema-compat.ts |  |

| component-system-08 | component-system | Dead Code | Theme provider duplicated verbatim between web and admin | apps/admin theme-provider.tsx line-for-line copy of web, differs only in storageKey. | Two copies to maintain; behavior can drift. | Move provider + useTheme into shared package parameterized by storageKey. | apps/web/src/components/theme-provider.tsx, apps/admin/src/components/theme-provider.tsx |  |

| component-system-09 | component-system | Architecture | Tailwind configs duplicated across web and admin | web/admin tailwind.config.ts share identical theme.extend colors/fontSize/boxShadow; admin notes Mirrors apps/web. | Manual-sync risk; new tone in one app silently missing in other. | Extract shared Tailwind preset consumed by both. | apps/web/tailwind.config.ts, apps/admin/tailwind.config.ts |  |

| component-system-10 | component-system | Theme | Frequent hardcoded raw color/alpha literals despite token system | mobile Button.tsx/theme.ts raw rgba; admin data-table-page literal opacity utilities; web loading-state bg-foreground/[0.02]. | Values not derived from tokens fail to track palette changes; subtle alpha inconsistencies. | Promote reused alpha surfaces to named tokens/CSS vars. | apps/mobile/src/components/ui/Button.tsx, apps/web/src/components/shared/loading-state.tsx |  |

| data-flow-map-02 | data-flow-map | Data | MoveTask has no own workspaceId; tenant isolation relies on MovingPlan join | schema.prisma:939-1021 MoveTask userId+movingPlanId no workspaceId; scoping via movingPlan workspaceId. | Every scoped query must join MovingPlan; forgotten join loses isolation, higher cost. | Add additive nullable workspaceId on MoveTask backfilled from plan. | packages/db/prisma/schema.prisma:939-1021; apps/web/src/app/api/move-tasks/route.ts:81-89 |  |

| data-flow-map-04 | data-flow-map | Data | Subscription.purchaseToken retained plaintext alongside encrypted twin | schema.prisma:250-252 purchaseToken plaintext @db.Text coexists with purchaseTokenEncrypted/Hash. | If plaintext column still written, sensitive IAP token recoverable from DB dump. | Confirm writers populate only encrypted/hash; backfill-null and drop purchaseToken. | packages/db/prisma/schema.prisma:250-252; apps/web/src/lib/iap-common.ts |  |

| data-flow-map-05 | data-flow-map | Data | EmailLog.to indexed plaintext-email hot path not scrubbed on erasure | schema.prisma:1572,1585 EmailLog.to plaintext indexed append-only; account-deletion purges only WaitlistSignup+NotificationQueue. | Deleted users email addresses persist in EmailLog after erasure. | Define retention/scrub policy for EmailLog.to on erasure. | packages/db/prisma/schema.prisma:1564-1589; apps/web/src/lib/account-deletion.ts:365-370 |  |

| data-flow-map-07 | data-flow-map | Performance | Broadcast notification fan-out writes one row per user per queue row, unbounded per tick | scheduled-delivery/route.ts:81-137 broadcast pages whole User table calling deliverToUser per user; only QUEUE_BATCH=200 rows capped, not audience size. | Single broadcast to large user base dominates a cron tick and DB write path. | Chunk broadcast across ticks via persisted cursor or use createMany. | apps/web/src/app/api/cron/scheduled-delivery/route.ts:81-137 |  |

| data-flow-map-08 | data-flow-map | Reliability | Signup provisioning is multiple sequential awaits without a transaction | auth/register/route.ts:162-214 user.create then ensureSubscriptionDefaults/Workspace/legal/token as independent awaits; dup-email guard blocks retry. | Partially-provisioned accounts without Subscription/Workspace; user blocked from re-signup. | Wrap provisioning in one transaction or self-heal via ensure-on-access. | apps/web/src/app/api/auth/register/route.ts:162-214 |  |

| marketing-seo-content-06 | marketing-seo-content | Security | POST /api/consent/ccpa has no rate limit (authenticated ledger bloat) | consent/ccpa/route.ts:77-104 no rateLimit; for logged-in each POST appends DataConsent row; sibling public POSTs all rate-limit. | Authenticated client can loop opt-out toggles to bloat append-only DataConsent and obscure audit history. | Wrap with rateLimit; alert on abnormal per-user DataConsent insert rate. | apps/web/src/app/api/consent/ccpa/route.ts:77-104 |  |

| marketing-seo-content-07 | marketing-seo-content | Security | POST /api/consent has no rate limit | consent/route.ts:85-139 appends up to CATEGORIES.length rows per POST with no rateLimit. | Authenticated consent-ledger bloat / audit-noise amplification. | Add per-user rate limit and insert-rate alarm. | apps/web/src/app/api/consent/route.ts:85-139 |  |

| marketing-seo-content-08 | marketing-seo-content | Performance | All 51 state + metro programmatic-SEO pages are force-dynamic (no static/ISR cache) | moving/[state]/page.tsx sets dynamic=force-dynamic (dynamicParams=false) for per-request CSP nonce; same for [city]; generateStaticParams pins slug set but pages still render per request. | Highest-volume SEO pages render on every request instead of static cache, raising TTFB/cost under crawl/burst. | Verify CDN cache in front; if not, explore static rendering with nonce-free CSP or signed static nonces. | apps/web/src/app/moving/[state]/page.tsx, apps/web/src/app/moving/[state]/[city]/page.tsx |  |

| marketing-seo-content-09 | marketing-seo-content | Performance | Three legacy font families loaded alongside the canonical design-system fonts | layout.tsx:27-52 loads Geist, Geist_Mono, Fraunces with comments stating superseded by Playfair/DM Sans/DM Mono. | Extra font and CSS-variable payload for non-canonical fonts. | Remove legacy font loads once no CSS-variable references remain. | apps/web/src/app/layout.tsx:27-52 |  |

| marketing-seo-content-10 | marketing-seo-content | Dead Code | Likely-dead testimonial-quote component retains fabricated-attribution risk | page.tsx:479-483 documents standalone TestimonialQuote removed from homepage because it shipped a fabricated named-customer attribution; component file still exists. | Unused component with fabricated testimonial can be re-introduced, re-creating FTC/trust issue. | Confirm no imports and delete, or keep only behind a real consented-quote gate. | apps/web/src/components/marketing/testimonial-quote.tsx, apps/web/src/app/page.tsx:479-483 |  |

| marketing-seo-content-11 | marketing-seo-content | UI/UX | Footer copyright uses the privacy i18n key as a standalone sentence | marketing-footer.tsx:69 renders copyright + tCommon(privacy) producing LocateFlow. Privacy. | Unpolished/confusing copyright line on every public footer. | Use dedicated All rights reserved. translation key. | apps/web/src/components/marketing/marketing-footer.tsx:69 |  |

| module-map-03 | module-map | Architecture | Same-named UI primitives forked per app (web/admin/mobile) | confirm-dialog, empty-state, language-selector, service-logo-mark exist independently in web/admin/mobile with no shared package. | web<->admin duplication is maintenance cost and inconsistent behavior. | Consider shared web UI package for web/admin primitives. | apps/web/src/components/shared/confirm-dialog.tsx, apps/admin/src/components/confirm-dialog.tsx |  |

| module-map-05 | module-map | Security | Large hand-maintained public-route allow-list with prefix matching | middleware.ts:26-126 ~40 public paths plus EXACT/PREFIXES/GET arrays; matchesPathOrChild exposes all children. | New sensitive child route under allow-listed prefix could become public. | Per-route confirmation; prefer explicit per-route public flags. | apps/web/src/middleware.ts |  |

| onboarding-flow-06 | onboarding-flow | Reliability | saveServices writes services non-atomically with no rollback on partial failure | saveServices loops POST /api/services per provider (onboarding-client.tsx:707-774); mid-loop SERVICE_LIMIT_REACHED/SUBSCRIPTION_REQUIRED throws after earlier persisted, no rollback; retry re-POSTs saved providers. | Inconsistent partial state and potential duplicate creation on retry. | Pre-check plan limit before loop; make batch idempotent/transactional; confirm duplicate-guard covers retry. | apps/web/src/app/onboarding/onboarding-client.tsx:707-774 |  |

| onboarding-flow-07 | onboarding-flow | Data | No unique constraint on UserEvent; progress dedup is TOCTOU | UserEvent has only non-unique indexes, no @@unique([userId,event]) (schema.prisma:1190-1209); progress route dedups with findFirst then create. | Concurrent double-submits can create duplicate progress/funnel rows, inflating analytics. Functionally harmless (set membership). | Add unique constraint scoped appropriately; use upsert/createMany skipDuplicates for once-per-user events. | packages/db/prisma/schema.prisma:1190-1209; apps/web/src/app/api/onboarding/progress/route.ts:62-76 |  |

| onboarding-flow-08 | onboarding-flow | Reliability | SERVICES_SKIPPED/MOVING_SKIPPED events are purgeable by retention | RETAINED_USER_EVENT_NAMES keeps only LEGAL_CONSENT and ONBOARDING_COMPLETED (user-event-retention.ts:12); baseEligibleWhere purges everything else past window. | User mid-onboarding after retention window could lose skip markers and be bounced back to services/moving step. | Retain skip events too, or treat absence as not-skipped only for actively-onboarding users. | apps/web/src/lib/user-event-retention.ts:12,70-74 |  |

| onboarding-flow-09 | onboarding-flow | Architecture | Onboarding completion derived from event rows rather than a durable column | Completion/step reconstructed each request from UserEvent rows + record counts across tables (onboarding-progress.ts; post-auth-redirect.ts:85-146). | Fragile to event-semantics changes and scope mismatches; every gate hit re-queries several tables. | Consider denormalized User.onboardingCompletedAt as authoritative source. | apps/web/src/lib/onboarding-progress.ts; apps/web/src/lib/post-auth-redirect.ts |  |

| repo-overview-05 | repo-overview | Security | Staging/non-prod indexability relies on host-regex and env heuristics | apps/web/src/lib/seo.ts:11 STAGING_HOST_PATTERN; custom staging domain not matching regex indexable unless APP_ENV set. | Staging on custom domain could be indexed, exposing pre-release content. | Confirm APP_ENV=staging on every non-prod deploy; default to noindex unless APP_ENV==production. | apps/web/src/lib/seo.ts; apps/web/src/middleware.ts; apps/web/src/app/layout.tsx |  |

| route-map-04 | route-map | UI/UX | Web server list pages own no empty/error state; no route-level loading.tsx anywhere | (app) addresses/dashboard/providers/services pages render only <XClient initial/>; empty markers in -client companions; no **/loading.tsx; only error.tsx + not-found.tsx. | No route-level Suspense fallback; first paint depends on in-component skeletons. Inconsistent but acceptable. | Optionally add loading.tsx for heavy server pages. | apps/web/src/app/(app)/{addresses,dashboard,providers,services}/page.tsx |  |

| route-map-05 | route-map | Architecture | Multiple token/cookie sub-sessions outside the primary auth model | mover-portal, partner-portal, HMAC token pages (blog/preview, invitations, reset-password, verify-email, account/setup-password) gate in-page not via standard middleware. | Distinct trust boundaries downstream audits must treat separately. | Track as separate auth surfaces; ensure each token single-use/expiring and rate-limited. | apps/web/src/app/movers/portal/page.tsx; apps/web/src/app/partners/portal/page.tsx; apps/web/src/app/blog/preview/[token]/page.tsx |  |

| security-surface-02 | security-surface | Architecture | Internal/cron/webhook route prefixes public at middleware layer (fail-open by omission) | middleware.ts L76-86 lists /api/internal/,/api/cron/,/api/webhooks/ in PUBLIC_API_PREFIXES; isPublicApi short-circuits auth; protection only inside each handler. | Any new route under these prefixes that forgets its in-route secret/signature check is internet-exposed by default; boundary fail-open. | CI/lint guard or test asserting every route under these prefixes calls its guard; consider shared wrapper enforcing secret check. | apps/web/src/middleware.ts; apps/admin/src/middleware.ts; apps/web/src/lib/cron-guard.ts; apps/web/src/lib/internal-secrets.ts | uncertain (sev High→Low) |

| security-surface-07 | security-surface | Reliability | Missing internal secrets cause security-event emission to silently no-op | admin middleware emitSecurityEvent returns early if getInternalCallerSecret unset (INTERNAL_WEBHOOK_SECRET unset); no boot-time hard gate. | If INTERNAL_WEBHOOK_SECRET unset, admin BLOCKED_IP/SESSION_HIJACK/break-glass events dropped without error, blinding detection. | Boot assertion required internal secrets present; log when emitSecurityEvent skips. | apps/admin/src/middleware.ts; apps/web/src/lib/internal-secrets.ts |  |

| security-surface-10 | security-surface | Security | Web CSRF origin allow-list depends on req.nextUrl.origin correctness behind proxies | applyCsrfCheck gates on Origin==req.nextUrl.origin + Referer fallback; admin uses multi-candidate getAllowedOrigins accounting for x-forwarded-host. | Behind Host-rewriting proxy req.nextUrl.origin may differ, causing false 403s; SameSite=lax cookie remains backstop. | Align web CSRF origin check with admin multi-candidate allow-list. | apps/web/src/middleware.ts; apps/admin/src/middleware.ts |  |

| security-surface-11 | security-surface | Security | Confirm all ?redirect= consumers route through normalizeAppRedirectPath | safe-redirect.ts normalizeAppRedirectPath correct guard; middleware sets redirect from server pathname; post-auth-redirect.ts and sign-in consume user-supplied ?redirect= not traced end-to-end. | If any consumer uses user-supplied redirect raw, reintroduces open-redirect/post-login redirect-to-attacker. | Verify sign-in page and post-auth-redirect.ts pass redirect through normalizeAppRedirectPath. | apps/web/src/lib/safe-redirect.ts; apps/web/src/lib/post-auth-redirect.ts |  |

| security-surface-06 | security-surface | Architecture | Two independent rate-limiter implementations (user upstash vs admin bespoke REST) | rate-limit.ts uses @upstash/ratelimit; admin middleware reimplements via raw REST INCR/EXPIRE/TTL with own fail-open/closed and memory fallback. | Duplicated fail-mode logic increases chance of divergent behavior during Redis outage. | Extract shared limiter with one fail-mode contract. | apps/web/src/lib/rate-limit.ts; apps/admin/src/middleware.ts |  |

| security-surface-08 | security-surface | Security | Field encryption and backup HMAC reuse the same FIELD_ENCRYPTION_KEY | encryption.ts signBackup/verifyBackupSignature derive HMAC key from same FIELD_ENCRYPTION_KEY used for AES-256-GCM. | Key reuse for confidentiality and integrity is a hygiene smell; no concrete break. | Derive separate sub-keys (HKDF) for encryption vs backup HMAC. | packages/shared/src/encryption.ts |  |

| services-catalog-02 | services-catalog | Architecture | Three divergent StateRule seeders upsert the same table (coverage/format drift) | seed-state-rules.ts seeds 5 states; seed-data/state-rules.ts 51 rows; seed.ts:103 JSON shape; all upsert StateRule by stateCode. | Whichever runs last determines coverage (5 vs 51) and format; wrong one truncates coverage or corrupts format. | Keep seed-data/state-rules.ts as single source; delete/mark others as fixtures after confirming scripts. | packages/db/prisma/seed-state-rules.ts; packages/db/prisma/seed-data/state-rules.ts; packages/db/prisma/seed.ts; packages/db/prisma/seed-master.ts:79 |  |

| services-catalog-03 | services-catalog | Data | /api/state-rules omits populated StateRule columns (data unreachable) | api/state-rules/route.ts:25-33 returns only stateCode/stateName/dmvRules/voterRegistration/taxInfo + static links; utilityInfo/insuranceRules/commonProviders seeded and modeled but never exposed. | Seeded utility/insurance/provider guidance dead through this API; wasted storage. | Expose the columns or drop from model and seeds. | apps/web/src/app/api/state-rules/route.ts:25; packages/db/prisma/schema.prisma:723 |  |

| services-catalog-04 | services-catalog | Reliability | Duplicate service create is check-then-act with no backing unique constraint | api/services/route.ts:260-271 reads findDuplicateTrackedService then creates; Service has only non-unique @@index, no unique on (scope,addressId,category,provider). | Two concurrent identical POSTs both pass guard and create duplicate tracked services. | Add partial/unique DB constraint or wrap check+create in transaction; add concurrent-create test. | apps/web/src/app/api/services/route.ts:260; apps/web/src/lib/service-duplicate-guard.ts:32; packages/db/prisma/schema.prisma:555 |  |

| services-catalog-05 | services-catalog | Security | Workspace duplicate guard scoped by workspace, not acting member; 409 leaks existingServiceId | findDuplicateTrackedService uses activeTrackedServiceWhereForScope({workspaceId}); duplicateServiceError returns existingServiceId. | CHILD/MEMBER who cannot view a peer service still gets 409 with that service id, leaking existence and blocking a legit add. | For non-manager members scope duplicate check to acting member (childSelfOnly), or omit existingServiceId from 409. | apps/web/src/lib/service-duplicate-guard.ts:39; apps/web/src/lib/service-active.ts:32 |  |

| services-catalog-08 | services-catalog | Test | No unit test for service-visibility.ts redaction or service-active.ts | Inventory shows duplicate-guard/sensitive-fields/logo-enrichment tests but no service-visibility.test.ts or service-active.test.ts; route tests do not exercise CHILD/VIEW_ONLY/MEMBER redaction. | RBAC field-masking and active-tracked filtering unverified by tests. | Add unit tests for redactService across roles/legacy mode and NON_TRACKED exclusion. | apps/web/src/lib/service-visibility.ts; apps/web/src/lib/service-active.ts |  |

| services-catalog-09 | services-catalog | UI/UX | Official government source links withheld when no StateRule row exists | api/state-rules/route.ts:24-33 returns stateRule:null for unknown states, so state-agnostic GOVERNMENT_INFO_SOURCE_LINKS attached only when DB row exists. | User moving to not-yet-seeded state receives zero official links even though links never depend on state. | Return static officialSources even when stateRule is null. | apps/web/src/app/api/state-rules/route.ts:24 |  |

| services-catalog-06 | services-catalog | Security | AUTH-015 sensitive-field redaction is dormant while WORKSPACE_MODEL_ENABLED is OFF | isWorkspaceModelEnabled defaults OFF so resolveWorkspaceDataScope returns legacyDataScope and canViewSensitiveService short-circuits true when !workspaceMode. | Not a live vuln in single-user mode; redaction control unverified end-to-end until flag flips. | Add workspace-mode redaction integration test and rollout-gate checklist item. | apps/web/src/lib/service-visibility.ts:54; apps/web/src/lib/workspace-context.ts:96 |  |

| services-catalog-07 | services-catalog | Reliability | decrypt called without isEncrypted guard on read (asymmetric with encrypt path) | decryptServiceSensitiveFields calls decrypt(value) for any truthy value whereas encryptValue guards with isEncrypted. | Legacy plaintext rows passed to decrypt unconditionally; safety depends on decrypt handling non-encrypted input. | Confirm decrypt safe no-op on plaintext; if not, guard symmetrically with isEncrypted. | apps/web/src/lib/service-sensitive-fields.ts:19; apps/web/src/lib/shared-encryption.ts |  |

| services-catalog-10 | services-catalog | Reliability | createAuditLog awaited without try/catch in service create path | api/services/route.ts:290 awaits createAuditLog after service.create with no try/catch (unlike provider-stats increment). | Audit-log failure after row created surfaces as 500, client believes creation failed while row persists. | Wrap audit write in best-effort try/catch. | apps/web/src/app/api/services/route.ts:290 |  |

| ui-ux-02 | ui-ux-baseline | Theme | Two different brand lockups across surfaces | Marketing Wordmark one word LocateFlow Playfair 900; app sidebar Locate+italic foil flow DM Sans semibold. | Inconsistent brand presentation between marketing and app shell. | Standardize one wordmark lockup component reused in both. | apps/web/src/components/marketing/logo.tsx; apps/web/src/components/layout/sidebar.tsx |  |

| ui-ux-03 | ui-ux-baseline | Theme | Light mode relies on a brittle hand-written per-utility override block | globals.css:619-767 manually remaps dozens of utilities (text-white/90..10, bg-white/*, text-amber-300) to light-mode values. | New opacity/utility not covered for light mode unless override added by hand; contrast regressions. | Replace opaque white/black utilities with semantic tokens so light mode derives automatically. | apps/web/src/styles/globals.css; apps/web/src/components |  |

| ui-ux-04 | ui-ux-baseline | UI/UX | Potential flash-of-incorrect-theme for light/system users | CSS default theme dark; ThemeProvider defaultTheme=dark with suppressHydrationWarning; light/system-light users depend on next-themes inline script resolving pre-paint. | Possible one-frame dark flash on initial load for light-theme users. | Verify next-themes blocking script runs before first paint; consider color-scheme meta. | apps/web/src/components/theme-provider.tsx; apps/web/src/styles/globals.css |  |

| ui-ux-05 | ui-ux-baseline | Theme | Static theme-color meta stays dark in light mode and mismatches surface token | layout.tsx:180 hardcodes theme-color #0A0F18; admin #171E2B; neither switches with light theme; #0A0F18 != --bg #070B14. | Mobile browser/PWA chrome stays dark in light theme; third unsynced value. | Drive theme-color from resolved theme; source from canonical token. | apps/web/src/app/layout.tsx; apps/admin/src/app/layout.tsx |  |

| ui-ux-06 | ui-ux-baseline | Theme | Mobile theme switching is partial — static theme-import components do not live-switch | theme.ts:36-45 components using static theme import keep dark palette, only update on reload; only useAppTheme components live-switch. | Inconsistent mobile appearance on theme change; some screens flip, others stay dark. | Migrate static theme import sites to useAppTheme; lint rule banning static import. | apps/mobile/src/lib/theme.ts |  |

| ui-ux-07 | ui-ux-baseline | Accessibility | Low-opacity foreground tokens and gold focus ring may fail WCAG AA contrast | design-tokens.ts:116-121 --fg-3 0.43, --fg-4 0.30, --fg-muted 0.22 over --bg #070B14; focus ring rgba(203,164,94,0.55). | Tertiary/muted text and focus outlines likely below 4.5:1 / 3:1, reducing legibility. | Measure contrast; raise minimum opacities or use solid color meeting AA. | packages/shared/src/design-tokens.ts; apps/web/src/styles/globals.css |  |

| ui-ux-10 | ui-ux-baseline | Performance | Web ships 6 font families including 3 legacy fallbacks to every visitor | layout.tsx:29-80 loads Geist, Geist Mono, Fraunces (legacy) plus Playfair Display, DM Sans, DM Mono. | Extra font payload/CSS vars on every page for superseded fonts. | Grep remaining font references, migrate, remove legacy loaders. | apps/web/src/app/layout.tsx |  |

| ui-ux-11 | ui-ux-baseline | Accessibility | CardTitle always renders h3, can break heading outline | ui/card.tsx:41 CardTitle fixed h3; card grids can emit multiple h3 without h1/h2 ancestor. | Inconsistent heading hierarchy hurts screen-reader navigation (WCAG 1.3.1). | Allow CardTitle to accept as/heading-level prop. | apps/web/src/components/ui/card.tsx |  |

| ui-ux-16 | ui-ux-baseline | Accessibility | Dialog focus trap is a reduced implementation (no description wiring, single focusable scan) | ui/dialog.tsx:53-119 scans focusables once on open, no aria-describedby linking DialogDescription, scrim aria-hidden clickable div. | Dynamically added controls not in trap; description not announced; edge cases vs vetted library. | Wire aria-describedby; re-query focusables on Tab, or adopt Radix Dialog. | apps/web/src/components/ui/dialog.tsx |  |

| ui-ux-18 | ui-ux-baseline | UI/UX | Marketing header hardcodes English nav labels, violating i18n convention | marketing-header.tsx:15-22 hardcodes Features/Why free/Pricing/Help/Blog/FAQ; sidebar comment forbids hardcoded nav strings. | Marketing nav does not localize, inconsistent with product i18n. | Move marketing nav labels to next-intl keys. | apps/web/src/components/marketing/marketing-header.tsx |  |

| ui-ux-09 | ui-ux-baseline | Architecture | Legacy color naming (orange to gold) aliased throughout creates reasoning traps | design-tokens.ts:31-90,275-293 and globals.css:41-57 alias brand.orange/orangeScale/--orange-* onto Gold/Sapphire intentionally. | bg-orange-500 renders gold; contributors mis-reason about color intent. | Plan codemod to rename legacy orange tokens to gold/primary once runtime token gen lands. | packages/shared/src/design-tokens.ts; apps/web/src/styles/globals.css |  |

| ui-ux-13 | ui-ux-baseline | Dead Code | Duplicated backdrop-blur-sm class on mobile menu scrim | app-shell.tsx:117 className includes backdrop-blur-sm backdrop-blur-sm. | Harmless duplicate utility; minor smell. | Remove the duplicate class. | apps/web/src/components/layout/app-shell.tsx |  |

| ui-ux-19 | ui-ux-baseline | UI/UX | EmptyState actions bypass the Button primitive | empty-state.tsx:36-37,49-63 styles primary/secondary actions inline instead of Button. | Visual/behavioral divergence from canonical Button. | Render EmptyState actions with Button (asChild for links). | apps/web/src/components/shared/empty-state.tsx |  |

