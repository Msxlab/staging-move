# Risk Register

Updated: 2026-06-21

## Verified Risks

| ID | Severity | Area | Status | Evidence | Summary | Recommended Action |
| --- | --- | --- | --- | --- | --- | --- |
| RISK-001 | High | Deployment/database | Mitigated in branch | `package.json:8`, `Dockerfile:127`, `.github/workflows/ci.yml:118-141`, `docker-compose.prod.yml:54-84`, `docker/migrate.Dockerfile` | Root runtime startup no longer runs `prisma migrate deploy`; CI/compose/migrator paths still own migrations as separate deploy steps. | Verify deployed platform run command does not reintroduce inline migrations; keep migrations in a dedicated deploy step before smoke tests. |
| RISK-002 | Medium | Rate limits/admin step-up/locks | Mitigated in branch | `apps/web/src/lib/rate-limit.ts:236-286`, `apps/web/src/lib/rate-limit.ts:407-415`, `apps/admin/src/middleware.ts`, `apps/admin/src/lib/auth-step-up-store.ts`, `apps/admin/src/lib/distributed-lock.ts` | Web and admin step-up controls already support distributed state; admin middleware route limiter now uses Upstash REST when configured, with short fail-closed behavior when configured Redis errors in production-like runtimes. Missing Upstash still degrades to memory and is treated as a readiness failure. | Make Upstash a production/staging readiness gate; test 429 behavior and configured Redis outage handling before signoff. |
| RISK-003 | Low | Admin backup | Mitigated in branch | `apps/admin/src/app/api/backup/sql-dump/route.ts`, `apps/admin/src/app/api/backup/sql-dump/route.test.ts` | SQL dump download filenames are sanitized and emitted with both safe `filename` and RFC 5987 `filename*`; the route test covers CR/LF/header-injection style database names. | Re-run SQL dump route tests after install; verify a real staging dump download opens and audit log is written. |
| RISK-004 | Low | Cron/reliability | Mitigated in branch | `apps/web/src/app/api/cron/partner-consents/[id]/refresh/route.ts`, `apps/web/src/middleware.test.ts` | The dead legacy `/api/partner-consents/[id]/refresh` route is absent; the canonical route is under `/api/cron/...` and middleware tests keep the legacy path sealed outside the cron namespace. | Re-run middleware and cron route tests after install; verify the scheduler uses `/api/cron/partner-consents/[id]/refresh`. |
| RISK-005 | Low | Mobile OAuth schema | Mitigated in branch | `packages/db/prisma/schema.prisma:117-132`, `packages/db/prisma/migrations/20260621120000_mobile_oauth_code_challenge_required/migration.sql`, `apps/web/src/lib/mobile-oauth.ts`, OAuth init/callback routes, `apps/web/src/app/api/mobile/auth/exchange/route.ts` | Runtime already enforces PKCE and new create paths require a valid challenge; this branch deletes legacy NULL/empty mobile handoff rows, makes `MobileOAuthCode.codeChallenge` `NOT NULL`, and requires `code_verifier` at exchange request validation. | Run Prisma validate/migrate in staging and re-run mobile OAuth exchange/callback tests after install. |
| RISK-006 | Medium | Mobile IAP token storage | Mitigated in branch | `packages/db/prisma/schema.prisma:250-254`, `apps/web/src/lib/iap-common.ts`, `apps/admin/src/app/api/billing/route.ts`, `apps/admin/src/app/api/subscriptions/route.ts` | New Google Play writes store purchase tokens in `purchaseTokenEncrypted`, clear plaintext `purchaseToken`, and retain `purchaseTokenHash` for lookup; old plaintext rows remain as legacy fallback until revalidated or migrated with app-runtime encryption. | Run the new migration, verify `FIELD_ENCRYPTION_KEY`, revalidate active Play Store rows, then audit for non-null legacy plaintext tokens before staging signoff. |
| RISK-007 | Medium | Cross-platform theme parity | Mitigated in branch | `packages/shared/src/design-tokens.ts`, `packages/shared/src/constants.ts`, `apps/web/tailwind.config.ts`, `apps/web/src/components/marketing/logo.tsx`, `apps/web/src/styles/globals.css`, `apps/web/src/styles/aurora.css`, `apps/admin/src/app/aurora.css`, `apps/mobile/src/lib/theme.ts`, `apps/mobile/tailwind.config.ts`, `apps/mobile/app/(auth)/sign-in.tsx`, `apps/mobile/android/app/src/main/res/values/colors.xml` | Mobile runtime theme, NativeWind palette, shared legacy color constants, web Tailwind font utilities, display/prose/wordmark fallbacks, and native/platform UI bits were using divergent or old palette/typography values while shared tokens define Aurora + Playfair/DM as the source of truth; sign-in Apple logo contrast also drifted from sign-up. | Run web/admin/mobile screenshots after install, compare core flows in light/dark, and audit remaining component-level hardcoded accents plus static SVG/blog asset typography. |
| RISK-008 | Medium | Portal magic-link sessions | Mitigated in branch | `apps/web/src/lib/mover-portal-auth.ts`, `apps/web/src/lib/partner-portal-auth.ts`, `apps/web/src/app/api/movers/portal/request/route.ts`, `apps/web/src/components/movers/mover-portal-login.tsx` | Mover/partner portal magic links previously behaved as long-lived sessions. The branch shortens tokens/cookies to 24 hours, supersedes prior mover/partner links on re-request, and adds mover per-email throttling. | Run portal request/enter/logout browser tests after install; verify email delivery/copy, expired-token UX, revocation, and rate-limit behavior with Redis configured. |
| RISK-009 | Medium | Download headers | Mitigated in branch | `apps/admin/src/lib/http-download.ts`, `apps/web/src/lib/http-download.ts`, admin CSV/backup/mover-document download routes, web account-export/PDF/dossier routes | Export and download routes now share safe attachment filename construction and emit both `filename` and RFC 5987 `filename*`; regression tests cover CR/LF-style dynamic filename inputs. | Re-run admin/web download route tests after install; browser-smoke real CSV/PDF/archive downloads in staging. |
| RISK-010 | Low | Public tracking payloads | Mitigated in branch | `apps/web/src/app/api/tracking/event/route.ts`, `apps/web/src/app/api/tracking/session/route.ts` | Tracking routes now parse malformed JSON as 400, sanitize and truncate client-supplied event/session fields to DB-safe lengths, and clamp page-view updates. | Re-run tracking route tests after install and verify analytics consent/session smoke in browser. |
| RISK-011 | Low | Admin security metadata cache | Mitigated in branch | `apps/admin/src/app/api/auth/mfa/trusted-devices/route.ts` | Trusted-device list and revoke responses include explicit `Cache-Control: no-store` so security-device metadata is not cached by browsers or intermediaries. | Re-run trusted-device route tests after install and browser-smoke the admin security panel. |
| RISK-012 | Medium | Workspace soft-delete consistency | Mitigated in branch | `apps/web/src/app/api/workspaces/[id]/route.ts`, `apps/web/src/app/api/workspaces/[id]/members/*`, `apps/web/src/app/api/workspaces/[id]/invitations/*`, `apps/web/src/app/api/workspaces/[id]/sync/route.ts`, `apps/web/src/app/api/workspaces/[id]/managed-sync/route.ts`, `apps/web/src/app/api/workspaces/[id]/transfer/route.ts`, `apps/web/src/app/api/workspaces/[id]/rename/route.ts` | Active workspace operations now require the caller's membership to resolve through a non-deleted workspace; restore/delete remain lifecycle exceptions. | Re-run workspace route tests after install and browser-smoke soft-delete/restore, invite, roster, managed sync, and transfer flows. |
| RISK-013 | Medium | Admin auth response cache | Mitigated in branch | `apps/admin/src/app/api/auth/login/route.ts`, `apps/admin/src/app/api/auth/sessions/route.ts`, `apps/admin/src/app/api/auth/mfa/setup/route.ts`, `apps/admin/src/app/api/auth/mfa/verify/route.ts` | Sensitive admin auth responses now send no-store/no-cache headers when returning auth state, MFA challenge/setup material, session revoke handles, or MFA verification state. | Re-run admin auth route tests after install and browser-smoke login, MFA setup/verify, and session revoke flows. |
| RISK-014 | Medium | Backup retention step-up | Mitigated in branch | `apps/admin/src/app/api/backup/retention/route.ts` | Manual destructive backup retention now requires password/MFA step-up before deleting backup rows or offsite archive objects; cron backup-secret and dry-run paths stay non-step-up. | Re-run backup retention tests after install; browser-smoke dry-run, failed step-up, confirmed cleanup, and cron-triggered cleanup in staging. |
| RISK-015 | Low | Route/link completeness | Mitigated in branch | `apps/web/src/app/movers/apply/page.tsx`; static route-link inventory | The mover application page linked to a missing `/legal/terms` route even though the canonical Terms page is `/terms`. Static web/admin/mobile literal page/API target scans now report zero misses after the fix. | Browser-smoke public mover/partner apply forms, legal links, and core mobile navigation after install. |
| RISK-016 | Low | i18n raw-key / fallback debt | Mitigated in branch | `apps/mobile/src/i18n/messages/en.json`, `apps/mobile/src/i18n/messages/es.json`; static i18n inventory | Mobile vehicle-check recall count now has a base translation key in addition to plural variants; namespace-aware i18n static scan reports EN/ES parity and zero no-fallback missing literal keys across web, admin, and mobile. | Review mobile calls that rely on explicit fallback strings and promote launch-critical fallback copy into EN/ES message files before release. |
| RISK-017 | Low | Deployment smoke docs | Mitigated in branch | `README.deploy.md`, `docs/deploy/digitalocean-app-platform-web.md`; admin health routes | Deployment smoke docs pointed unauthenticated checks at admin `/api/health`, but that route is the authenticated detailed health view; public liveness is `/api/healthz`. | Use `/api/healthz` for unauthenticated admin uptime checks and verify authenticated `/api/health` only after admin login. |
| RISK-018 | Low | Env/config catalog coverage | Mitigated in branch | `packages/shared/src/env-catalog.ts`; static env reference inventory | Several optional process-env knobs used by web/admin/mobile were documented in code or examples but absent from the readiness catalog, so staging operators could miss them in the admin/env view. The combined env/runtime catalog now covers all statically detected env references after excluding platform/QA-script-only keys. | Re-run env readiness after install with real staging values set locally; optional managed runtime-config keys still need operator review for integrations in scope. |
| RISK-019 | Medium | Cookie/session staging consistency | Mitigated in branch | `apps/web/src/lib/mover-portal-auth.ts`, `apps/web/src/lib/partner-portal-auth.ts`, `apps/web/src/app/api/consent/ccpa/route.ts`, `apps/web/src/app/api/user/locale/route.ts`, OAuth callback routes, `apps/web/src/app/api/partner-consents/oauth/callback/route.ts`, `apps/admin/src/middleware.ts` | Some cookies used production-only secure flags instead of staging/preview/HTTPS-aware policy, and several callback/middleware paths relied on unscoped `cookies.delete()` for root-scoped cookies. This could leave stale OAuth/session/consent cookies or make staging cookie posture differ from production-like HTTPS. | Re-run cookie-focused unit tests after install and browser-smoke portal magic links, CCPA opt-out/revoke, locale switching, OAuth callback cleanup, partner connector callback cleanup, and invalid admin-session logout. |
| RISK-020 | Medium | Admin invite staging URLs | Mitigated in branch | `apps/admin/src/app/api/team/route.ts`, `apps/admin/src/app/api/team/route.test.ts` | Admin invite set-password links could fall back to `https://admin.locateflow.com` when `ADMIN_APP_URL` / `NEXT_PUBLIC_ADMIN_URL` were missing and `NODE_ENV=production`, which is also true for staging builds. | Verify staging has explicit admin URL env/runtime config, re-run admin team route tests after install, and send a staging invite to confirm emailed links stay on the staging admin origin. |
| RISK-021 | Low | Uptime cron staging targets | Mitigated in branch | `apps/web/src/app/api/cron/uptime-check/route.ts`, `apps/web/src/app/api/cron/uptime-check/route.test.ts`, `apps/admin/src/app/api/team/route.ts` | Uptime cron default targets and one admin invite final fallback could treat `NODE_ENV=production` as enough to use production domains, so a misconfigured staging/preview deploy might probe production or build links from production defaults. | Configure explicit staging `UPTIME_WEB_BASE_URL`, `UPTIME_ADMIN_BASE_URL` or `NEXT_PUBLIC_ADMIN_URL`; re-run uptime/admin-team route tests after install and verify staging cron probes only staging/local targets. |
| RISK-022 | Medium | Web navigation semantics | Mitigated in branch | `apps/web/src/app/interactive-nesting-regression.test.ts`, address/moving/services/settings/support web pages, `apps/web/src/components/shared/empty-state.tsx`, `apps/web/src/components/shared/upgrade-prompt.tsx`, `apps/web/src/components/layout/notification-center.tsx` | Multiple navigation CTAs nested `<button>` inside `Link`/anchor elements, creating invalid interactive markup that can produce inconsistent click, keyboard, and assistive-technology behavior. | Re-run the new interactive-nesting regression test after install and browser-smoke dashboard, address, moving, services, settings, support, notifications, empty states, and upgrade prompts. |
| RISK-023 | Medium | Email template link hardening | Mitigated in branch | `apps/admin/src/lib/email-template-sanitizer.ts`, `packages/shared/src/email-html-sanitizer.ts`, `apps/admin/src/lib/email-template-sanitizer.test.ts`, `packages/shared/src/email-html-sanitizer.test.ts` | Sanitized email/template HTML could preserve `<a target="_blank" rel="nofollow">` without adding `noopener noreferrer`, leaving opener isolation dependent on template-supplied rel content. | Re-run admin sanitizer tests after install; wire shared package tests into the approved test gate, then verify target-blank links preserve existing rel tokens while always adding `noopener noreferrer`. |
| RISK-024 | Low | Shared package test execution | Open | `packages/shared/package.json`, root `package.json`, `packages/shared/src/*.test.ts` | Shared source has multiple test files but no shared package `test` script and root `verify:tests` omits shared, so shared-only regressions may not run in normal verification. | With approval, add a shared Vitest execution path and include it in root verification; avoid dependency/script churn until the owner approves package/test-gate changes. |
| RISK-025 | Low | Source text hygiene | Mitigated in branch | `packages/shared/src/email-html-sanitizer.ts`, `apps/admin/src/app/(admin)/insights/insights-data.ts` | Literal NUL/control bytes in TypeScript source made text tools classify files as binary and weakened static audit visibility. | Keep control-character regexes and delimiter bytes escaped in source; re-run text/binary source scans after install-backed formatting/typecheck. |

## Positive Control Notes

- User/admin auth routes re-check DB session state rather than trusting middleware JWT alone.
- Admin irreversible deletion has strong step-up, OTP, audit, and Stripe reconciliation controls.
- Stripe and store purchase flows verify server-side before granting entitlement.
- Public uploads and remote fetches use size/type/host controls.
- Connector framework enforces HTTPS allowlists, redirect revalidation, circuit breakers, retries, idempotency, and fallback.

## Unverified Risks

| Area | Reason |
| --- | --- |
| Live production env readiness | `.env`/deployed env values were intentionally not read. |
| Real provider integrations | No live USPS/Stripe/App Store/Google Play/Resend/R2 calls were made. |
| Full route authorization coverage | 295 API route files were inventoried, but not all were reviewed line-by-line. |
| Fallback-backed mobile copy | Static i18n scan found mobile missing exact keys only when calls also provide explicit fallback copy; this avoids raw-key runtime display but is still localization debt. |
| Runtime performance/load | No tests or benchmarks were run. |
| Static SVG/blog asset typography | Public SVG logo/OG/blog art still names legacy Fraunces/Geist fonts; external SVG font loading may fall back differently and needs visual asset review. |
| Optional runtime-config examples | The code/runtime catalog is covered, but many optional managed Runtime Config keys are intentionally configured through admin/runtime UI rather than both env example files. |
| Browser cookie behavior | Cookie fixes are statically implemented, but real browser cookie set/clear behavior still needs staging proof over HTTPS. |
| Admin invite email delivery | Invite link origin is fixed statically, but real email delivery and clicked set-password flow still need staging proof. |
| Uptime cron target proof | Uptime fallback behavior is fixed statically, but staging cron still needs runtime proof with configured staging target URLs. |
| Web keyboard/focus QA | Nested interactive markup is removed statically, but full keyboard/focus and click behavior still needs browser proof once the app runs. |
| Shared test gate | Shared test files exist but are not currently wired into the normal root verification command. |
| Delegated security scan coverage | Codex Security preflight is incomplete for full delegated scan claims because active multi-agent mode/capacity is unknown in this session. |
