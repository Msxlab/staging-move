# Phase 1 CI Green Handoff

## Branch and commits

- Branch: `codex/phase1-integration`
- Phase-1 integration commit: `4dcb1390 feat: integrate phase 1 experiments`
- Current repo status after that commit still contains unrelated dirty/untracked files that were intentionally excluded, including `AGENTS.md`, `apps/admin/next-env.d.ts`, `apps/admin/tsconfig.json`, `docs/ai-ops/04-commands.md`, `SYSTEM_AUDIT_REPORT/**`, and non-Phase-1 docs.
- Final CI was run from a clean detached worktree at `C:\Users\Windows\Documents\move-main\phase1-integration-clean-1129` checked out at `4dcb1390`.

## What was committed

- Experiment 1: Command Center dependability and de-noise behind `ux_ai_briefing_experience_v1`.
- Experiment 2: point-of-action trust confirmation behind `ux_trust_copy_v1`.
- Experiment 3: widened onboarding teaser behind `ux_onboarding_teaser_v1`.
- Phase-1 telemetry persistence through the existing consent-gated `trackEvent` -> `/api/tracking/event` -> `UserEvent` path.
- UserEvent retention and optional sampling policy with dry-run deletion by default.
- Briefing typecheck fix.
- Phase-1 analytics, experiment, QA, and handoff docs.

## Ours vs pre-existing triage

### Clean main baseline

Baseline worktree: `C:\Users\Windows\Documents\move-main\phase1-clean-main-1118` at `5fe35bbc`.

- `pnpm install --frozen-lockfile`: passed with Node engine warning (`wanted node 22.x`, current `v24.13.0`).
- `pnpm lint`: passed.
- `pnpm verify:typecheck`: passed.
- `pnpm --filter @locateflow/admin test`: passed, 118 test files / 745 tests.

### Mobile typed-route errors

Original dirty integration checkout showed:

```text
app/(tabs)/index.tsx(1138,44): error TS2345: Argument of type '"/settings/workspace"' is not assignable...
app/(tabs)/index.tsx(1152,81): error TS2345: Argument of type '"/notifications"' is not assignable...
```

It also reported generated typed-route misses for routes including `/custom-providers`, `/settings/connections`, `/help/tickets`, `/notifications`, `/blog`, `/blog/[slug]`, `/custom-providers/[id]`, `/custom-providers/[id]/edit`, and `/help/tickets/[id]`.

Conclusion: not present on clean main and not present on the clean Phase-1 commit. The red result was caused by dirty-worktree generated mobile route state, specifically an untracked/stale `apps/mobile/.expo/types/router.d.ts` in the original checkout, not by the committed Phase-1 branch.

### Admin `React is not defined` failures

Original dirty integration checkout showed three failures in `apps/admin/src/app/(admin)/runtime-config/runtime-config-client.test.ts`:

```text
ReferenceError: React is not defined
```

The stack pointed at:

```text
apps/admin/src/app/(admin)/runtime-config/runtime-config-client.tsx:224
```

Conclusion: not present on clean main and not present on the clean Phase-1 commit. The red result was caused by unrelated dirty admin config in the original checkout, not by the committed Phase-1 branch.

## Fixes made in this pass

- Committed the Phase-1 change set so the integration branch is no longer fragile/uncommitted.
- No application source fix was required after verifying the clean Phase-1 commit.
- No unrelated dirty files were cleaned, reset, stashed, deleted, or committed.

## Final full CI results

Run from `C:\Users\Windows\Documents\move-main\phase1-integration-clean-1129` at `4dcb1390`.

| Gate | Result | Notes |
|---|---:|---|
| `pnpm install --frozen-lockfile` | Green | Passed with Node engine warning: repo wants Node `22.x`, current runtime was `v24.13.0`. |
| `pnpm lint` | Green | Turbo reported 6 successful tasks. |
| `pnpm verify:typecheck` | Green | Web, admin, mobile, db, and connectors typechecks passed. |
| `pnpm --filter @locateflow/web test` | Green | 278 test files / 2484 tests passed. |
| `pnpm --filter @locateflow/admin test` | Green | 118 test files / 745 tests passed. |
| `pnpm --filter @locateflow/mobile test` | Green | 28 test files / 284 tests passed. |
| `pnpm --filter @locateflow/web e2e -- --project=chromium tests/e2e/public-pages.spec.ts` | Red, known separate | 1 passed / 8 failed. Do not block Phase-1 consolidation on this per task instruction. |

## Public e2e separate failure details

The public e2e suite still fails separately:

- `page.goto` timeouts waiting for `load` on `/`, `/sign-in`, `/sign-up`, `/pricing`, and `/dashboard`.
- `robots.txt is served` expected `/User-agent/` but received `User-Agent: *` casing.
- FAQ raw HTML JSON-LD check expected one `Organization` schema and got zero.
- Evidence artifacts were written under `C:\Users\Windows\Documents\move-main\phase1-integration-clean-1129\apps\web\test-results`.

## Post-merge invariants

- `ux_ai_briefing_experience_v1`, `ux_trust_copy_v1`, and `ux_onboarding_teaser_v1` still default to `control` in `packages/shared/src/ux-experiments.ts`.
- UserEvent retention remains dry-run by default; `USER_EVENT_RETENTION_ENABLED` must be explicitly true before deletion can occur.
- Optional UserEvent sampling remains config-gated and off by default; Phase-1 experiment events are retained at 100%.
- No flag was enabled.
- No retention deletion was run.
- No schema or migration was created or applied.
- No push, deploy, or main merge occurred.

## Recommendation

Treat `codex/phase1-integration` at `4dcb1390` as green for Phase-1 code review, excluding the known separate public e2e suite. Before promotion, run CI from a clean checkout rather than the current dirty local checkout, or first make a human decision on the unrelated dirty admin config and generated mobile route file.
